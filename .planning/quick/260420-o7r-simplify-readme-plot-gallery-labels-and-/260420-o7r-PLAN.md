# Quick Task 260420-o7r: Simplify README plot gallery labels and emphasize EML curves - Plan

**Date:** 2026-04-20
**Status:** Complete

## Goal

Simplify the README plot gallery so the plot itself has only a legend, no visible titles or panel labels, no extrapolation series, and clearly visible verified EML curves.

## Tasks

1. Remove visible SVG title, panel labels, formula labels, axis labels, and status text.
2. Keep only the legend text for target, verified EML, training data, and validation data.
3. Remove extrapolation markers from the plot and README plot prose.
4. Draw verified EML curves after the black target curves so the dashed line is on top.
5. Tighten panel scaling to train plus validation domains so recovered EML curves remain visible.

## Verification

- `python -m xml.etree.ElementTree readme-assets/fit-gallery.svg`
- Visible SVG text check
- SVG series count and extrapolation removal check
- Verified EML ordering check
- README local-link existence check
- `git diff --check`
- `python scripts/validate-ci-contract.py --mode dev --root .`
