---
phase: 58-focused-evidence-and-artifact-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/benchmark.py
  - tests/test_benchmark_contract.py
  - tests/test_benchmark_runner.py
  - tests/test_verifier_demos_cli.py
autonomous: true
requirements:
  - LOGI-04
  - LOGI-05
  - EVID-01
  - EVID-02
  - EVID-03
  - EVID-04
  - EVID-05
must_haves:
  truths:
    - "Focused logistic suite contains compile diagnostics and no warm-start case because strict support did not pass."
    - "Focused Planck suite contains compile diagnostics and no warm-start case because strict support did not pass."
    - "CLI writes deterministic artifacts under the requested v1.10 campaign paths."
    - "Artifacts record support status, depth, nodes, macro hits, and unsupported reasons."
    - "Existing v1.9 focused evidence suites do not regress."
  artifacts:
    - path: "artifacts/campaigns/v1.10-logistic-evidence/"
      provides: "Focused logistic compile diagnostic evidence"
    - path: "artifacts/campaigns/v1.10-planck-diagnostics/"
      provides: "Focused Planck compile diagnostic evidence"
---

<objective>
Add focused v1.10 benchmark suites, CLI coverage, and committed artifacts for logistic and Planck diagnostic outcomes.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused suites and contracts</name>
  <files>src/eml_symbolic_regression/benchmark.py, tests/test_benchmark_contract.py</files>
  <behavior>
    - `v1.10-logistic-evidence` contains `logistic-compile` only.
    - `v1.10-planck-diagnostics` contains `planck-compile` only.
    - Both cases are compile mode and `expect_recovery=False`.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify runner and CLI artifacts</name>
  <files>tests/test_benchmark_runner.py, tests/test_verifier_demos_cli.py</files>
  <behavior>
    - Runner artifacts record logistic depth `15` and Planck depth `14`.
    - CLI benchmark command writes aggregate reports under the requested output roots.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate focused evidence artifacts</name>
  <files>artifacts/campaigns/v1.10-logistic-evidence/, artifacts/campaigns/v1.10-planck-diagnostics/</files>
  <behavior>
    - Committed artifacts match the focused suite outputs.
    - No warm-start or recovered evidence is claimed for logistic or Planck.
  </behavior>
</task>

</tasks>

<verification>
Run:

```bash
PYTHONPATH=src python -m pytest tests/test_benchmark_contract.py tests/test_benchmark_runner.py tests/test_verifier_demos_cli.py -q
```
</verification>

<success_criteria>
Phase 58 is complete when suite contracts, runner artifacts, CLI paths, and committed focused evidence all report the v1.10 outcomes honestly.
</success_criteria>
