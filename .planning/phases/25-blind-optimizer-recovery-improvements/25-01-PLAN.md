---
phase: 25-blind-optimizer-recovery-improvements
plan: 01
type: execute
wave: 1
depends_on: ["24-01"]
files_modified:
  - src/eml_symbolic_regression/optimize.py
  - src/eml_symbolic_regression/diagnostics.py
  - tests/test_optimizer_cleanup.py
  - tests/test_diagnostics.py
requirements: [BLIND-01, BLIND-02, BLIND-03, BLIND-04]
autonomous: true
user_setup: []
must_haves:
  truths:
    - Blind optimizer runs record explicit random and scaffold attempts with seeds and budgets.
    - Generic primitive scaffolds improve verifier recovery or post-snap loss for at least one v1.3 blind failure family without changing verifier status logic.
    - Diagnostics can compare blind v1.4 candidate runs against v1.3 baseline rows using verifier-owned status and loss metrics.
    - Remaining blind failures are classified by likely failure mechanism: soft loss, snap instability, expression depth, verifier mismatch, or non-finite snap.
  artifacts:
    - src/eml_symbolic_regression/optimize.py
    - src/eml_symbolic_regression/diagnostics.py
    - tests/test_optimizer_cleanup.py
    - tests/test_diagnostics.py
  key_links:
    - benchmark.py continues to call fit_eml_tree and verify_candidate; optimizer changes cannot bypass verifier-owned recovery.
---

<objective>
Improve shallow blind optimizer behavior with auditable generic scaffolds and baseline comparison diagnostics.

Purpose: v1.3 showed blind exp/log failures despite shallow paper identities. This plan adds generic candidate-generation starts while preserving the verifier contract.
Output: Optimizer scaffold attempts, failure-mechanism diagnostics, comparison helpers, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-baseline-failure-triage-and-diagnostic-harness/24-01-SUMMARY.md
@.planning/phases/25-blind-optimizer-recovery-improvements/25-CONTEXT.md
@src/eml_symbolic_regression/optimize.py
@src/eml_symbolic_regression/master_tree.py
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/diagnostics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generic primitive scaffold attempts</name>
  <files>src/eml_symbolic_regression/optimize.py, tests/test_optimizer_cleanup.py</files>
  <read_first>src/eml_symbolic_regression/optimize.py, src/eml_symbolic_regression/master_tree.py, tests/test_optimizer_cleanup.py</read_first>
  <action>Add a `scaffold_initializers` field to `TrainingConfig`, defaulting to `("exp", "log")`. When `fit_eml_tree` is called without an explicit initializer, run legal scaffold attempts for each configured variable in addition to random restarts: `exp` when depth >= 1 and `log` when depth >= 3. Record each attempt in `restarts` with `initialization.kind` set to `scaffold_exp` or `scaffold_log`. Keep random restart seeds unchanged. Do not apply scaffolds when an initializer argument is supplied.</action>
  <verify>python -m pytest tests/test_optimizer_cleanup.py -q</verify>
  <acceptance_criteria>
    - src/eml_symbolic_regression/optimize.py contains `scaffold_initializers`.
    - tests/test_optimizer_cleanup.py contains a test that blind `exp` recovers with scaffold provenance.
    - tests/test_optimizer_cleanup.py contains a test that warm-start/custom initializer path does not add scaffold provenance.
  </acceptance_criteria>
  <done>Blind exp/log can recover through generic scaffold attempts while warm-start paths remain isolated.</done>
</task>

<task type="auto">
  <name>Task 2: Add blind failure mechanism diagnostics and comparison helpers</name>
  <files>src/eml_symbolic_regression/diagnostics.py, tests/test_diagnostics.py</files>
  <read_first>src/eml_symbolic_regression/diagnostics.py, tests/test_diagnostics.py, artifacts/diagnostics/v1.4-baseline/triage.json</read_first>
  <action>Add `classify_blind_failure` and `compare_blind_runs` helpers. The classifier must distinguish `recovered`, `soft_loss`, `snap_instability`, `expression_depth`, `non_finite_snap`, and `verifier_mismatch` using run metrics/status. The comparison helper must consume baseline and candidate aggregate rows and return per-formula/seed deltas for verifier status, best loss, post-snap loss, and classification.</action>
  <verify>python -m pytest tests/test_diagnostics.py -q</verify>
  <acceptance_criteria>
    - src/eml_symbolic_regression/diagnostics.py contains `classify_blind_failure`.
    - src/eml_symbolic_regression/diagnostics.py contains `compare_blind_runs`.
    - tests/test_diagnostics.py asserts a snapped high-loss run is not classified as recovered.
  </acceptance_criteria>
  <done>Remaining blind failures have diagnostic labels and comparable deltas.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_optimizer_cleanup.py tests/test_diagnostics.py tests/test_benchmark_runner.py -q
- [ ] PYTHONPATH=src python -m eml_symbolic_regression.cli campaign smoke --case exp-blind --output-root /tmp/eml-v1.4-phase25 --label blind-exp --overwrite
</verification>

<success_criteria>
- Scaffold attempts are explicit in manifests.
- At least one v1.3 blind failure family improves under the same verifier-owned recovery contract.
- No warm-start initializer path is changed.
- Tests pass.
</success_criteria>
