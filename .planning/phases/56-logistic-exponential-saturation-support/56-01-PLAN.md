---
phase: 56-logistic-exponential-saturation-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/compiler.py
  - tests/test_compiler_warm_start.py
autonomous: true
requirements:
  - MOTIF-02
  - LOGI-01
  - LOGI-02
  - LOGI-03
  - LOGI-04
  - LOGI-05
must_haves:
  truths:
    - "Compiler recognizes logistic-like 1/(1+c*exp(a)) structurally through exponential_saturation_template."
    - "Logistic relaxed compile depth is materially lower than archived depth 27 and reports the new macro hit."
    - "Logistic remains strict-unsupported if the unchanged depth 13 gate does not pass."
    - "Warm-start is not attempted or promoted unless strict compile support exists."
    - "Existing Shockley, Arrhenius, Michaelis-Menten, and Planck macro behavior does not regress."
  artifacts:
    - path: "src/eml_symbolic_regression/compiler.py"
      provides: "Reusable exponential saturation compiler motif"
      contains: "exponential_saturation_template"
    - path: "tests/test_compiler_warm_start.py"
      provides: "Logistic compile improvement and strict gate regression tests"
      contains: "test_compile_logistic_uses_exponential_saturation_template"
---

<objective>
Add reusable structural exponential-saturation compile support for logistic-like formulas without formula-name branching or gate relaxation.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exponential_saturation_template</name>
  <files>src/eml_symbolic_regression/compiler.py, tests/test_compiler_warm_start.py</files>
  <behavior>
    - `1/(1+c*exp(a))` validates through `exponential_saturation_template`.
    - Logistic relaxed compile depth is below archived `27`.
    - Macro diagnostics include baseline depth `27`, depth delta, and validation status.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve honest strict and warm-start status</name>
  <files>tests/test_compiler_warm_start.py</files>
  <behavior>
    - Logistic is strict-supported only if depth `<= 13`.
    - If depth remains above `13`, diagnostics stay `unsupported` with strict reason `depth_exceeded`.
    - Existing supported macro demos and Planck diagnostics remain unchanged.
  </behavior>
</task>

</tasks>

<verification>
Run:

```bash
PYTHONPATH=src python -m pytest tests/test_compiler_warm_start.py -q
```
</verification>

<success_criteria>
Phase 56 is complete when logistic has a structural macro-backed exact compile path with materially lower relaxed depth and honest strict/warm-start status under unchanged gates.
</success_criteria>
