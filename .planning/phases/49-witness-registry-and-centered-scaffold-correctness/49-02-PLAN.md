---
phase: 49-witness-registry-and-centered-scaffold-correctness
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - src/eml_symbolic_regression/optimize.py
  - src/eml_symbolic_regression/master_tree.py
  - tests/test_optimizer_cleanup.py
  - tests/test_master_tree.py
  - tests/test_benchmark_runner.py
autonomous: true
requirements:
  - WIT-02
  - WIT-03
  - WIT-04
must_haves:
  truths:
    - "Direct centered fit_eml_tree calls with default scaffolds do not generate scaffold_exp, scaffold_log, or scaffold_scaled_exp attempts."
    - "Direct raw scaffold helper calls on centered SoftEMLTree instances fail closed before mutating slots or embedding expressions."
    - "Optimizer manifests and benchmark run artifacts expose centered scaffold exclusions with operator-family context."
    - "Raw EML scaffold recovery tests still recover exp and scaled_exp through existing raw helper paths."
  artifacts:
    - path: "src/eml_symbolic_regression/optimize.py"
      provides: "Registry-backed direct optimizer scaffold filtering and manifest exclusions"
      contains: "scaffold_exclusions"
    - path: "src/eml_symbolic_regression/master_tree.py"
      provides: "Fail-closed guard on raw scaffold helper methods"
      contains: "centered_family_same_family_witness_missing"
    - path: "tests/test_optimizer_cleanup.py"
      provides: "Direct optimizer centered exclusion and raw default regression tests"
    - path: "tests/test_master_tree.py"
      provides: "Direct helper fail-closed tests"
    - path: "tests/test_benchmark_runner.py"
      provides: "Run artifact and aggregate reason-code visibility tests"
  key_links:
    - from: "src/eml_symbolic_regression/optimize.py"
      to: "src/eml_symbolic_regression/witnesses.py"
      via: "fit_eml_tree resolves config.scaffold_initializers against initial training operator"
      pattern: "resolve_scaffold_plan"
    - from: "src/eml_symbolic_regression/master_tree.py"
      to: "src/eml_symbolic_regression/witnesses.py"
      via: "force_exp/force_log/force_scaled_exp require a same-family witness before slot mutation"
      pattern: "scaffold_witness_for"
    - from: "tests/test_benchmark_runner.py"
      to: "src/eml_symbolic_regression/benchmark.py"
      via: "artifact metrics copy budget scaffold_exclusions and trained optimizer manifest exclusions"
      pattern: "scaffold_exclusions"
---

<objective>
Close optimizer and helper bypasses, then prove reason-code visibility survives execution artifacts.

Purpose: Plan 49-01 prevents centered benchmark budgets from carrying raw scaffolds. This plan applies the same registry contract to direct optimizer callers and direct raw helper methods, while keeping raw EML recovery and verifier-owned status semantics unchanged.

Output: Direct optimizer scaffold filtering, raw helper fail-closed guards, optimizer manifest exclusions, and runner artifact tests.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-CONTEXT.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-RESEARCH.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-PATTERNS.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-01-SUMMARY.md
@.planning/milestones/v1.5-phases/30-bounded-shallow-blind-training-recovery/30-02-SUMMARY.md
@.planning/milestones/v1.7-phases/40-family-aware-master-tree-and-recovery-pipeline/40-01-SUMMARY.md
@.planning/milestones/v1.8-phases/45-centered-integration-fixes/45-01-SUMMARY.md
@src/eml_symbolic_regression/witnesses.py
@src/eml_symbolic_regression/optimize.py
@src/eml_symbolic_regression/master_tree.py
@tests/test_optimizer_cleanup.py
@tests/test_master_tree.py
@tests/test_benchmark_runner.py

<interfaces>
Registry contract produced by Plan 49-01:
```python
CENTERED_FAMILY_SAME_FAMILY_WITNESS_MISSING = "centered_family_same_family_witness_missing"

def scaffold_witness_for(kind: str, operator: EmlOperator) -> ScaffoldWitness | None: ...
def resolve_scaffold_plan(requested: Iterable[str], operator: EmlOperator) -> ScaffoldPlan: ...
```

Existing optimizer entry points:
```python
def fit_eml_tree(inputs, target, config: TrainingConfig, initializer=None, *, verification_splits=None, tolerance=1e-8) -> FitResult: ...
def _training_attempts(config: TrainingConfig, has_external_initializer: bool) -> list[dict[str, Any]]: ...
def _operator_for_step(config: TrainingConfig, step: int, total_steps: int) -> EmlOperator: ...
def _apply_scaffold(model: SoftEMLTree, attempt: Mapping[str, Any]) -> dict[str, Any]: ...
```

Existing direct helper error shape:
```python
class EmbeddingError(ValueError):
    reason: str
    detail: str
    def as_dict(self) -> dict[str, str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter direct optimizer scaffolds and serialize exclusions</name>
  <read_first>
    <file>src/eml_symbolic_regression/optimize.py</file>
    <file>src/eml_symbolic_regression/witnesses.py</file>
    <file>tests/test_optimizer_cleanup.py</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-RESEARCH.md</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-PATTERNS.md</file>
  </read_first>
  <files>src/eml_symbolic_regression/optimize.py, tests/test_optimizer_cleanup.py</files>
  <behavior>
    - Test 1: Raw `TrainingConfig()` still includes and can use `scaffold_exp`.
    - Test 2: Centered `TrainingConfig(operator_family=ceml_s_operator(2.0))` records exp/log/scaled_exp exclusions and has no scaffold attempt kinds.
    - Test 3: Scheduled `TrainingConfig(operator_schedule=(zeml_s_operator(8.0), zeml_s_operator(4.0)))` uses ZEML_8 for scaffold availability, records the same exclusions, and keeps the existing hardening final operator metadata.
  </behavior>
  <action>In `src/eml_symbolic_regression/optimize.py`, import `CENTERED_FAMILY_SAME_FAMILY_WITNESS_MISSING`, `known_scaffold_kinds`, `resolve_scaffold_plan`, and `scaffold_witness_for` as needed. At the start of `fit_eml_tree()`, compute `initial_operator = _operator_for_step(config, 0, max(config.steps, 1))`, `scaffold_plan = resolve_scaffold_plan(config.scaffold_initializers, initial_operator)`, and `effective_config = replace(config, scaffold_initializers=scaffold_plan.enabled)`. Use `effective_config` for `_training_attempts`, model construction, training, hardening, `_training_config_payload`, `_operator_trace`, and `_final_operator`; raw configs should produce the same effective scaffold list as before. Add top-level optimizer manifest fields: `"scaffold_exclusions": list(scaffold_plan.exclusions)` and `"scaffold_witness_operator": initial_operator.as_dict()`. Replace `_training_attempts()`'s hardcoded scaffold-name set with `set(known_scaffold_kinds())`. In `_apply_scaffold()`, derive scaffold kind from `attempt["kind"]` (`scaffold_exp -> exp`, `scaffold_log -> log`, `scaffold_scaled_exp -> scaled_exp`) and fail closed before dispatch if `scaffold_witness_for(kind, model.operator_family)` is `None`; use `ValueError(f"{kind}:{CENTERED_FAMILY_SAME_FAMILY_WITNESS_MISSING}")` at this optimizer boundary. Extend `tests/test_optimizer_cleanup.py`: keep `test_optimizer_scaffold_recovers_exp_with_manifest_provenance` unchanged for raw recovery; update `test_optimizer_runs_fixed_centered_family_with_manifest_metadata` to assert `manifest["config"]["scaffold_initializers"] == []`, `manifest["scaffold_exclusions"]` equals the three canonical exclusions, `manifest["scaffold_witness_operator"]["label"] == "CEML_2"`, and no `manifest["restarts"][*]["attempt_kind"]` starts with `scaffold_`; update `test_optimizer_preserves_centered_schedule_metadata` to omit explicit `scaffold_initializers=()` and assert the same exclusions with witness operator label `ZEML_8` while preserving final snap operator `ZEML_4`.</action>
  <verify>
    <automated>python -m pytest tests/test_optimizer_cleanup.py::test_optimizer_scaffold_recovers_exp_with_manifest_provenance tests/test_optimizer_cleanup.py::test_optimizer_runs_fixed_centered_family_with_manifest_metadata tests/test_optimizer_cleanup.py::test_optimizer_preserves_centered_schedule_metadata -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "resolve_scaffold_plan" src/eml_symbolic_regression/optimize.py` finds optimizer-level direct-call filtering.</criterion>
    <criterion>`rg "\"scaffold_exclusions\"" src/eml_symbolic_regression/optimize.py` finds optimizer manifest serialization.</criterion>
    <criterion>`python -m pytest tests/test_optimizer_cleanup.py::test_optimizer_runs_fixed_centered_family_with_manifest_metadata -q` passes and proves centered direct optimizer runs have no scaffold attempt kinds.</criterion>
  </acceptance_criteria>
  <done>WIT-02 and optimizer-side WIT-04 are satisfied: direct centered optimizer calls drop raw scaffolds and manifests expose the canonical exclusion codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard raw scaffold helper methods</name>
  <read_first>
    <file>src/eml_symbolic_regression/master_tree.py</file>
    <file>src/eml_symbolic_regression/witnesses.py</file>
    <file>tests/test_master_tree.py</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-CONTEXT.md</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-PATTERNS.md</file>
  </read_first>
  <files>src/eml_symbolic_regression/master_tree.py, tests/test_master_tree.py</files>
  <behavior>
    - Test 1: `SoftEMLTree(..., operator_family=ceml_s_operator(2.0)).force_exp("x")` raises `EmbeddingError.reason == "centered_family_same_family_witness_missing"`.
    - Test 2: Centered `force_log` and `force_scaled_exp` fail with the same reason before depth or missing-constant errors.
    - Test 3: Existing raw `force_exp`, raw `force_log`, and raw `force_scaled_exp` tests still pass.
  </behavior>
  <action>In `src/eml_symbolic_regression/master_tree.py`, import `CENTERED_FAMILY_SAME_FAMILY_WITNESS_MISSING` and `scaffold_witness_for` from `witnesses.py`. Add a private `SoftEMLTree` helper such as `_require_scaffold_witness(self, kind: str) -> None` that checks `scaffold_witness_for(kind, self.operator_family)` and raises `EmbeddingError(CENTERED_FAMILY_SAME_FAMILY_WITNESS_MISSING, f"{kind} scaffold has no same-family witness for operator {self.operator_family.label}")` when missing. Call this helper as the first line of `force_exp("x")`, `force_log("x")`, and `force_scaled_exp(variable, coefficient, strength=30.0)` before any `set_slot()`, depth check, or compiler embedding. Update `tests/test_master_tree.py`: replace the existing centered `force_exp` success expectation with `test_centered_tree_rejects_raw_scaffold_helpers_without_same_family_witness`, covering `force_exp`, `force_log`, and `force_scaled_exp` on a centered tree and asserting `exc.value.reason == "centered_family_same_family_witness_missing"` plus the scaffold kind and operator label in `exc.value.detail`. Keep raw helper tests unchanged to prove raw defaults remain available.</action>
  <verify>
    <automated>python -m pytest tests/test_master_tree.py::test_force_exp_snaps_to_paper_identity tests/test_master_tree.py::test_force_log_snaps_to_paper_identity tests/test_master_tree.py::test_force_scaled_exp_snaps_to_exact_depth_nine_shape tests/test_master_tree.py::test_centered_tree_rejects_raw_scaffold_helpers_without_same_family_witness -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "_require_scaffold_witness" src/eml_symbolic_regression/master_tree.py` finds the helper guard.</criterion>
    <criterion>`python -m pytest tests/test_master_tree.py::test_centered_tree_rejects_raw_scaffold_helpers_without_same_family_witness -q` passes and asserts `EmbeddingError.reason` exactly.</criterion>
    <criterion>Raw helper tests for exp, log, and scaled_exp still pass.</criterion>
  </acceptance_criteria>
  <done>WIT-03 is satisfied: raw-specific helper methods are no longer directly callable under centered-family operators without a registered same-family witness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove scaffold exclusion reason codes survive run artifacts</name>
  <read_first>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>src/eml_symbolic_regression/optimize.py</file>
    <file>tests/test_benchmark_runner.py</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-CONTEXT.md</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-PATTERNS.md</file>
  </read_first>
  <files>tests/test_benchmark_runner.py</files>
  <behavior>
    - Test 1: Centered family-smoke blind artifacts serialize empty budget scaffold_initializers and the three canonical scaffold_exclusions.
    - Test 2: Trained optimizer manifests inside artifacts serialize the same scaffold_exclusions and no scaffold attempt kinds.
    - Test 3: Extracted metrics and aggregate run summaries preserve scaffold_exclusions.
    - Test 4: Raw Beer-Lambert artifact still records `scaffold_scaled_exp` provenance and verifier status `recovered`.
  </behavior>
  <action>Update `tests/test_benchmark_runner.py::test_runner_executes_operator_family_smoke_matrix`. After `result = run_benchmark_suite(...)`, compute `aggregate = benchmark_module.aggregate_evidence(result)` and define the expected exclusion list `["exp:centered_family_same_family_witness_missing", "log:centered_family_same_family_witness_missing", "scaled_exp:centered_family_same_family_witness_missing"]`. For each centered artifact, assert `artifact["budget"]["scaffold_initializers"] == []`, `artifact["budget"]["scaffold_exclusions"] == expected`, `artifact["trained_eml_candidate"]["config"]["scaffold_initializers"] == []`, `artifact["trained_eml_candidate"]["scaffold_exclusions"] == expected`, every `restart["attempt_kind"]` is not prefixed with `scaffold_`, and `artifact["metrics"]["scaffold_exclusions"] == expected`. Assert the matching aggregate row at `aggregate["runs"]` carries `metrics.scaffold_exclusions == expected`. Keep `test_shallow_beer_lambert_blind_run_artifact_exposes_scaled_scaffold_diagnostics` raw assertions intact, and add `candidate.get("scaffold_exclusions", []) == []` there only if the optimizer manifest now always serializes that field for raw runs. Do not change verifier status classification, evidence classes, recovery thresholds, or warm-start seed-missing reason handling.</action>
  <verify>
    <automated>python -m pytest tests/test_benchmark_runner.py::test_runner_executes_operator_family_smoke_matrix tests/test_benchmark_runner.py::test_shallow_beer_lambert_blind_run_artifact_exposes_scaled_scaffold_diagnostics -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`python -m pytest tests/test_benchmark_runner.py::test_runner_executes_operator_family_smoke_matrix -q` passes and checks budget, optimizer manifest, metrics, and aggregate scaffold exclusions.</criterion>
    <criterion>`python -m pytest tests/test_benchmark_runner.py::test_shallow_beer_lambert_blind_run_artifact_exposes_scaled_scaffold_diagnostics -q` passes, proving raw scaled_exp artifact provenance remains visible.</criterion>
    <criterion>`rg "centered_family_same_family_seed_missing" tests/test_benchmark_runner.py` still finds the existing warm-start and perturbed-tree seed-gate assertions.</criterion>
  </acceptance_criteria>
  <done>WIT-04 is satisfied end to end: centered scaffold exclusions are visible in run budgets, trained optimizer manifests, extracted metrics, and aggregate run summaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| direct optimizer caller -> `fit_eml_tree` | Code can construct centered `TrainingConfig` without benchmark validation. |
| initializer/helper code -> `SoftEMLTree` | Custom initializers can call raw scaffold helper methods directly. |
| run artifact -> paper evidence consumer | Campaign reports and audits depend on serialized reason codes to interpret centered evidence honestly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Tampering | `fit_eml_tree` attempt generation | mitigate | Resolve scaffold initializers through the registry for the initial training operator before `_training_attempts()`. |
| T-49-05 | Elevation of Privilege | `SoftEMLTree.force_exp/force_log/force_scaled_exp` | mitigate | Add `EmbeddingError` guards so centered trees cannot call raw-only helper identities without a registered same-family witness. |
| T-49-06 | Repudiation | optimizer and benchmark artifacts | mitigate | Serialize `scaffold_exclusions` in optimizer manifests and assert budget, metrics, and aggregate survival. |
| T-49-07 | Information Integrity | verifier-owned recovery status | mitigate | Leave verification and evidence-class logic unchanged; tests preserve raw recovered scaffold diagnostics separately from centered exclusions. |
</threat_model>

<verification>
Run focused optimizer, helper, and runner checks:

```bash
python -m pytest tests/test_optimizer_cleanup.py::test_optimizer_scaffold_recovers_exp_with_manifest_provenance tests/test_optimizer_cleanup.py::test_optimizer_runs_fixed_centered_family_with_manifest_metadata tests/test_optimizer_cleanup.py::test_optimizer_preserves_centered_schedule_metadata tests/test_master_tree.py::test_centered_tree_rejects_raw_scaffold_helpers_without_same_family_witness tests/test_benchmark_runner.py::test_runner_executes_operator_family_smoke_matrix tests/test_benchmark_runner.py::test_shallow_beer_lambert_blind_run_artifact_exposes_scaled_scaffold_diagnostics -q
```
</verification>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Prevent raw witnesses from contaminating centered-family runs | Task 1 handles direct optimizer calls; Task 2 handles direct helper calls. |
| REQ | WIT-02 centered families no longer receive raw exp/log/scaled_exp attempts | Task 1 proves direct centered optimizer attempts contain no scaffold kinds. |
| REQ | WIT-03 raw-specific helpers fail closed or are only reachable through raw registry entries | Task 2 guards `force_exp`, `force_log`, and `force_scaled_exp`. |
| REQ | WIT-04 optimizer and benchmark artifacts record exclusions with reason codes | Tasks 1 and 3 serialize and assert reason-code visibility. |
| RESEARCH | Preserve `centered_family_same_family_seed_missing` for warm-start and perturbed-tree gates | Task 3 leaves existing seed-gate assertions unchanged. |
| CONTEXT | Keep raw EML defaults unchanged | Tasks 1, 2, and 3 keep raw exp/scaled_exp tests passing. |
| CONTEXT | Preserve verifier-owned recovery semantics | Task 3 explicitly avoids evidence-class and verifier-status changes. |
</source_audit>

<success_criteria>
Plan 49-02 is complete when direct centered optimizer runs and direct centered helper calls cannot use raw scaffold witnesses, manifests expose `centered_family_same_family_witness_missing` scaffold exclusions, runner artifacts and aggregates preserve those codes, and raw EML scaffold recovery tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-02-SUMMARY.md`.
</output>
