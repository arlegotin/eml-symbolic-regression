---
phase: 53-raw-hybrid-paper-campaign-and-claim-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/raw_hybrid_paper.py
  - src/eml_symbolic_regression/cli.py
  - tests/test_raw_hybrid_paper.py
autonomous: true
requirements:
  - RHY-01
  - RHY-02
  - RHY-03
  - RHY-04
must_haves:
  truths:
    - "Developer can run a paper-facing `raw-hybrid-paper` CLI package command for preset `v1.9-raw-hybrid-paper`."
    - "The package writer validates all required source artifacts before writing paper-facing outputs."
    - "Regime labels for pure blind, scaffolded, compile-only, warm-start, same-AST return, repaired, refit, and perturbed basin are structured fields, not prose-only text."
    - "Scientific-law rows expose formula, compile support, compile depth, macro hits, warm-start status, verifier status, evidence regime, and source artifact path."
    - "Centered-family evidence is rendered only as negative diagnostics with the same-family witness caveat."
  artifacts:
    - path: "src/eml_symbolic_regression/raw_hybrid_paper.py"
      provides: "Raw-hybrid paper package writer, source validation, source locks, regime summaries, scientific-law table extraction, and claim-boundary renderers"
      contains: "write_raw_hybrid_paper_package"
    - path: "src/eml_symbolic_regression/cli.py"
      provides: "`raw-hybrid-paper` CLI entry point"
      contains: "raw-hybrid-paper"
    - path: "tests/test_raw_hybrid_paper.py"
      provides: "Contract tests for source validation, regime separation, scientific-law table fields, centered caveats, and CLI parsing"
      contains: "raw_hybrid"
  key_links:
    - from: "src/eml_symbolic_regression/cli.py"
      to: "src/eml_symbolic_regression/raw_hybrid_paper.py"
      via: "CLI imports and calls `write_raw_hybrid_paper_package`"
      pattern: "write_raw_hybrid_paper_package"
    - from: "src/eml_symbolic_regression/raw_hybrid_paper.py"
      to: "artifacts/proof/v1.6/campaigns/*/aggregate.json"
      via: "required proof aggregate source definitions"
      pattern: "proof-shallow-pure-blind"
    - from: "src/eml_symbolic_regression/raw_hybrid_paper.py"
      to: "artifacts/campaigns/v1.9-arrhenius-evidence"
      via: "scientific-law source definitions"
      pattern: "arrhenius-warm"
    - from: "src/eml_symbolic_regression/raw_hybrid_paper.py"
      to: "artifacts/paper/v1.8/decision-memo.json"
      via: "centered negative diagnostic source"
      pattern: "same-family witness"
---

<objective>
Implement the raw-hybrid paper package writer and CLI contract.

Purpose: RHY-01 through RHY-04 require a reproducible paper-facing package command that synthesizes existing measured evidence without rerunning broad historical campaigns or merging evidence regimes.

Output: A new `raw_hybrid_paper.py` module, a `raw-hybrid-paper` CLI subcommand, and contract tests proving source validation, source locks, regime-separated reporting, scientific-law table extraction, and centered-family claim boundaries.
</objective>

<scope>
In scope:
- New synthesis module `src/eml_symbolic_regression/raw_hybrid_paper.py`.
- CLI wiring in `src/eml_symbolic_regression/cli.py`.
- Unit and CLI contract tests in `tests/test_raw_hybrid_paper.py`.

Out of scope:
- Running the package command into `artifacts/paper/v1.9/raw-hybrid/`; that is 53-02.
- README or `docs/IMPLEMENTATION.md` edits; those are gated until 53-02 artifacts exist and are handled by 53-03.
- New scientific demos, changed proof thresholds, changed benchmark denominators, broad proof campaign reruns, or centered-family theoretical claims.
</scope>

<dependencies>
No Phase 53 plan dependency.

Uses completed source evidence from Phases 49-52:
- Phase 49: centered scaffold exclusions and same-family witness caveat.
- Phase 50: `v1.9-arrhenius-evidence` same-AST warm-start artifacts.
- Phase 51: `v1.9-michaelis-evidence` same-AST warm-start artifacts.
- Phase 52: `v1.9-repair-evidence` repair-only no-improvement artifacts.
</dependencies>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-CONTEXT.md
@.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-RESEARCH.md
@.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-PATTERNS.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-02-SUMMARY.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-03-SUMMARY.md
@.planning/phases/51-reciprocal-and-saturation-compiler-motifs/51-03-SUMMARY.md
@.planning/phases/52-verifier-gated-exact-cleanup-expansion/52-03-SUMMARY.md
@src/eml_symbolic_regression/cli.py
@src/eml_symbolic_regression/proof_campaign.py
@src/eml_symbolic_regression/paper_decision.py
@src/eml_symbolic_regression/benchmark.py
@tests/test_paper_decision.py
@tests/test_campaign.py

<interfaces>
Existing CLI wiring pattern:
```python
def paper_decision_command(args: argparse.Namespace) -> int:
    paths = write_paper_decision_package(
        tuple(Path(path) for path in args.aggregate or ()),
        output_dir=Path(args.output_dir),
    )
    print(f"paper decision: memo -> {paths.decision_markdown}; json -> {paths.decision_json}")
    return 0
```

Existing proof source-lock helper pattern:
```python
def _sha256(path: Path) -> str:
    digest = hashlib.sha256()
    with path.open("rb") as handle:
        for chunk in iter(lambda: handle.read(65536), b""):
            digest.update(chunk)
    return digest.hexdigest()
```

Existing scientific-law payload locations to extract:
```python
payload["compiled_eml"]["metadata"]["depth"]
payload["compiled_eml"]["metadata"]["macro_diagnostics"]["hits"]
payload["stage_statuses"]["compiled_seed"]
payload["stage_statuses"]["warm_start_attempt"]
payload["stage_statuses"]["trained_exact_recovery"]
payload["warm_start_eml"]["status"]
payload["warm_start_eml"]["verification"]["status"]
payload["provenance"]["symbolic_expression"]
```

Existing regime fields from aggregate rows:
```python
run["start_mode"]
run["evidence_class"]
run["classification"]
run["return_kind"]
run["raw_status"]
run["repair_status"]
run["refit_status"]
run["claim_status"]
run["artifact_path"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add raw-hybrid package contracts, source validation, and source locks</name>
  <files>src/eml_symbolic_regression/raw_hybrid_paper.py, tests/test_raw_hybrid_paper.py</files>
  <behavior>
    - Test 1: `default_raw_hybrid_sources()` returns preset id `v1.9-raw-hybrid-paper` and includes shallow pure-blind, scaffolded shallow, perturbed basin, basin probes, depth curve, v1.8 centered decision, v1.9 Arrhenius, v1.9 Michaelis, and v1.9 repair evidence sources.
    - Test 2: `write_raw_hybrid_paper_package(..., require_existing=True)` raises a typed `RawHybridPaperError` naming the missing source path when any required source artifact is absent.
    - Test 3: generated `source-locks.json` hashes specific files, not whole directories, and records source id, role, path, sha256, and required boolean.
    - Test 4: generated `manifest.json` records schema `eml.raw_hybrid_paper.v1`, preset id, reproduction command, output paths, and source-lock path.
  </behavior>
  <action>Create `src/eml_symbolic_regression/raw_hybrid_paper.py` with dataclasses `RawHybridSource`, `RawHybridPaperPaths`, and `RawHybridPaperError`. Add constants `RAW_HYBRID_PAPER_PRESET_ID = "v1.9-raw-hybrid-paper"` and `DEFAULT_RAW_HYBRID_OUTPUT_DIR = Path("artifacts") / "paper" / "v1.9" / "raw-hybrid"`. Implement `default_raw_hybrid_sources() -> tuple[RawHybridSource, ...]` using explicit file paths from 53-RESEARCH.md, including proof v1.6 aggregate JSON files, v1.8 centered paper JSON/Markdown files, v1.9 Arrhenius and Michaelis aggregate/run artifacts, v1.9 repair aggregate and repair summary artifacts, and v1.6 standard Beer-Lambert/Shockley/Planck/logistic diagnostic run artifacts. Implement `write_raw_hybrid_paper_package(output_dir=DEFAULT_RAW_HYBRID_OUTPUT_DIR, require_existing=True, overwrite=False, reproduction_command=None) -> RawHybridPaperPaths`. It must validate required source paths before writing, refuse to overwrite an existing non-empty output directory unless `overwrite=True`, read JSON through `json.loads`, hash only the declared source files, and write `manifest.json` plus `source-locks.json`. Do not run benchmarks or campaigns inside this module.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_raw_hybrid_paper.py::test_default_raw_hybrid_sources_cover_required_evidence tests/test_raw_hybrid_paper.py::test_raw_hybrid_package_fails_closed_on_missing_required_source tests/test_raw_hybrid_paper.py::test_raw_hybrid_source_locks_hash_specific_files -q</automated>
  </verify>
  <done>The package writer has explicit source contracts, fails closed on missing evidence, records file-level source locks, and writes a manifest without rerunning broad campaigns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render regime-separated reports and scientific-law tables</name>
  <files>src/eml_symbolic_regression/raw_hybrid_paper.py, tests/test_raw_hybrid_paper.py</files>
  <behavior>
    - Test 1: `regime-summary.json` has separate top-level keys for `pure_blind`, `scaffolded`, `compile_only`, `warm_start`, `same_ast_return`, `repaired`, `refit`, and `perturbed_basin`.
    - Test 2: `raw-hybrid-report.md` contains separate sections for all required regimes and does not contain a single merged recovery headline over mixed regimes.
    - Test 3: `scientific-law-table.json`, `.csv`, and `.md` include required columns `law`, `formula`, `compile_support`, `compile_depth`, `macro_hits`, `warm_start_status`, `verifier_status`, `evidence_regime`, and `artifact_path`.
    - Test 4: Beer-Lambert, Shockley, Arrhenius, and Michaelis rows are reported as warm-start/same-AST diagnostics where appropriate; Arrhenius and Michaelis are never labeled blind discovery.
    - Test 5: Planck and logistic rows are unsupported/stretch diagnostics only, and historical Michaelis unsupported context is labeled historical before/after context if included.
  </behavior>
  <action>Extend `raw_hybrid_paper.py` with structured builders: `build_regime_summary(...)`, `build_scientific_law_rows(...)`, `render_raw_hybrid_report(...)`, `render_scientific_law_table_markdown(...)`, `render_claim_boundaries(...)`, and `render_centered_negative_diagnostics(...)`. The regime summary must preserve pure blind, scaffolded, compile-only, warm-start, same-AST return, repaired, refit, and perturbed-basin buckets as separate JSON arrays or objects. Use existing aggregate/run fields from `benchmark.py`; do not invent new recovery statuses when existing fields are present. For scientific-law rows, extract compile support and depth from `compiled_eml`: strict supported rows use `compiled_eml.metadata.depth`; unsupported rows use `compiled_eml.diagnostic.relaxed.metadata.depth` or the existing diagnostic depth field when present. Extract macro hits from `compiled_eml.metadata.macro_diagnostics.hits` for supported rows and from relaxed diagnostic metadata for unsupported rows. Use source artifact paths as written in source locks. Render `claim-boundaries.md` with explicit statements that warm-start, same-AST, scaffolded, repaired, refit, compile-only, and perturbed-basin evidence are not pure blind discovery. Render `centered-negative-diagnostics.md` from v1.8 decision/completeness sources with the same-family witness caveat and no impossibility claim.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_raw_hybrid_paper.py::test_raw_hybrid_report_keeps_required_regimes_separate tests/test_raw_hybrid_paper.py::test_scientific_law_table_contains_required_columns_and_rows tests/test_raw_hybrid_paper.py::test_centered_diagnostics_keep_same_family_witness_caveat -q</automated>
  </verify>
  <done>The writer emits structured regime summaries, report Markdown, scientific-law JSON/CSV/Markdown tables, claim boundaries, and centered negative diagnostics that preserve all Phase 53 evidence boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the raw-hybrid-paper CLI command</name>
  <files>src/eml_symbolic_regression/cli.py, tests/test_raw_hybrid_paper.py</files>
  <behavior>
    - Test 1: `build_parser().parse_args(["raw-hybrid-paper", "--output-dir", "...", "--require-existing"])` resolves to the raw-hybrid command handler.
    - Test 2: the command prints manifest, report, scientific-law table, claim-boundary, and source-lock paths.
    - Test 3: the command accepts `--overwrite` but otherwise refuses an existing non-empty package output directory.
    - Test 4: the command's reproduction string round-trips through `shlex.split` and preserves shell-sensitive paths.
  </behavior>
  <action>Import `DEFAULT_RAW_HYBRID_OUTPUT_DIR` and `write_raw_hybrid_paper_package` in `cli.py`. Add `raw_hybrid_paper_command(args)` following the existing `paper_decision_command` style. Add a `raw-hybrid-paper` subparser with `--output-dir` defaulting to `artifacts/paper/v1.9/raw-hybrid`, `--require-existing` defaulting to true behavior, `--allow-missing` to set `require_existing=False` only for fixture/debug use, and `--overwrite` for explicit output replacement. Build the reproduction command with `shlex.join`, matching the proof-campaign reproduction-command pattern. The CLI must call only the package writer; it must not run `benchmark`, `campaign`, `proof-campaign`, or `paper-decision` internally.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_raw_hybrid_paper.py::test_raw_hybrid_cli_parser_and_command_writes_package tests/test_raw_hybrid_paper.py::test_raw_hybrid_cli_refuses_silent_overwrite -q</automated>
  </verify>
  <done>`python -m eml_symbolic_regression.cli raw-hybrid-paper --output-dir ... --require-existing` is available, tested, and delegates to the source-validating package writer.</done>
</task>

</tasks>

<tests>
Required automated checks:
```bash
PYTHONPATH=src python -m pytest tests/test_raw_hybrid_paper.py -q
PYTHONPATH=src python -m pytest tests/test_paper_decision.py tests/test_campaign.py::test_reproduction_command_quotes_shell_sensitive_values -q
PYTHONPATH=src python -m eml_symbolic_regression.cli raw-hybrid-paper --output-dir /tmp/eml-raw-hybrid-paper-test --require-existing --overwrite
```
</tests>

<risks>
- Existing artifact schemas are not identical across v1.6, v1.8, and v1.9. Mitigate with explicit source-specific extractors and fail-closed required-field errors.
- Scientific-law rows can accidentally overclaim warm-start same-AST evidence as blind discovery. Mitigate with tested `evidence_regime` values and forbidden-phrase assertions.
- Source locks can become unstable if directories are hashed. Mitigate by locking only declared files.
- A CLI command that reruns broad campaigns would violate 53-CONTEXT.md. Keep the command synthesis-only.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local evidence artifacts -> package writer | Historical and focused JSON/Markdown files are read and converted into paper-facing outputs. |
| Package writer -> output directory | Generated reports and locks are written to a local artifact root. |
| CLI arguments -> filesystem paths | User-provided output path and overwrite flags control local writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | Tampering | source evidence inputs | mitigate | Hash every declared source file into `source-locks.json` and include source ids/roles in `manifest.json`. |
| T-53-01-02 | Repudiation | paper package generation | mitigate | Record reproduction command, generated timestamp, package schema, source paths, and source hashes. |
| T-53-01-03 | Information Integrity | regime classification | mitigate | Preserve existing aggregate/run status fields and test each required regime bucket separately. |
| T-53-01-04 | Denial of Service | package command | mitigate | Do not invoke training or broad campaigns from `raw-hybrid-paper`; read bounded declared source files only. |
| T-53-01-05 | Tampering | output directory overwrite | mitigate | Refuse existing non-empty output directories unless `--overwrite` is passed. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Generate paper-facing raw-hybrid suite/report/table/claim package | 53-01 | COVERED | Writer and CLI contract are implemented here; generated artifacts are 53-02. |
| REQ | RHY-01 | Runnable paper-facing raw-hybrid suite/preset | 53-01 | COVERED | `raw-hybrid-paper` CLI and preset id `v1.9-raw-hybrid-paper`. |
| REQ | RHY-02 | Keep evidence regimes separate | 53-01 | COVERED | Structured regime summary/report renderer. |
| REQ | RHY-03 | Scientific-law table required fields | 53-01 | COVERED | Extractor and table renderers. |
| REQ | RHY-04 | Centered results are negative diagnostics with witness caveat | 53-01 | COVERED | Centered diagnostics renderer and tests. |
| REQ | RHY-05 | Docs after successful artifacts only | 53-01 | PARTIAL | CLI provides artifact generation gate; docs are 53-03 after 53-02. |
| RESEARCH | - | Use a dedicated raw-hybrid paper module, not `paper_decision.py` | 53-01 | COVERED | New `raw_hybrid_paper.py`. |
| RESEARCH | - | Synthesize existing committed evidence, fail closed on missing sources | 53-01 | COVERED | Source definitions and validation. |
| CONTEXT | - | Do not silently rerun broad historical campaigns | 53-01 | COVERED | CLI is synthesis-only. |
| CONTEXT | - | Treat Arrhenius and Michaelis as exact compiler warm-start / same-AST evidence | 53-01 | COVERED | Scientific-law rows and forbidden overclaim tests. |
| CONTEXT | - | Treat Phase 52 repair output as repair-only negative evidence | 53-01 | COVERED | Repair bucket remains separate from recovery regimes. |
</source_audit>

<success_criteria>
Plan 53-01 is complete when `tests/test_raw_hybrid_paper.py` passes, `raw_hybrid_paper.py` can write a complete package into a temporary output directory using existing artifacts, the CLI command is available, and no source code path reruns broad campaigns or changes historical denominators.
</success_criteria>

<output>
After completion, create `.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-01-SUMMARY.md`.
</output>
