---
context: phase
phase: 38-hybrid-recovery-evaluation-and-regression-lockdown
task: 3
total_tasks: 3
status: paused
last_updated: 2026-04-16T15:04:06.219Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Pure-blind proof prose must be checked against threshold-eligible evidence classes — `proof-shallow-pure-blind` previously overstated pure-blind recovery by counting a `repaired_candidate` as a threshold-eligible blind recovery — structurally mitigate by rerunning `campaign proof-shallow-pure-blind` after any report-text change and comparing `report.md` against `aggregate.json` before trusting the full proof bundle.
- [ ] CONSTRAINT: Perturbed-tree basin counts cannot be derived from warm-start-only labels — `proof-basin` previously dropped `same_ast_return` rows from campaign-level headline/regime sections because report code only recognized warm-start classifications — structurally mitigate by checking `proof-basin/report.md` headline and regime summary against `aggregate.json` after the full proof rerun.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Mixed-denominator prose | Blind proof narrative drifted away from the actual threshold-eligible evidence classes, which would weaken paper claims. | blocking | Compare human-readable report text against `aggregate.json` threshold rows and evidence classes before trusting any publication-facing proof report. |
| Warm-start-only counting in mixed exact-return logic | Campaign summary code treated `same_ast_return` and `verified_equivalent_ast` as warm-start-only concepts, understating perturbed-tree basin evidence. | blocking | Validate `proof-basin` headline/regime numbers from exact-return helpers, not from warm-start-specific labels. |
| Overwrite-count blindness | While rerunning into an existing `artifacts/proof/v1.6` root, raw file counts looked complete even when reports were stale or mixed-provenance. | advisory | Track report mtimes and inspect rewritten report content instead of relying on file counts during overwrite reruns. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 38 implementation is complete and the milestone audit passes, but the user explicitly requested a fresh paper-grade `v1.6` evidence bundle before archive/tagging. Fresh `artifacts/campaigns/v1.6-standard` and `artifacts/campaigns/v1.6-showcase` were already generated and inspected. The proof bundle work is paused after hardening report generation in `src/eml_symbolic_regression/campaign.py` and validating the hardest canary suite: `artifacts/proof/v1.6/campaigns/proof-shallow-pure-blind/` was rerun from the latest code state and now reports the correct denominator split (3 verifier recoveries = 2 threshold-eligible pure blind recoveries + 1 repaired candidate).

No long-running proof process is currently active. The top-level `artifacts/proof/v1.6/` bundle is not authoritative yet because it still mixes one completed dry run with aborted reruns from earlier buggy report code. The next session must start a fresh full `proof-campaign` rerun from the current code state.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Proof and campaign regime summaries - Done
- Task 2: Generic comparisons and archived anchor locks - Done
- Task 3: Hybrid-stage regression locks - Done
- Follow-up: Fresh `v1.6-standard` campaign - Done and inspected
- Follow-up: Fresh `v1.6-showcase` campaign - Done and inspected
- Follow-up: Report hardening for paper-grade proof artifacts - In progress, with `campaign.py` fixed for regime-aware strengths text, exact-return counting across warm-start and perturbed-tree regimes, pure-blind threshold-eligible wording, and singular/plural grammar
- Follow-up: `proof-shallow-pure-blind` isolated gate rerun - Done and validated against `aggregate.json`
</completed_work>

<remaining_work>

- Run `PYTHONPATH=src python -m eml_symbolic_regression.cli proof-campaign --output-root artifacts/proof/v1.6 --overwrite` from the current tree.
- Use report mtimes, not file counts, to track progress while the proof root is being overwritten.
- After `proof-shallow` rewrites, verify its report does not mention warm-start evidence and still shows `18/18` bounded scaffolded blind recoveries.
- After `proof-basin` rewrites, verify the headline/regime summary shows perturbed-tree same-AST exact returns instead of dropping them.
- When the full proof run completes, inspect `artifacts/proof/v1.6/proof-campaign.json`, `artifacts/proof/v1.6/proof-report.md`, and `artifacts/proof/v1.6/anchor-locks.json`.
- Only after the proof bundle is authoritative should the next session consider milestone archive/tagging.
</remaining_work>

<decisions_made>

- Kept the actual empirical rerun separate from the already-complete implementation phases; the current work is evidence production and publication hardening, not new feature scope.
- Chose to fix report-layer truthfulness before trusting fresh proof outputs, even when the underlying aggregates were already correct.
- Used `proof-shallow-pure-blind` as the gate suite because it surfaced every report prose/counting defect fastest.
- Chose not to launch another full proof-campaign until the isolated gate suite and focused campaign tests were clean.
</decisions_made>

<blockers>
- Mixed `artifacts/proof/v1.6` provenance: top-level proof bundle files and non-pure-blind suite reports are stale or partially overwritten from earlier aborted reruns. Workaround: rerun the full proof campaign from the current code state before citing any `v1.6` proof artifact.
</blockers>

## Required Reading (in order)
1. [.planning/HANDOFF.json](/Volumes/git/legotin/eml-symbolic-regression/.planning/HANDOFF.json) — machine-readable pause state and next action
1. [.planning/phases/38-hybrid-recovery-evaluation-and-regression-lockdown/38-01-PLAN.md](/Volumes/git/legotin/eml-symbolic-regression/.planning/phases/38-hybrid-recovery-evaluation-and-regression-lockdown/38-01-PLAN.md) — original phase contract and task framing
1. [.planning/phases/38-hybrid-recovery-evaluation-and-regression-lockdown/38-01-SUMMARY.md](/Volumes/git/legotin/eml-symbolic-regression/.planning/phases/38-hybrid-recovery-evaluation-and-regression-lockdown/38-01-SUMMARY.md) — what phase 38 already shipped
1. [src/eml_symbolic_regression/campaign.py](/Volumes/git/legotin/eml-symbolic-regression/src/eml_symbolic_regression/campaign.py) — report generation logic hardened during this session
1. [tests/test_campaign.py](/Volumes/git/legotin/eml-symbolic-regression/tests/test_campaign.py) — focused regressions that currently pass and encode the discovered failures
1. [artifacts/proof/v1.6/campaigns/proof-shallow-pure-blind/aggregate.json](/Volumes/git/legotin/eml-symbolic-regression/artifacts/proof/v1.6/campaigns/proof-shallow-pure-blind/aggregate.json) — authoritative canary aggregate from the latest code state
1. [artifacts/proof/v1.6/campaigns/proof-shallow-pure-blind/report.md](/Volumes/git/legotin/eml-symbolic-regression/artifacts/proof/v1.6/campaigns/proof-shallow-pure-blind/report.md) — authoritative canary report from the latest code state
1. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Trusting human-readable proof prose without cross-checking threshold/evidence-class denominators → structurally prevent by reading `aggregate.json` beside the rewritten report before trusting it.
- [ANTI-PATTERN]: Using raw file counts as progress while overwriting an existing artifact root → structurally prevent by tracking report mtimes or writing to a fresh labeled root.

## Infrastructure State
- Background processes: none running
- Safe verification baseline: `python -m pytest tests/test_campaign.py -q` passed (`17 passed in 14.50s`)
- Fresh canary proof suite: `PYTHONPATH=src python -m eml_symbolic_regression.cli campaign proof-shallow-pure-blind --output-root artifacts/proof/v1.6/campaigns --label proof-shallow-pure-blind --overwrite`
- Expected runtime noise during proof reruns: repeated `RuntimeWarning: overflow encountered in exp` from `src/eml_symbolic_regression/semantics.py:208`
- Worktree state: many code/doc/artifact changes remain uncommitted; do not revert them

<context>
The implementation milestone itself is done. The only reason work is still open is that the user wants paper-grade fresh evidence, and repeated dry-run inspection found report issues that would have been embarrassing in a publication bundle even though the aggregate numbers were mostly right. The key mental model for resumption is: `proof-shallow-pure-blind` is the trusted canary; everything else in `artifacts/proof/v1.6` must be regenerated from the current code state and then spot-checked against aggregates before being cited.
</context>

<next_action>
Start with: `PYTHONPATH=src python -m eml_symbolic_regression.cli proof-campaign --output-root artifacts/proof/v1.6 --overwrite`
</next_action>
