---
phase: 50-arrhenius-exact-warm-start-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/datasets.py
  - tests/test_proof_dataset_manifest.py
  - tests/test_compiler_warm_start.py
autonomous: true
requirements:
  - ARR-01
  - ARR-02
  - ARR-03
must_haves:
  truths:
    - "Developer can load demo id arrhenius and see normalized dimensionless formula exp(-0.8/x)."
    - "Generated arrhenius train, held-out, and extrapolation splits stay positive using domains (0.5, 3.0), (0.6, 2.7), and (3.1, 4.2)."
    - "Strict compilation of exp(-0.8/x) succeeds inside max_depth=13 and records macro hit direct_division_template."
    - "Zero-noise warm start for arrhenius returns same_ast_return and verifier status recovered."
    - "CLI demo output preserves same-AST warm-start evidence as same_ast, not blind discovery, for downstream suite v1.9-arrhenius-evidence case arrhenius-warm."
  artifacts:
    - path: "src/eml_symbolic_regression/datasets.py"
      provides: "DemoSpec registry entry for arrhenius"
      contains: "exp(-0.8 / x)"
    - path: "tests/test_proof_dataset_manifest.py"
      provides: "Positive-domain and provenance coverage for arrhenius"
      contains: "arrhenius"
    - path: "tests/test_compiler_warm_start.py"
      provides: "Strict compile, warm-start, and CLI evidence checks"
      contains: "direct_division_template"
  key_links:
    - from: "src/eml_symbolic_regression/datasets.py"
      to: "src/eml_symbolic_regression/compiler.py"
      via: "SymPy candidate sp.exp(-sp.Float('0.8') / x) exposes direct division"
      pattern: "direct_division_template"
    - from: "tests/test_compiler_warm_start.py"
      to: "src/eml_symbolic_regression/warm_start.py"
      via: "fit_warm_started_eml_tree with noise_scale=0.0"
      pattern: "same_ast_return"
    - from: "tests/test_compiler_warm_start.py"
      to: "src/eml_symbolic_regression/cli.py"
      via: "demo arrhenius --warm-start-eml JSON output"
      pattern: "trained_exact_recovery"
---

<objective>
Add the normalized Arrhenius demo and lock the strict compiler plus exact warm-start behavior.

Purpose: Phase 50 starts by making `arrhenius` a first-class `DemoSpec` for formula `exp(-0.8/x)` over positive domains, then proving the existing compiler and warm-start path return the exact same AST with verifier status `recovered`.

Output: Dataset registry entry, positive-domain manifest test, strict `direct_division_template` compile test, zero-noise `same_ast_return` warm-start test, and CLI demo artifact test.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-CONTEXT.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-RESEARCH.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-PATTERNS.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-02-SUMMARY.md
@sources/FOR_DEMO.md
@src/eml_symbolic_regression/datasets.py
@src/eml_symbolic_regression/compiler.py
@src/eml_symbolic_regression/warm_start.py
@src/eml_symbolic_regression/cli.py
@tests/test_proof_dataset_manifest.py
@tests/test_compiler_warm_start.py

<interfaces>
Existing demo registry contract from `src/eml_symbolic_regression/datasets.py`:
```python
@dataclass(frozen=True)
class DemoSpec:
    name: str
    variable: str
    description: str
    target: Callable[[np.ndarray], np.ndarray]
    candidate: Candidate
    train_domain: tuple[float, float]
    heldout_domain: tuple[float, float]
    extrap_domain: tuple[float, float]
    source_document: str
    source_linkage: str
    normalized_dimensionless: bool

    def make_splits(self, *, points: int = 80, seed: int = 0) -> list[DataSplit]: ...
    def formula_provenance(self) -> dict[str, Any]: ...
```

Existing strict compiler entry point:
```python
compile_and_validate(
    expr: sp.Expr,
    config: CompilerConfig,
    validation_inputs: Mapping[str, np.ndarray] | None = None,
) -> CompiledExpression
```

Existing warm-start entry point:
```python
fit_warm_started_eml_tree(
    inputs,
    target,
    config: TrainingConfig,
    initial_expr: Expr,
    *,
    perturbation_config: PerturbationConfig,
    verification_splits: Sequence[DataSplit] | None = None,
    compiler_metadata: Mapping[str, Any] | None = None,
) -> WarmStartResult
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalized arrhenius DemoSpec and positive-domain manifest coverage</name>
  <read_first>
    <file>src/eml_symbolic_regression/datasets.py</file>
    <file>tests/test_proof_dataset_manifest.py</file>
    <file>sources/FOR_DEMO.md</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-CONTEXT.md</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-PATTERNS.md</file>
  </read_first>
  <files>src/eml_symbolic_regression/datasets.py, tests/test_proof_dataset_manifest.py</files>
  <behavior>
    - Test 1: `get_demo("arrhenius")` returns a normalized dimensionless `DemoSpec` with formula `exp(-0.8/x)`.
    - Test 2: train domain `(0.5, 3.0)`, held-out domain `(0.6, 2.7)`, and extrapolation domain `(3.1, 4.2)` generate strictly positive samples even after seeded jitter.
    - Test 3: provenance points to `sources/FOR_DEMO.md`, includes Arrhenius source linkage, and records `normalized_dimensionless is True`.
    - Test 4: downstream evidence names remain explicit: suite `v1.9-arrhenius-evidence`, case `arrhenius-warm`, macro `direct_division_template`, warm status `same_ast_return`, verifier status `recovered`, evidence class `same_ast`.
  </behavior>
  <action>Add a test such as `test_arrhenius_demo_uses_positive_dimensionless_domains` to `tests/test_proof_dataset_manifest.py` before implementation. It must call `get_demo("arrhenius")`, assert `spec.variable == "x"`, `spec.train_domain == (0.5, 3.0)`, `spec.heldout_domain == (0.6, 2.7)`, `spec.extrap_domain == (3.1, 4.2)`, `spec.normalized_dimensionless is True`, `spec.source_document == "sources/FOR_DEMO.md"`, and `"Arrhenius"` appears in `spec.source_linkage`. It must call `spec.make_splits(points=24, seed=0)` and assert every split input for variable `x` has `min() > 0.0`, target dtype is complex, and all targets are finite. It must call `proof_dataset_manifest("arrhenius", points=24, seed=0, tolerance=1e-8)` and assert split domains are exactly `[0.5, 3.0]`, `[0.6, 2.7]`, `[3.1, 4.2]`, formula id is `arrhenius`, and provenance symbolic expression contains `exp(-0.8/x)` or SymPy's equivalent `exp(-0.8/x)`. Then add the `arrhenius` entry to `demo_specs()` in `src/eml_symbolic_regression/datasets.py`, near the existing FOR_DEMO scientific laws. Use `name="arrhenius"`, `variable="x"`, `target=lambda a: np.exp(-0.8 / a).astype(np.complex128)`, `candidate=_sympy_candidate(sp.exp(-sp.Float("0.8") / x), "x", "arrhenius_catalog")`, the locked positive domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)`, `source_document="sources/FOR_DEMO.md"`, an Arrhenius normalized reciprocal-temperature source linkage, and `normalized_dimensionless=True`. Use only the locked normalized dimensionless input `x`, do not add a one-off sampler, and do not touch Michaelis-Menten or Planck behavior.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_proof_dataset_manifest.py::test_arrhenius_demo_uses_positive_dimensionless_domains -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`get_demo("arrhenius")` succeeds and exposes formula `exp(-0.8/x)` over positive domains `(0.5, 3.0)`, `(0.6, 2.7)`, and `(3.1, 4.2)`.</criterion>
    <criterion>`proof_dataset_manifest("arrhenius", points=24, seed=0, tolerance=1e-8)` records `sources/FOR_DEMO.md`, normalized dimensionless provenance, and no raw arrays.</criterion>
    <criterion>The test names suite `v1.9-arrhenius-evidence`, case `arrhenius-warm`, macro `direct_division_template`, statuses `same_ast_return` and `recovered`, and evidence class `same_ast` in comments or assertions so downstream requirements are traceable.</criterion>
  </acceptance_criteria>
  <done>ARR-01 is satisfied: `arrhenius` is a built-in normalized positive-domain demo for `exp(-0.8/x)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock strict direct-division compile and zero-noise same-AST warm start</name>
  <read_first>
    <file>tests/test_compiler_warm_start.py</file>
    <file>src/eml_symbolic_regression/compiler.py</file>
    <file>src/eml_symbolic_regression/warm_start.py</file>
    <file>src/eml_symbolic_regression/optimize.py</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-RESEARCH.md</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-PATTERNS.md</file>
  </read_first>
  <files>tests/test_compiler_warm_start.py</files>
  <behavior>
    - Test 1: strict compile of demo id `arrhenius`, formula `exp(-0.8/x)`, positive train domain `(0.5, 3.0)`, max depth `13`, and max nodes `256` validates successfully.
    - Test 2: compile metadata reports depth `7`, node count within gate, unsupported reason `None`, and macro hit exactly `["direct_division_template"]`.
    - Test 3: zero-noise warm start over train `(0.5, 3.0)`, held-out `(0.6, 2.7)`, extrapolation `(3.1, 4.2)` returns status `same_ast_return`, verifier status `recovered`, and diagnosis mechanism `same_ast_return` with `changed_slot_count == 0`.
    - Test 4: the expected benchmark path remains suite `v1.9-arrhenius-evidence`, case `arrhenius-warm`, evidence class `same_ast`.
  </behavior>
  <action>Add focused tests to `tests/test_compiler_warm_start.py` near the Beer-Lambert/Shockley warm-start coverage. First add `test_compile_arrhenius_uses_direct_division_template`: load `spec = get_demo("arrhenius")`, build `splits = spec.make_splits(points=24, seed=0)`, call `compile_and_validate(spec.candidate.to_sympy(), CompilerConfig(variables=(spec.variable,), max_depth=13, max_nodes=256), {spec.variable: splits[0].inputs[spec.variable]})`, and assert validation passed, `metadata.unsupported_reason is None`, `metadata.depth == 7`, `metadata.macro_diagnostics["hits"] == ["direct_division_template"]`, and trace contains `direct_division_template`. Then add `test_arrhenius_warm_start_returns_same_ast_and_verifies`: use the compiled expression, `TrainingConfig(depth=compiled.metadata.depth, variables=(spec.variable,), steps=1, restarts=1, seed=0)`, `PerturbationConfig(seed=0, noise_scale=0.0)`, all three arrhenius splits, and `compiler_metadata=compiled.metadata.as_dict()`. Assert `result.status == "same_ast_return"`, `result.verification.status == "recovered"`, `result.manifest["status"] == "same_ast_return"`, `result.manifest["diagnosis"]["mechanism"] == "same_ast_return"`, `result.manifest["diagnosis"]["changed_slot_count"] == 0`, and the manifest compiler metadata carries `direct_division_template`. Do not add an Arrhenius-specific compiler branch; the reusable `direct_division_template` must remain the mechanism.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_compiler_warm_start.py::test_compile_arrhenius_uses_direct_division_template tests/test_compiler_warm_start.py::test_arrhenius_warm_start_returns_same_ast_and_verifies -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Strict compile of `arrhenius` formula `exp(-0.8/x)` under domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)` reports depth `7`, macro `direct_division_template`, and no unsupported reason.</criterion>
    <criterion>Zero-noise warm start reports `same_ast_return`, verifier status `recovered`, and future evidence class `same_ast` for suite `v1.9-arrhenius-evidence` case `arrhenius-warm`.</criterion>
    <criterion>No production compiler changes are made unless the existing reusable direct-division macro has a regression; formula-specific hardcoded recognition is not allowed.</criterion>
  </acceptance_criteria>
  <done>ARR-02 and ARR-03 are satisfied at the library level: strict compile and exact same-AST warm-start return are locked by focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI demo evidence test and preserve unsupported stretch behavior</name>
  <read_first>
    <file>tests/test_compiler_warm_start.py</file>
    <file>src/eml_symbolic_regression/cli.py</file>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-CONTEXT.md</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-RESEARCH.md</file>
  </read_first>
  <files>tests/test_compiler_warm_start.py</files>
  <behavior>
    - Test 1: `demo arrhenius --warm-start-eml --points 24` writes JSON for formula `exp(-0.8/x)` using positive domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)`.
    - Test 2: CLI payload records `compiled_seed=recovered`, `warm_start_attempt=same_ast_return`, `trained_exact_recovery=recovered`, macro `direct_division_template`, and downstream evidence class expectation `same_ast`.
    - Test 3: existing Michaelis-Menten and Planck CLI tests still report unsupported/stretch behavior rather than promotion.
    - Test 4: future artifact path remains suite `v1.9-arrhenius-evidence`, case `arrhenius-warm`.
  </behavior>
  <action>Add a CLI regression test such as `test_cli_warm_start_promotes_arrhenius_same_ast_evidence` to `tests/test_compiler_warm_start.py`. Use `subprocess.run([sys.executable, "-m", "eml_symbolic_regression.cli", "demo", "arrhenius", "--warm-start-eml", "--points", "24", "--output", str(output)], check=True, capture_output=True, env=CLI_ENV, text=True)`. Assert stdout includes `compiled_seed=recovered` and `trained_exact_recovery=recovered`. Load the JSON payload and assert `payload["formula"] == "arrhenius"` if that field exists, `payload["claim_status"] == "recovered"`, `payload["stage_statuses"]["compiled_seed"] == "recovered"`, `payload["stage_statuses"]["warm_start_attempt"] == "same_ast_return"`, `payload["stage_statuses"]["trained_exact_recovery"] == "recovered"`, `payload["compiled_eml"]["metadata"]["macro_diagnostics"]["hits"] == ["direct_division_template"]`, `payload["compiled_eml"]["metadata"]["depth"] == 7`, `payload["warm_start_eml"]["status"] == "same_ast_return"`, `payload["warm_start_eml"]["verification"]["status"] == "recovered"`, and `payload["warm_start_eml"]["diagnosis"]["changed_slot_count"] == 0`. Add an assertion or comment in this test that benchmark evidence class must be `same_ast` in suite `v1.9-arrhenius-evidence` case `arrhenius-warm`; do not present this CLI result as blind discovery. Re-run and preserve existing tests `test_cli_reports_michaelis_menten_depth_gate_without_promotion` and `test_cli_reports_planck_as_stretch_without_promotion` unchanged except for any harmless helper reuse.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_compiler_warm_start.py::test_cli_warm_start_promotes_arrhenius_same_ast_evidence tests/test_compiler_warm_start.py::test_cli_reports_michaelis_menten_depth_gate_without_promotion tests/test_compiler_warm_start.py::test_cli_reports_planck_as_stretch_without_promotion -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>CLI demo artifact for `arrhenius` records formula `exp(-0.8/x)`, macro `direct_division_template`, status `same_ast_return`, verifier status `recovered`, and downstream evidence class `same_ast` for `arrhenius-warm`.</criterion>
    <criterion>Michaelis-Menten still has `stage_statuses.compiled_seed == "unsupported"` and Planck still reports stretch/unsupported warm start; neither is promoted by Arrhenius work.</criterion>
    <criterion>The test names positive train `(0.5, 3.0)`, held-out `(0.6, 2.7)`, and extrapolation `(3.1, 4.2)` domains or reads them from the `DemoSpec` added in Task 1.</criterion>
  </acceptance_criteria>
  <done>ARR-03 is satisfied through the public CLI path while preserving existing unsupported Michaelis-Menten and Planck behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DemoSpec registry -> numerical evaluators | Dataset definitions control domains, target arrays, and high-precision candidate evaluation. |
| SymPy candidate -> compiler | Symbolic expression shape determines whether strict compilation uses a reusable macro or falls back to unsupported diagnostics. |
| CLI JSON artifact -> evidence consumer | Users may read CLI recovery fields as scientific claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | `datasets.demo_specs` | mitigate | Lock formula `exp(-0.8/x)`, positive domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)`, and FOR_DEMO provenance in tests. |
| T-50-02 | Denial of Service | Arrhenius sampling near zero | mitigate | Domains stay safely away from zero and tests inspect generated split minima after jitter. |
| T-50-03 | Information Integrity | compiler macro path | mitigate | Tests require strict compile success with `direct_division_template`; no formula-specific compiler branch is allowed. |
| T-50-04 | Repudiation | CLI evidence labels | mitigate | Tests assert `same_ast_return`, verifier `recovered`, and document the downstream `same_ast` evidence class so warm start is not represented as blind discovery. |
</threat_model>

<verification>
Run the focused Phase 50 Plan 01 checks:

```bash
PYTHONPATH=src python -m pytest \
  tests/test_proof_dataset_manifest.py::test_arrhenius_demo_uses_positive_dimensionless_domains \
  tests/test_compiler_warm_start.py::test_compile_arrhenius_uses_direct_division_template \
  tests/test_compiler_warm_start.py::test_arrhenius_warm_start_returns_same_ast_and_verifies \
  tests/test_compiler_warm_start.py::test_cli_warm_start_promotes_arrhenius_same_ast_evidence \
  tests/test_compiler_warm_start.py::test_cli_reports_michaelis_menten_depth_gate_without_promotion \
  tests/test_compiler_warm_start.py::test_cli_reports_planck_as_stretch_without_promotion \
  -q
```
</verification>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Add Arrhenius normalized scientific-law demo | Task 1 adds `DemoSpec("arrhenius")`. |
| GOAL | Strict compile support and exact verified warm-start return | Tasks 2 and 3 prove `direct_division_template`, `same_ast_return`, and verifier `recovered`. |
| REQ | ARR-01 normalized dataset for `exp(-0.8/x)` over positive domains | Task 1. |
| REQ | ARR-02 strict compile within depth gate with `direct_division_template` | Task 2. |
| REQ | ARR-03 zero-noise warm start same exact AST and verifier `recovered` | Tasks 2 and 3. |
| RESEARCH | Existing compiler already supports direct division at measured depth 7 | Task 2 locks the current strict compile path without new compiler logic. |
| RESEARCH | Same-AST return is evidence class `same_ast`, not blind discovery | Tasks 2 and 3 name and assert the regime. |
| CONTEXT | Use dimensionless transformed input `x` | Task 1. |
| CONTEXT | Preserve Michaelis-Menten and Planck unsupported/stretch behavior | Task 3 reruns existing CLI unsupported tests. |
| CONTEXT | Prefer existing DemoSpec and CLI machinery | All tasks reuse existing registry, compiler, warm-start, verifier, and CLI paths. |
</source_audit>

<success_criteria>
Plan 50-01 is complete when `arrhenius` exists in the built-in demo registry with formula `exp(-0.8/x)`, domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)`, strict compile reports `direct_division_template`, zero-noise warm start returns `same_ast_return`, verifier status is `recovered`, and Michaelis-Menten/Planck unsupported behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/50-arrhenius-exact-warm-start-demo/50-01-SUMMARY.md`.
</output>
