---
phase: 26-warm-start-perturbation-robustness
plan: 01
type: execute
wave: 1
depends_on: ["24-01"]
files_modified:
  - src/eml_symbolic_regression/warm_start.py
  - src/eml_symbolic_regression/benchmark.py
  - src/eml_symbolic_regression/campaign.py
  - tests/test_compiler_warm_start.py
  - tests/test_campaign.py
requirements: [PERT-01, PERT-02, PERT-03, PERT-04]
autonomous: true
user_setup: []
must_haves:
  truths:
    - Beer-Lambert perturbation manifests include active-slot counts, changed-slot counts, verifier status, and a failure mechanism.
    - High-perturbation failures are narrowed to a reproducible mechanism without promoting failed snaps.
    - Existing warm-start status taxonomy remains distinct.
    - Literal-constant provenance and verifier-owned recovery semantics remain unchanged.
  artifacts:
    - src/eml_symbolic_regression/warm_start.py
    - src/eml_symbolic_regression/benchmark.py
    - src/eml_symbolic_regression/campaign.py
  key_links:
    - Warm-start diagnosis is copied from manifest to benchmark metrics to campaign CSV rows.
---

<objective>
Add warm-start perturbation diagnosis without changing recovery semantics.

Purpose: v1.3 high-noise Beer-Lambert failures need a reproducible explanation before deeper algorithmic repair.
Output: Warm-start diagnosis manifests, benchmark metrics, campaign CSV columns, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-warm-start-perturbation-robustness/26-CONTEXT.md
@src/eml_symbolic_regression/warm_start.py
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/campaign.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add warm-start diagnosis to manifests</name>
  <files>src/eml_symbolic_regression/warm_start.py, tests/test_compiler_warm_start.py</files>
  <read_first>src/eml_symbolic_regression/warm_start.py, tests/test_compiler_warm_start.py</read_first>
  <action>Add a diagnosis dict to `WarmStartResult.manifest` with fields `status`, `mechanism`, `active_slot_count`, `changed_slot_count`, `snap_min_margin`, `best_loss`, `post_snap_loss`, and `verifier_status`. Classify mechanisms conservatively: successful statuses map to themselves; changed active slots map to `active_slot_perturbation`; low snap margin maps to `snap_instability`; non-finite snap maps to `non_finite_snap`; finite failed verifier maps to `verifier_mismatch`; otherwise use `soft_fit_only` or `failed`.</action>
  <verify>python -m pytest tests/test_compiler_warm_start.py -q</verify>
  <acceptance_criteria>
    - src/eml_symbolic_regression/warm_start.py contains `diagnosis`.
    - tests/test_compiler_warm_start.py asserts no-noise same-AST diagnosis.
    - tests/test_compiler_warm_start.py asserts high-noise diagnosis includes changed slot counts and mechanism.
  </acceptance_criteria>
  <done>Warm-start manifests explain perturbation failures directly.</done>
</task>

<task type="auto">
  <name>Task 2: Surface warm-start mechanisms in campaign outputs</name>
  <files>src/eml_symbolic_regression/benchmark.py, src/eml_symbolic_regression/campaign.py, tests/test_campaign.py</files>
  <read_first>src/eml_symbolic_regression/benchmark.py, src/eml_symbolic_regression/campaign.py, tests/test_campaign.py</read_first>
  <action>Copy warm-start diagnosis fields into benchmark metrics as `warm_start_mechanism` and include that field in campaign `runs.csv` as `warm_start_mechanism`. Preserve existing columns and statuses.</action>
  <verify>python -m pytest tests/test_campaign.py tests/test_benchmark_runner.py -q</verify>
  <acceptance_criteria>
    - src/eml_symbolic_regression/benchmark.py contains `warm_start_mechanism`.
    - src/eml_symbolic_regression/campaign.py contains `warm_start_mechanism`.
    - tests/test_campaign.py asserts the `runs.csv` header includes `warm_start_mechanism`.
  </acceptance_criteria>
  <done>Campaign artifacts expose perturbation mechanism diagnostics.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_compiler_warm_start.py tests/test_campaign.py tests/test_benchmark_runner.py -q
</verification>

<success_criteria>
- Warm-start failures carry reproducible mechanism diagnostics.
- Same-AST/equivalent/snapped/soft-fit statuses remain distinct.
- Verifier semantics remain unchanged.
- Tests pass.
</success_criteria>
