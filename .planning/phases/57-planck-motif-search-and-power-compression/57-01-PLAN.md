---
phase: 57-planck-motif-search-and-power-compression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/compiler.py
  - tests/test_compiler_warm_start.py
autonomous: true
requirements:
  - MOTIF-03
  - MOTIF-04
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-05
must_haves:
  truths:
    - "Compiler uses low_degree_power_template for cubes only when it is shorter than repeated multiplication."
    - "Compiler does not use the power template for squares when the existing path is shorter."
    - "Planck compile depth drops materially below archived relaxed depth 20."
    - "Planck support and warm-start status are reported from actual strict/verifier outcomes, not assumed."
    - "Existing logistic, Shockley, Arrhenius, and Michaelis-Menten macro behavior does not regress."
  artifacts:
    - path: "src/eml_symbolic_regression/compiler.py"
      provides: "Low-degree power shortening"
      contains: "low_degree_power_template"
    - path: "tests/test_compiler_warm_start.py"
      provides: "Cube compression and Planck diagnostic regression tests"
      contains: "test_planck_compile_depth_drops_below_archived_relaxed_diagnostic"
---

<objective>
Add reusable low-degree power compression for Planck's `x**3` bottleneck and report Planck status honestly from the resulting strict compile and verifier behavior.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add low_degree_power_template</name>
  <files>src/eml_symbolic_regression/compiler.py, tests/test_compiler_warm_start.py</files>
  <behavior>
    - `x**3` validates through `low_degree_power_template` and is shorter than the archived repeated-multiplication depth `16`.
    - `x**2` remains on the existing path when the power template is not shorter.
    - Macro diagnostics include validation status and baseline deltas.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Planck diagnostics from actual outcome</name>
  <files>tests/test_compiler_warm_start.py</files>
  <behavior>
    - Planck compile depth is below archived depth `20`.
    - If Planck strict compile passes, tests assert strict support and macro hits.
    - If Planck remains above strict gate, tests assert unsupported status with improved relaxed depth.
  </behavior>
</task>

</tasks>

<verification>
Run:

```bash
PYTHONPATH=src python -m pytest tests/test_compiler_warm_start.py -q
```
</verification>

<success_criteria>
Phase 57 is complete when reusable cube compression is validation-backed, Planck depth is materially below 20, and Planck promotion or unsupported status follows the actual compile/verifier contract.
</success_criteria>
