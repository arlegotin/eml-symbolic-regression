---
phase: 51-reciprocal-and-saturation-compiler-motifs
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - src/eml_symbolic_regression/benchmark.py
  - tests/test_benchmark_contract.py
  - tests/test_benchmark_runner.py
autonomous: true
requirements:
  - MIC-04
must_haves:
  truths:
    - "Developer can list and run focused suite `v1.9-michaelis-evidence`."
    - "Suite `v1.9-michaelis-evidence` expands exactly case `michaelis-warm` for demo id `michaelis_menten`, formula `2*x/(x+0.5)`, start mode `warm_start`, seed `0`, and perturbation noise `0.0`."
    - "Benchmark artifact records compile depth 12, node count 41, macro `saturation_ratio_template`, status `same_ast_return`, verifier claim status `recovered`, and evidence class `same_ast`."
    - "The focused Michaelis evidence path does not broaden historical campaign denominators or label Michaelis as blind discovery."
  artifacts:
    - path: "src/eml_symbolic_regression/benchmark.py"
      provides: "Built-in suite v1.9-michaelis-evidence and case michaelis-warm"
      contains: "michaelis-warm"
    - path: "tests/test_benchmark_contract.py"
      provides: "Suite registry and expansion contract for Michaelis evidence"
      contains: "v1.9-michaelis-evidence"
    - path: "tests/test_benchmark_runner.py"
      provides: "Artifact regression for same_ast Michaelis evidence"
      contains: "saturation_ratio_template"
  key_links:
    - from: "src/eml_symbolic_regression/benchmark.py"
      to: "src/eml_symbolic_regression/datasets.py"
      via: "_case('michaelis-warm', 'michaelis_menten', 'warm_start')"
      pattern: "michaelis-warm"
    - from: "src/eml_symbolic_regression/benchmark.py"
      to: "src/eml_symbolic_regression/compiler.py"
      via: "warm_start run calls _compile_demo and serializes compiled_eml"
      pattern: "compiled_eml"
    - from: "tests/test_benchmark_runner.py"
      to: "src/eml_symbolic_regression/benchmark.py"
      via: "run_benchmark_suite writes artifact and evidence_class_for_payload derives same_ast"
      pattern: "evidence_class"
---

<objective>
Add a focused Michaelis benchmark evidence path and lock its artifact contract.

Purpose: MIC-04 requires durable evidence once strict support lands. This plan adds one focused suite, `v1.9-michaelis-evidence`, with one case, `michaelis-warm`, and proves the emitted artifact carries saturation macro and same-AST warm-start fields.

Output: Built-in benchmark suite, suite contract tests, and benchmark runner artifact regression coverage.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-reciprocal-and-saturation-compiler-motifs/51-CONTEXT.md
@.planning/phases/51-reciprocal-and-saturation-compiler-motifs/51-RESEARCH.md
@.planning/phases/51-reciprocal-and-saturation-compiler-motifs/51-PATTERNS.md
@.planning/phases/51-reciprocal-and-saturation-compiler-motifs/51-01-SUMMARY.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-03-SUMMARY.md
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/datasets.py
@tests/test_benchmark_contract.py
@tests/test_benchmark_runner.py

<interfaces>
Existing benchmark case helper:
```python
def _case(
    id: str,
    formula: str,
    start_mode: str,
    *,
    seeds: Iterable[int] = (0,),
    perturbation_noise: Iterable[float] = (0.0,),
    points: int = 24,
    warm_steps: int = 8,
    warm_restarts: int = 1,
    max_warm_depth: int = 14,
    tags: Iterable[str] = (),
    expect_recovery: bool = False,
) -> BenchmarkCase: ...
```

Existing benchmark artifact flow:
```python
def run_benchmark_suite(suite: BenchmarkSuite, *, run_filter: RunFilter | None = None) -> BenchmarkSuiteResult: ...
def evidence_class_for_payload(payload: Mapping[str, Any]) -> str: ...
```

Expected focused run identity:
```text
suite_id: v1.9-michaelis-evidence
case_id: michaelis-warm
run_id: v1-9-michaelis-evidence-michaelis-warm-a67d8ccfb108
formula: michaelis_menten
start_mode: warm_start
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register v1.9-michaelis-evidence suite with michaelis-warm case</name>
  <files>src/eml_symbolic_regression/benchmark.py, tests/test_benchmark_contract.py</files>
  <behavior>
    - Test 1: `list_builtin_suites()` includes `v1.9-michaelis-evidence`.
    - Test 2: `builtin_suite("v1.9-michaelis-evidence").expanded_runs()` contains exactly one run for case `michaelis-warm`, formula `michaelis_menten`, symbolic expression `2*x/(x + 0.5)`, and start mode `warm_start`.
    - Test 3: the run uses seed `0`, perturbation noise `0.0`, dataset points `24`, warm steps `1`, default max compile depth `13`, default max compile nodes `256`, and default max warm depth `14`.
    - Test 4: tags include `v1.9`, `michaelis`, `warm_start`, and `same_ast`; `expect_recovery is True`; run id is stable.
  </behavior>
  <action>Add `"v1.9-michaelis-evidence"` to `BUILTIN_SUITES` in `src/eml_symbolic_regression/benchmark.py`. Add a focused branch in `builtin_suite(name)` returning `BenchmarkSuite(id="v1.9-michaelis-evidence", description="Focused v1.9 Michaelis-Menten exact warm-start evidence for normalized 2*x/(x+0.5).", cases=(...))`. The single case must be `_case("michaelis-warm", "michaelis_menten", "warm_start", seeds=(0,), perturbation_noise=(0.0,), points=24, warm_steps=1, tags=("v1.9", "michaelis", "warm_start", "same_ast"), expect_recovery=True)`. Keep default `max_compile_depth=13`, `max_compile_nodes=256`, and `max_warm_depth=14`; do not override them to make the case pass. Do not add this row to broad v1.2, v1.3, v1.8, or paper-facing suites. In `tests/test_benchmark_contract.py`, update the built-in suite registry assertion and add `test_michaelis_evidence_suite_contains_exact_warm_start_case` asserting the exact expanded run properties, domain tuples `(0.05, 5.0)`, `(0.08, 4.5)`, `(5.1, 7.0)`, symbolic expression `2*x/(x + 0.5)`, run id `v1-9-michaelis-evidence-michaelis-warm-a67d8ccfb108`, and expected tags. Do not add `evidence_class` to suite JSON or case definitions; evidence class remains derived after execution.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_benchmark_contract.py::test_builtin_suite_registry_expands_stable_run_ids tests/test_benchmark_contract.py::test_michaelis_evidence_suite_contains_exact_warm_start_case -q</automated>
  </verify>
  <done>`v1.9-michaelis-evidence` is a built-in one-case focused suite for `michaelis-warm` without changing broad campaign denominators or default gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove michaelis-warm artifact records same_ast saturation evidence</name>
  <files>tests/test_benchmark_runner.py</files>
  <behavior>
    - Test 1: running suite `v1.9-michaelis-evidence` filtered to `michaelis-warm` writes one JSON artifact.
    - Test 2: artifact status is `same_ast_return`, claim status is `recovered`, evidence class is `same_ast`, and stage statuses include `compiled_seed=recovered`, `warm_start_attempt=same_ast_return`, `trained_exact_recovery=recovered`.
    - Test 3: artifact records formula `michaelis_menten`, symbolic expression `2*x/(x + 0.5)`, compile depth `12`, node count `41`, and macro `saturation_ratio_template`.
    - Test 4: aggregate evidence preserves `same_ast_warm_start_return` classification and does not classify the run as blind discovery.
  </behavior>
  <action>Add `test_michaelis_warm_benchmark_records_same_ast_evidence` to `tests/test_benchmark_runner.py`. Build `base = builtin_suite("v1.9-michaelis-evidence")`, override its artifact root with `tmp_path / "artifacts"`, and run `run_benchmark_suite(suite, run_filter=RunFilter(case_ids=("michaelis-warm",), seeds=(0,), perturbation_noises=(0.0,)))`. Assert exactly one result with status `same_ast_return` and that the artifact path exists under the suite id. Load the artifact JSON and assert `artifact["run"]["suite_id"] == "v1.9-michaelis-evidence"`, `artifact["run"]["case_id"] == "michaelis-warm"`, `artifact["run"]["formula"] == "michaelis_menten"`, `artifact["run"]["start_mode"] == "warm_start"`, `artifact["training_mode"] == "compiler_warm_start_training"`, `artifact["status"] == "same_ast_return"`, `artifact["claim_status"] == "recovered"`, `artifact["evidence_class"] == "same_ast"`, and stage statuses `compiled_seed=recovered`, `warm_start_attempt=same_ast_return`, `trained_exact_recovery=recovered`. Assert `artifact["compiled_eml"]["metadata"]["macro_diagnostics"]["hits"] == ["saturation_ratio_template"]`, depth `12`, node count `41`, `warm_start_eml.status == "same_ast_return"`, `warm_start_eml.verification.status == "recovered"`, and `warm_start_eml.diagnosis.changed_slot_count == 0`. Assert dataset manifest formula id `michaelis_menten`, provenance symbolic expression `2*x/(x + 0.5)`, and split domains `(0.05, 5.0)`, `(0.08, 4.5)`, `(5.1, 7.0)`. Add aggregate assertions using `benchmark_module.aggregate_evidence(result)` for `evidence_class == "same_ast"` and classification `same_ast_warm_start_return`.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_benchmark_runner.py::test_michaelis_warm_benchmark_records_same_ast_evidence -q</automated>
  </verify>
  <done>The benchmark runner proves `michaelis-warm` produces same-AST warm-start evidence with saturation macro metadata and verifier-owned recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built-in suite registry -> benchmark runner | Suite definitions materialize run identities, budgets, and artifact paths. |
| Benchmark artifact -> aggregate/reporting consumers | Later phases consume evidence class, status, and macro metadata. |
| Focused evidence suite -> paper suite | This phase produces focused evidence but must not broaden paper-facing denominators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-06 | Tampering | `builtin_suite("v1.9-michaelis-evidence")` | mitigate | Contract tests assert exactly one case, locked run fields, default gates, and stable run id. |
| T-51-07 | Repudiation | benchmark artifact payload | mitigate | Runner test asserts macro `saturation_ratio_template`, depth 12, nodes 41, status `same_ast_return`, verifier `recovered`, and evidence class `same_ast`. |
| T-51-08 | Information Integrity | evidence classification | mitigate | Evidence class remains derived by `evidence_class_for_payload`; tests prove same-AST warm start is not blind discovery. |
| T-51-09 | Denial of Service | campaign denominator growth | mitigate | Add only the focused one-case suite; do not edit broad campaign suites. |
</threat_model>

<verification>
Run focused benchmark contract and runner checks:

```bash
PYTHONPATH=src python -m pytest \
  tests/test_benchmark_contract.py::test_builtin_suite_registry_expands_stable_run_ids \
  tests/test_benchmark_contract.py::test_michaelis_evidence_suite_contains_exact_warm_start_case \
  tests/test_benchmark_runner.py::test_michaelis_warm_benchmark_records_same_ast_evidence \
  -q
```
</verification>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Support honest Michaelis recovery diagnostics | 51-02 | COVERED | Focused suite and artifact contract. |
| REQ | MIC-04 | Run Michaelis warm-start recovery if strict support reaches gate | 51-02 | COVERED | `michaelis-warm` same-AST artifact test. |
| RESEARCH | - | Add focused suite `v1.9-michaelis-evidence` with case `michaelis-warm` if strict support lands | 51-02 | COVERED | Task 1 adds suite and contract. |
| RESEARCH | - | Artifact should record same-AST status, verifier status, macro hit, and evidence class | 51-02 | COVERED | Task 2 asserts fields. |
| CONTEXT | - | Do not broaden campaigns; Phase 53 owns paper-facing packaging | 51-02 | COVERED | Task 1 limits changes to one focused suite. |
| CONTEXT | - | Same-AST warm-start evidence is not blind discovery | 51-02 | COVERED | Task 2 aggregate and evidence-class assertions. |
</source_audit>

<success_criteria>
Plan 51-02 is complete when `v1.9-michaelis-evidence` is listed as a built-in suite, `michaelis-warm` expands as one stable focused run, and the runner artifact proves depth 12, node count 41, macro `saturation_ratio_template`, status `same_ast_return`, verifier `recovered`, and evidence class `same_ast`.
</success_criteria>

<output>
After completion, create `.planning/phases/51-reciprocal-and-saturation-compiler-motifs/51-02-SUMMARY.md`.
</output>
