---
phase: 55-generalized-structural-motif-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/compiler.py
  - tests/test_compiler_warm_start.py
autonomous: true
requirements:
  - MOTIF-01
  - MOTIF-05
  - MOTIF-06
must_haves:
  truths:
    - "Reciprocal shift templates accept validated subexpressions of the form 1/(g(x)+b)."
    - "Saturation ratio templates accept validated subexpressions of the form c*g(x)/(g(x)+b)."
    - "Macro diagnostics include validation status for compile-and-validate paths."
    - "Malformed, non-unit, non-finite, or unsupported variants fail closed and do not use generalized templates."
    - "Phase 54 baseline locks and existing supported macro depths continue to pass."
  artifacts:
    - path: "src/eml_symbolic_regression/compiler.py"
      provides: "Generalized structural unit-shift matching over compilable subexpressions"
      contains: "_match_unit_shift"
    - path: "tests/test_compiler_warm_start.py"
      provides: "Generalized motif and fail-closed regression tests"
      contains: "test_reciprocal_shift_accepts_compilable_subexpression"
---

<objective>
Refactor reciprocal and saturation motif matching beyond raw variables so reusable motifs operate on compilable subexpressions while preserving strict gates and fail-closed behavior.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize unit-shift matching to g(x)+b</name>
  <files>src/eml_symbolic_regression/compiler.py, tests/test_compiler_warm_start.py</files>
  <behavior>
    - `1/(exp(x)+0.5)` validates and records `reciprocal_shift_template`.
    - Existing `1/(x+0.5)` behavior remains unchanged.
    - `1/(2*exp(x)+0.5)` does not use `reciprocal_shift_template`.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize saturation matching to c*g(x)/(g(x)+b)</name>
  <files>src/eml_symbolic_regression/compiler.py, tests/test_compiler_warm_start.py</files>
  <behavior>
    - `2*exp(x)/(exp(x)+0.5)` validates and records `saturation_ratio_template`.
    - Existing Michaelis-Menten `2*x/(x+0.5)` behavior remains unchanged.
    - Numerator and denominator base mismatch fails closed without a saturation hit.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add validation-visible macro diagnostics</name>
  <files>src/eml_symbolic_regression/compiler.py, tests/test_compiler_warm_start.py</files>
  <behavior>
    - `compile_and_validate()` macro diagnostics include `validation_status == "validated"` and `validation_passed is True`.
    - Plain `compile_sympy_expression()` macro diagnostics remain marked as not validation-run.
    - Existing diagnostic consumers still see `hits`, `misses`, baseline depth/node count, and deltas unchanged.
  </behavior>
</task>

</tasks>

<verification>
Run:

```bash
PYTHONPATH=src python -m pytest tests/test_compiler_warm_start.py -q
```
</verification>

<success_criteria>
Phase 55 is complete when generalized subexpression reciprocal/saturation tests pass, fail-closed cases reject safely, macro diagnostics expose validation status, and the full compiler warm-start test file remains green.
</success_criteria>
