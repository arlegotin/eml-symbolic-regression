---
phase: 30-bounded-shallow-blind-training-recovery
plan: 03
status: pending
type: execute
wave: 3
depends_on:
  - 30-02
files_modified:
  - src/eml_symbolic_regression/benchmark.py
  - tests/test_shallow_blind_proof_regression.py
  - tests/test_benchmark_reports.py
requirements:
  - SHAL-02
  - SHAL-03
  - SHAL-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The full declared shallow blind proof suite passes the bounded 100% threshold with every eligible run classified as blind_training_recovered."
    - "Regression tests fail if radioactive_decay, Beer-Lambert, or any declared signed/scaled exponential target drops below the 100% bounded target."
    - "The final regression gate rejects catalog, compile-only, warm-start, repaired, same-AST, or soft-fit evidence for SHAL-02 counting."
    - "Diagnostics required by SHAL-03 are present in every proof run summary and artifact inspected by the tests."
  artifacts:
    - path: "tests/test_shallow_blind_proof_regression.py"
      provides: "End-to-end bounded shallow proof suite regression tests"
    - path: "src/eml_symbolic_regression/benchmark.py"
      provides: "Claim-specific threshold evidence-class guardrail for shallow blind proof"
      exports: ["aggregate_evidence"]
    - path: "tests/test_benchmark_reports.py"
      provides: "Aggregate threshold and evidence-class guardrails for shallow proof runs"
  key_links:
    - from: "tests/test_shallow_blind_proof_regression.py"
      to: "src/eml_symbolic_regression/benchmark.py"
      via: "run_benchmark_suite(load_suite('v1.5-shallow-proof')) plus aggregate_evidence"
      pattern: "v1.5-shallow-proof|bounded_100_percent|blind_training_recovered"
    - from: "tests/test_shallow_blind_proof_regression.py"
      to: "artifacts"
      via: "temporary artifact root contains run JSON with diagnostics and no compile/warm-start payloads"
      pattern: "trained_eml_candidate|metrics|evidence_class"
---

<objective>
Lock Phase 30 with deterministic regression tests that prove the declared suite reaches 100% verifier-owned blind recovery.

Purpose: SHAL-04 requires tests that fail on recovery regressions, and D-01 requires the bounded target to count only proof-aware blind `blind_training_recovered` runs. This plan adds the final gate after the suite contract and optimizer path exist.
Output: end-to-end shallow proof regression tests and aggregate guardrails.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-bounded-shallow-blind-training-recovery/30-CONTEXT.md
@.planning/phases/30-bounded-shallow-blind-training-recovery/30-RESEARCH.md
@.planning/phases/30-bounded-shallow-blind-training-recovery/30-01-SUMMARY.md
@.planning/phases/30-bounded-shallow-blind-training-recovery/30-02-SUMMARY.md
@.planning/phases/29-paper-claim-contract-and-proof-dataset-harness/29-VERIFICATION.md
@AGENTS.md
@src/eml_symbolic_regression/benchmark.py
@tests/test_benchmark_reports.py

<interfaces>
Aggregate threshold row contract:
```python
aggregate = aggregate_evidence(result)
threshold = aggregate["thresholds"][0]
threshold["claim_id"] == "paper-shallow-blind-recovery"
threshold["threshold_policy_id"] == "bounded_100_percent"
threshold["eligible"] == len(aggregate["runs"])
threshold["rate"] == 1.0
threshold["status"] == "passed"
threshold["evidence_classes"] == {"blind_training_recovered": threshold["eligible"]}
```

Required per-run diagnostics:
```python
run_summary["metrics"]["scaffold_source"]
run_summary["metrics"]["best_loss"]
run_summary["metrics"]["post_snap_loss"]
run_summary["metrics"]["snap_min_margin"]
run_summary["metrics"]["snap_active_node_count"]
run_summary["metrics"]["verifier_status"]
run_summary["evidence_class"] == "blind_training_recovered"
```
</interfaces>

<decision_trace>
D-01: The regression gate asserts all counted evidence classes are exactly `blind_training_recovered`.
D-03: Tests execute the declared suite contract; CLI filters are not allowed to redefine the proof target.
D-09: Tests inspect diagnostics for scaffold source, losses, snap margin, active node count, verifier status, and evidence class.
D-10: Tests fail when radioactive_decay or any signed/scaled exponential proof target is not recovered.
D-12: Tests use existing threshold policies and verifier semantics without loosening tolerance or redefining recovered.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add end-to-end bounded shallow proof regression</name>
  <read_first>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>tests/test_benchmark_reports.py</file>
    <file>.planning/phases/30-bounded-shallow-blind-training-recovery/30-CONTEXT.md</file>
  </read_first>
  <files>tests/test_shallow_blind_proof_regression.py</files>
  <behavior>
    - Execute the full `v1.5-shallow-proof` suite against a temporary artifact root, not a filtered subset.
    - Assert exactly six case IDs and 18 runs are present, including `shallow-radioactive-decay-blind`, `shallow-beer-lambert-blind`, `shallow-scaled-exp-growth-blind`, and `shallow-scaled-exp-fast-decay-blind`.
    - Assert every result has `status == "recovered"`, `claim_status == "recovered"`, `start_mode == "blind"`, `training_mode == "blind_training"`, and `evidence_class == "blind_training_recovered"`.
    - Assert no run artifact contains `compiled_eml`, `warm_start_eml`, `verification` catalog payloads, `same_ast`, `compile_only_verified`, or `catalog_verified` evidence.
  </behavior>
  <action>Create `tests/test_shallow_blind_proof_regression.py`. Build a temp-root suite from `load_suite("v1.5-shallow-proof")` using `type(base)(base.id, base.description, base.cases, tmp_path / "artifacts")`, run it with `run_benchmark_suite(suite)`, then aggregate with `aggregate_evidence(result)`. Keep the test deterministic and runtime practical by relying on the suite's minimal budgets from Plan 01 and the scaffold path from Plan 02; do not filter seeds or cases in this full proof test because D-03 says filters cannot redefine the proof target.</action>
  <verify>
    <automated>python -m pytest tests/test_shallow_blind_proof_regression.py -q</automated>
  </verify>
  <acceptance_criteria>The test fails on any non-recovered declared run, any non-blind evidence class, or any attempt to satisfy SHAL-02 with catalog, compile-only, warm-start, repair, same-AST, or soft-fit evidence.</acceptance_criteria>
  <done>SHAL-02 and SHAL-04 are locked by an end-to-end proof-suite regression gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten aggregate and diagnostic guardrails</name>
  <read_first>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>src/eml_symbolic_regression/proof.py</file>
    <file>tests/test_benchmark_reports.py</file>
    <file>tests/test_shallow_blind_proof_regression.py</file>
  </read_first>
  <files>src/eml_symbolic_regression/benchmark.py, tests/test_benchmark_reports.py, tests/test_shallow_blind_proof_regression.py</files>
  <behavior>
    - The full suite aggregate threshold for `paper-shallow-blind-recovery` has status `passed`, rate `1.0`, failed `0`, and evidence_classes exactly `{"blind_training_recovered": 18}`.
    - Every aggregate run summary includes non-null metrics for best loss, post-snap loss, snap min margin, snap active node count, verifier status, and scaffold source.
    - `tests/test_benchmark_reports.py` includes a synthetic guardrail showing compile-only, catalog, warm-start, repaired, verified-equivalent, same-AST, and soft-fit evidence all fail the shallow bounded target when mixed into this claim.
  </behavior>
  <action>Extend `_threshold_summary()` in `src/eml_symbolic_regression/benchmark.py` with a narrow claim-specific allowed-evidence override for `claim_id == "paper-shallow-blind-recovery"`: only `EVIDENCE_CLASSES["blind_training_recovered"]` counts as passed for that claim. Do not loosen `threshold_policies()` or redefine `recovered`; this is a stricter D-01 counting rule for the shallow blind claim only. Extend `tests/test_shallow_blind_proof_regression.py` to assert the aggregate threshold row and diagnostic metrics for every run summary. Extend `tests/test_benchmark_reports.py` with a synthetic shallow-claim aggregate where one blind recovery is mixed with compile-only, catalog, compiler-warm-start recovered, repaired_candidate, verified_equivalent, same_ast, and soft_fit_only evidence; assert the shallow bounded threshold status remains failed for every non-blind evidence class and document this as the SHAL-02/D-01 guardrail.</action>
  <verify>
    <automated>python -m pytest tests/test_shallow_blind_proof_regression.py tests/test_benchmark_reports.py -q</automated>
  </verify>
  <acceptance_criteria>Aggregate tests prove the declared suite is 100% recovered only because every run is blind_training_recovered, and diagnostics are present for post-failure inspection if a future regression occurs.</acceptance_criteria>
  <done>SHAL-03 diagnostics and SHAL-04 threshold regression behavior are locked in tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regression test -> benchmark execution | Tests execute local benchmark code and write temporary artifacts. |
| aggregate rows -> proof claim | Threshold summaries can misrepresent success if evidence classes are not checked. |
| artifact diagnostics -> debugging evidence | Missing diagnostics make failures hard to reproduce or can hide proof claim inflation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Tampering | Full proof regression test | mitigate | Assert exact suite case IDs, run count, start mode, training mode, and evidence class; no filtered target can pass the full gate. |
| T-30-10 | Repudiation | Aggregate threshold row | mitigate | Assert exact evidence-class counts and diagnostic metrics so future failures explain whether training, snapping, or verification regressed. |
| T-30-11 | Information Disclosure | Temporary benchmark artifacts | accept | Tests write only to pytest tmp paths and use synthetic datasets; no secrets or user data are present. |
| T-30-12 | Denial of Service | Full suite regression runtime | mitigate | Keep suite budgets minimal in Plan 01 and use scaffold recovery from Plan 02; verification command remains a targeted pytest file. |
</threat_model>

<verification>
Overall plan verification:
- `python -m pytest tests/test_shallow_blind_proof_regression.py tests/test_benchmark_reports.py -q`
- `python -m pytest tests/test_shallow_scaled_exponential_contract.py tests/test_master_tree.py tests/test_optimizer_cleanup.py tests/test_benchmark_contract.py tests/test_benchmark_runner.py tests/test_benchmark_reports.py tests/test_shallow_blind_proof_regression.py -q`
- `PYTHONPATH=src python -m eml_symbolic_regression.cli benchmark v1.5-shallow-proof --output-dir /tmp/eml-phase30-proof-regression`
</verification>

<success_criteria>
Plan 03 succeeds when tests fail on any declared shallow proof regression and pass only when all proof runs are verifier-owned blind training recoveries with diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/30-bounded-shallow-blind-training-recovery/30-03-SUMMARY.md`.
</output>
