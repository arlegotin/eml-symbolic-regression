---
plan_id: 45-01
phase: 45
name: Centered integration fixes and fail-closed metadata
status: complete
wave: 1
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - FIX-04
  - FIX-05
---

<objective>
Strengthen centered-family integration contracts so unsupported paths are explicit and supported paths preserve operator-family semantics.
</objective>

<tasks>
1. Add budget-level scaffold exclusion metadata for centered variants.
2. Add optimizer schedule trace metadata showing operator changes during training and hardening.
3. Enrich centered warm-start and perturbed-tree unsupported payloads with same-family seed diagnostics.
4. Derive operator-family metrics from run budgets when no trained candidate exists.
5. Add regression tests for fail-closed metadata and schedule/scaffold contracts.
</tasks>

<verification>
- Focused benchmark and optimizer tests pass.
- Smoke aggregate tables retain operator-family labels for unsupported centered rows.
- No raw EML default behavior is changed except optional metadata fields.
</verification>
