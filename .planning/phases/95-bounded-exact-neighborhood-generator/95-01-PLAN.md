---
phase: 95
plan: 01
name: bounded exact neighborhood manifests
wave: 1
depends_on: [94]
requirements: [NBR-01, NBR-03, NBR-04]
files_modified:
  - src/eml_symbolic_regression/paper_v117.py
  - src/eml_symbolic_regression/cli.py
  - tests/test_paper_v117.py
autonomous: true
---

<objective>
Generate bounded, deterministic exact candidate alternatives around Phase 94 snap seeds while preserving original and fallback provenance.
</objective>

<tasks>
<task type="auto">
<name>Add neighborhood candidate writer</name>
<read_first>
- src/eml_symbolic_regression/master_tree.py
- src/eml_symbolic_regression/expression.py
- src/eml_symbolic_regression/paper_v117.py
</read_first>
<action>
Extend `paper_v117.py` with `write_v117_neighborhood_candidates()`. It reads `snap-neighborhood-seeds.json`, optional candidate artifact JSON, and produces `neighborhood-candidates.json`, `neighborhood-candidates.csv`, `neighborhood-candidates.md`, source locks, and a manifest. It must preserve original and fallback rows and generate bounded one/two-slot alternatives when serialized snap and slot alternatives are available.
</action>
<acceptance_criteria>
- `src/eml_symbolic_regression/paper_v117.py` contains `eml.v117_neighborhood_candidates.v1`.
- `src/eml_symbolic_regression/paper_v117.py` contains `target_formula_leakage`.
- `tests/test_paper_v117.py` asserts one-slot and two-slot alternatives are deterministic.
</acceptance_criteria>
</task>

<task type="auto">
<name>Add CLI registration</name>
<read_first>
- src/eml_symbolic_regression/cli.py
- tests/test_paper_v117.py
</read_first>
<action>
Register `geml-v117-neighborhoods` with output directory, snap diagnostics directory, campaign directory, budget, beam-width, max-moves, and overwrite options.
</action>
<acceptance_criteria>
- `src/eml_symbolic_regression/cli.py` contains `geml-v117-neighborhoods`.
- `tests/test_paper_v117.py` asserts the CLI parser maps to the v1.17 neighborhood command.
</acceptance_criteria>
</task>
</tasks>

<verification>
python -m pytest tests/test_paper_v117.py -q
</verification>

<must_haves>
- One-slot and two-slot alternatives are generated deterministically from low-margin candidate choices.
- Candidate budgets, ordering, and pruning are source-locked and reproducible.
- Original snapped candidates and fallback candidates remain present with provenance.
- Tests reject formula-name recognizers, exact target-tree seeds, and hidden oracle promotion paths.
</must_haves>
