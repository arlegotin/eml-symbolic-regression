---
phase: 88
plan: 88-01
name: Paper-strength gate and campaign contract
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/paper_v116.py
  - src/eml_symbolic_regression/cli.py
  - tests/test_paper_v116.py
requirements_addressed:
  - STRG-01
  - STRG-02
  - STRG-03
  - STRG-04
  - CAMP-04
autonomous: true
---

<objective>
Add a v1.16 machine-readable paper-strength gate and campaign contract that fails closed unless verifier-gated exact recovery, matched denominators, negative-control discipline, and source locks are present.
</objective>

<tasks>
<task id="1">
<read_first>
- src/eml_symbolic_regression/geml_package.py
- src/eml_symbolic_regression/campaign.py
- .planning/REQUIREMENTS.md
</read_first>
<action>
Create `src/eml_symbolic_regression/paper_v116.py` with a default gate config, outcome evaluator, claim audit, source-lock helper, and contract/package path helpers. The evaluator must support `paper_positive`, `promising_preliminary`, `negative`, and `inconclusive`; loss-only rows must never count as recovery.
</action>
<acceptance_criteria>
- `paper_v116.py` contains `DEFAULT_V116_PACKAGE_DIR`
- `paper_v116.py` contains `evaluate_v116_gate`
- `paper_v116.py` contains `build_v116_claim_audit`
</acceptance_criteria>
</task>
<task id="2">
<read_first>
- src/eml_symbolic_regression/cli.py
- tests/test_geml_package.py
</read_first>
<action>
Expose a CLI command for writing the v1.16 paper contract/package artifacts without replacing v1.15 package behavior.
</action>
<acceptance_criteria>
- `cli.py` contains `geml-paper-v116`
- Tests can import and call the new package writer directly.
</acceptance_criteria>
</task>
<task id="3">
<read_first>
- tests/test_geml_package.py
</read_first>
<action>
Add fixture tests proving paper-positive requires exact i*pi recovery wins on natural families, matched denominator completeness, no negative-control i*pi wins, and no unsafe claim language.
</action>
<acceptance_criteria>
- `tests/test_paper_v116.py` exits 0 under pytest
- The loss-only fixture returns `inconclusive`, not `paper_positive`
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python -m pytest tests/test_paper_v116.py tests/test_geml_package.py -q`.
</verification>

<must_haves>
- Exact-recovery-only positive gate
- Negative-control cherry-picking blocker
- Machine-readable JSON and readable Markdown artifacts
- Source-lock-ready contract for downstream phases
</must_haves>
