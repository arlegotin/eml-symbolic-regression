---
phase: 50-arrhenius-exact-warm-start-demo
plan: 02
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - src/eml_symbolic_regression/benchmark.py
  - tests/test_benchmark_contract.py
  - tests/test_benchmark_runner.py
autonomous: true
requirements:
  - ARR-04
must_haves:
  truths:
    - "Developer can list and run focused suite v1.9-arrhenius-evidence."
    - "Suite v1.9-arrhenius-evidence expands exactly case arrhenius-warm for demo id arrhenius, formula exp(-0.8/x), start mode warm_start, seed 0, and perturbation_noise 0.0."
    - "Benchmark artifact records compile depth 7, macro direct_division_template, status same_ast_return, verifier claim_status recovered, and evidence_class same_ast."
    - "The focused benchmark path does not broaden campaigns or change Michaelis-Menten and Planck unsupported/stretch behavior."
  artifacts:
    - path: "src/eml_symbolic_regression/benchmark.py"
      provides: "Built-in suite v1.9-arrhenius-evidence and case arrhenius-warm"
      contains: "arrhenius-warm"
    - path: "tests/test_benchmark_contract.py"
      provides: "Suite registry and expansion contract"
      contains: "v1.9-arrhenius-evidence"
    - path: "tests/test_benchmark_runner.py"
      provides: "Artifact regression for same_ast Arrhenius evidence"
      contains: "same_ast"
  key_links:
    - from: "src/eml_symbolic_regression/benchmark.py"
      to: "src/eml_symbolic_regression/datasets.py"
      via: "_case('arrhenius-warm', 'arrhenius', 'warm_start')"
      pattern: "arrhenius-warm"
    - from: "src/eml_symbolic_regression/benchmark.py"
      to: "src/eml_symbolic_regression/compiler.py"
      via: "warm_start run calls _compile_demo and serializes compiled_eml"
      pattern: "compiled_eml"
    - from: "tests/test_benchmark_runner.py"
      to: "src/eml_symbolic_regression/benchmark.py"
      via: "run_benchmark_suite writes artifact and evidence_class_for_payload derives same_ast"
      pattern: "evidence_class"
---

<objective>
Add a focused Arrhenius benchmark evidence path and lock its artifact contract.

Purpose: ARR-04 needs a reproducible benchmark/report path, not a broad campaign rerun. This plan adds one built-in suite, `v1.9-arrhenius-evidence`, with one case, `arrhenius-warm`, and proves the emitted artifact carries the strict compile and same-AST warm-start evidence fields.

Output: Built-in benchmark suite, suite contract tests, and benchmark runner artifact regression coverage.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-CONTEXT.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-RESEARCH.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-PATTERNS.md
@.planning/phases/50-arrhenius-exact-warm-start-demo/50-01-SUMMARY.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-02-SUMMARY.md
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/datasets.py
@tests/test_benchmark_contract.py
@tests/test_benchmark_runner.py

<interfaces>
Existing benchmark case helper:
```python
def _case(
    id: str,
    formula: str,
    start_mode: str,
    *,
    seeds: Iterable[int] = (0,),
    perturbation_noise: Iterable[float] = (0.0,),
    points: int = 24,
    warm_steps: int = 8,
    max_warm_depth: int = 14,
    tags: Iterable[str] = (),
    expect_recovery: bool = False,
) -> BenchmarkCase: ...
```

Existing artifact evidence classifier:
```python
def evidence_class_for_payload(payload: Mapping[str, Any]) -> str:
    if training_mode == TRAINING_MODES["compiler_warm_start_training"]:
        if status == "same_ast_return" or claim_status == "same_ast_return":
            return EVIDENCE_CLASSES["same_ast"]
```

Existing CLI benchmark command:
```bash
PYTHONPATH=src python -m eml_symbolic_regression.cli benchmark <suite> --case <case-id> --output-dir <artifact-root>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register v1.9-arrhenius-evidence suite with arrhenius-warm case</name>
  <read_first>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>tests/test_benchmark_contract.py</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-01-SUMMARY.md</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-RESEARCH.md</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-PATTERNS.md</file>
  </read_first>
  <files>src/eml_symbolic_regression/benchmark.py, tests/test_benchmark_contract.py</files>
  <behavior>
    - Test 1: `list_builtin_suites()` includes `v1.9-arrhenius-evidence`.
    - Test 2: `builtin_suite("v1.9-arrhenius-evidence").expanded_runs()` contains exactly one run for case `arrhenius-warm`, formula `arrhenius`, formula expression `exp(-0.8/x)`, and start mode `warm_start`.
    - Test 3: the run uses seed `0`, perturbation noise `0.0`, dataset points `24`, positive domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)`, and tags include `v1.9`, `arrhenius`, `warm_start`, and `same_ast`.
    - Test 4: expected artifact values remain macro `direct_division_template`, status `same_ast_return`, verifier `recovered`, and evidence class `same_ast`.
  </behavior>
  <action>Add `"v1.9-arrhenius-evidence"` to `BUILTIN_SUITES` in `src/eml_symbolic_regression/benchmark.py`. Add a focused branch in `builtin_suite(name)` returning `BenchmarkSuite(id="v1.9-arrhenius-evidence", description="Focused v1.9 Arrhenius exact warm-start evidence for normalized exp(-0.8/x).", cases=(...))`. The single case must be `_case("arrhenius-warm", "arrhenius", "warm_start", seeds=(0,), perturbation_noise=(0.0,), points=24, warm_steps=1, tags=("v1.9", "arrhenius", "warm_start", "same_ast"), expect_recovery=True)`. Keep `max_warm_depth` at the default `14`; Plan 50-01 locks strict compile depth `7`, so the focused case remains inside the existing warm-start gate. Do not add this row to broad v1.3/v1.8 suites and do not add Michaelis-Menten or Planck rows here. In `tests/test_benchmark_contract.py`, update the existing built-in suite registry assertion to include `v1.9-arrhenius-evidence`, and add `test_arrhenius_evidence_suite_contains_exact_warm_start_case` asserting the exact expanded run properties, tags, run id stability, and that `get_demo("arrhenius")` carries domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)` for formula `exp(-0.8/x)`.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_benchmark_contract.py::test_builtin_suite_registry_expands_stable_run_ids tests/test_benchmark_contract.py::test_arrhenius_evidence_suite_contains_exact_warm_start_case -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`v1.9-arrhenius-evidence` is listed by `list_builtin_suites()` and expands one `arrhenius-warm` run.</criterion>
    <criterion>The case uses demo id `arrhenius`, formula `exp(-0.8/x)`, positive domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)`, macro expectation `direct_division_template`, expected status `same_ast_return`, expected verifier status `recovered`, and expected evidence class `same_ast`.</criterion>
    <criterion>No broad campaign suite is changed and no Michaelis-Menten or Planck status is reclassified.</criterion>
  </acceptance_criteria>
  <done>ARR-04 has a focused built-in benchmark suite and case path for reproducible Arrhenius evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove arrhenius-warm artifact records same_ast evidence</name>
  <read_first>
    <file>tests/test_benchmark_runner.py</file>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>src/eml_symbolic_regression/datasets.py</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-CONTEXT.md</file>
    <file>.planning/phases/50-arrhenius-exact-warm-start-demo/50-RESEARCH.md</file>
  </read_first>
  <files>tests/test_benchmark_runner.py</files>
  <behavior>
    - Test 1: running suite `v1.9-arrhenius-evidence` filtered to case `arrhenius-warm` writes a JSON artifact.
    - Test 2: artifact status is `same_ast_return`, claim status is `recovered`, evidence class is `same_ast`, and stage statuses include `compiled_seed=recovered`, `warm_start_attempt=same_ast_return`, `trained_exact_recovery=recovered`.
    - Test 3: artifact records formula `arrhenius`, formula `exp(-0.8/x)`, domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)`, compile depth `7`, and macro `direct_division_template`.
    - Test 4: artifact path is under the suite root and can feed docs after evidence exists.
  </behavior>
  <action>Add `test_arrhenius_warm_benchmark_records_same_ast_evidence` to `tests/test_benchmark_runner.py`. Build `base = builtin_suite("v1.9-arrhenius-evidence")`, override its artifact root with `tmp_path / "artifacts"`, run `run_benchmark_suite(suite, run_filter=RunFilter(case_ids=("arrhenius-warm",), seeds=(0,), perturbation_noises=(0.0,)))`, and assert there is exactly one result with `status == "same_ast_return"`. Load the artifact JSON and assert `artifact["run"]["suite_id"] == "v1.9-arrhenius-evidence"`, `artifact["run"]["case_id"] == "arrhenius-warm"`, `artifact["run"]["formula"] == "arrhenius"`, `artifact["run"]["start_mode"] == "warm_start"`, `artifact["training_mode"] == "compiler_warm_start_training"`, `artifact["status"] == "same_ast_return"`, `artifact["claim_status"] == "recovered"`, `artifact["evidence_class"] == "same_ast"`, `artifact["stage_statuses"]["compiled_seed"] == "recovered"`, `artifact["stage_statuses"]["warm_start_attempt"] == "same_ast_return"`, and `artifact["stage_statuses"]["trained_exact_recovery"] == "recovered"`. Assert `artifact["compiled_eml"]["metadata"]["macro_diagnostics"]["hits"] == ["direct_division_template"]`, `artifact["compiled_eml"]["metadata"]["depth"] == 7`, `artifact["warm_start_eml"]["status"] == "same_ast_return"`, `artifact["warm_start_eml"]["verification"]["status"] == "recovered"`, and `artifact["warm_start_eml"]["diagnosis"]["changed_slot_count"] == 0`. Assert `artifact["dataset_manifest"]["formula_id"] == "arrhenius"` and its split domains are the positive domains `(0.5, 3.0)`, `(0.6, 2.7)`, `(3.1, 4.2)`. Add `aggregate = benchmark_module.aggregate_evidence(result)` and assert the aggregate run preserves `evidence_class == "same_ast"` or classification `same_ast_warm_start_return`. Do not treat this as blind recovery.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_benchmark_runner.py::test_arrhenius_warm_benchmark_records_same_ast_evidence -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The benchmark runner artifact for suite `v1.9-arrhenius-evidence`, case `arrhenius-warm`, demo id `arrhenius`, formula `exp(-0.8/x)` records macro `direct_division_template`, status `same_ast_return`, verifier `recovered`, and evidence class `same_ast`.</criterion>
    <criterion>The artifact records positive train `(0.5, 3.0)`, held-out `(0.6, 2.7)`, and extrapolation `(3.1, 4.2)` domains through the dataset manifest.</criterion>
    <criterion>Aggregate evidence keeps same-AST warm start separate from blind discovery.</criterion>
  </acceptance_criteria>
  <done>ARR-04 is satisfied at the benchmark-runner level: the focused artifact path has the required fields and regime labeling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built-in suite registry -> benchmark runner | Suite definitions materialize runs and determine artifact identity. |
| Benchmark run artifact -> aggregate/reporting consumers | Evidence class and claim status are read by later campaign and paper-report phases. |
| Run filter -> artifact generation | CLI filters can run subsets; focused Arrhenius evidence should not alter broad campaign denominators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | Tampering | `builtin_suite("v1.9-arrhenius-evidence")` | mitigate | Contract tests assert exactly one `arrhenius-warm` run with seed 0 and perturbation noise 0.0. |
| T-50-06 | Repudiation | benchmark artifact payload | mitigate | Runner test asserts macro `direct_division_template`, status `same_ast_return`, verifier `recovered`, and evidence class `same_ast`. |
| T-50-07 | Information Integrity | evidence classification | mitigate | Use existing `evidence_class_for_payload`; tests prove same-AST warm-start evidence is not blind recovery. |
| T-50-08 | Denial of Service | broad campaign churn | mitigate | Add one focused suite and one focused case; do not expand existing large suites in this phase. |
</threat_model>

<verification>
Run focused benchmark contract and runner checks:

```bash
PYTHONPATH=src python -m pytest \
  tests/test_benchmark_contract.py::test_builtin_suite_registry_expands_stable_run_ids \
  tests/test_benchmark_contract.py::test_arrhenius_evidence_suite_contains_exact_warm_start_case \
  tests/test_benchmark_runner.py::test_arrhenius_warm_benchmark_records_same_ast_evidence \
  -q
```
</verification>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Produce reproducible compile/warm-start evidence | Tasks 1 and 2 add suite `v1.9-arrhenius-evidence` and prove artifact fields. |
| REQ | ARR-04 reproducible benchmark/report artifact with compile depth, warm-start status, verifier status, regime labeling | Tasks 1 and 2. |
| RESEARCH | Add focused benchmark case rather than broad campaign rerun | Task 1 adds one suite with one case. |
| RESEARCH | Existing artifact schema already carries compile depth, macro hits, warm-start status, verifier status, and evidence class | Task 2 asserts those fields. |
| CONTEXT | Expected demo id `arrhenius`, formula `exp(-0.8/x)`, macro `direct_division_template` | Both tasks. |
| CONTEXT | Expected statuses `same_ast_return`, `recovered`, evidence class `same_ast` | Task 2. |
| CONTEXT | Preserve Michaelis-Menten and Planck unsupported/stretch behavior | Task 1 avoids broad suite edits; Plan 50-01 reruns CLI unsupported checks. |
</source_audit>

<success_criteria>
Plan 50-02 is complete when `v1.9-arrhenius-evidence` is a built-in suite, `arrhenius-warm` expands as a stable focused run, and the runner artifact proves compile depth, `direct_division_template`, status `same_ast_return`, verifier `recovered`, and evidence class `same_ast`.
</success_criteria>

<output>
After completion, create `.planning/phases/50-arrhenius-exact-warm-start-demo/50-02-SUMMARY.md`.
</output>
