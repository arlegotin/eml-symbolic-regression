---
phase: 53-raw-hybrid-paper-campaign-and-claim-package
plan: 03
type: execute
wave: 3
depends_on:
  - 53-02
files_modified:
  - README.md
  - docs/IMPLEMENTATION.md
  - tests/test_raw_hybrid_paper_regression.py
autonomous: true
requirements:
  - RHY-02
  - RHY-03
  - RHY-04
  - RHY-05
must_haves:
  truths:
    - "README and implementation docs cite the generated v1.9 raw-hybrid paper package only after package artifacts exist and validate."
    - "Docs preserve raw-hybrid claim boundaries: warm-start, same-AST, scaffolded, repaired, refit, compile-only, and perturbed-basin evidence are not pure blind discovery."
    - "Regression tests lock the final package schema, required output files, source locks, scientific-law table rows, regime sections, and forbidden overclaim wording."
    - "Centered-family text stays limited to negative diagnostics under missing same-family witnesses."
  artifacts:
    - path: "README.md"
      provides: "User-facing command and regime-safe v1.9 raw-hybrid package summary"
      contains: "v1.9 raw-hybrid"
    - path: "docs/IMPLEMENTATION.md"
      provides: "Implementation documentation for package command, artifact paths, source locks, scientific-law table fields, and claim boundaries"
      contains: "raw-hybrid-paper"
    - path: "tests/test_raw_hybrid_paper_regression.py"
      provides: "Final regression locks for generated package artifacts and report wording"
      contains: "artifacts/paper/v1.9/raw-hybrid"
  key_links:
    - from: "README.md"
      to: "artifacts/paper/v1.9/raw-hybrid/manifest.json"
      via: "documented artifact path after 53-02 validation"
      pattern: "artifacts/paper/v1.9/raw-hybrid"
    - from: "docs/IMPLEMENTATION.md"
      to: "artifacts/paper/v1.9/raw-hybrid/scientific-law-table.json"
      via: "documented scientific-law table fields"
      pattern: "scientific-law-table"
    - from: "tests/test_raw_hybrid_paper_regression.py"
      to: "artifacts/paper/v1.9/raw-hybrid/raw-hybrid-report.md"
      via: "regression test reads committed generated report"
      pattern: "pure_blind"
---

<objective>
Update README/docs after successful artifacts and lock the final raw-hybrid package report.

Purpose: RHY-05 requires documentation to cite successful artifacts only after they exist, while RHY-02 through RHY-04 need final regression locks that keep evidence regimes, scientific-law rows, and centered-family caveats from drifting.

Output: README and implementation documentation updates plus final regression tests over the generated package artifacts and claim wording.
</objective>

<scope>
In scope:
- `README.md` and `docs/IMPLEMENTATION.md` updates that cite `artifacts/paper/v1.9/raw-hybrid/`.
- New regression test file `tests/test_raw_hybrid_paper_regression.py` reading the generated 53-02 package artifacts.
- Final verification commands for package generation, artifact locks, docs wording, and relevant existing tests.

Out of scope:
- Source changes in `src/`.
- Regenerating or editing the package artifacts from 53-02 except by rerunning the documented command if validation reveals a package-generation defect.
- Any claim that Arrhenius, Michaelis, Shockley, Beer-Lambert warm-start rows, scaffolded shallow proof, repaired candidates, refit attempts, compile-only diagnostics, or perturbed-basin returns are pure blind discovery.
</scope>

<dependencies>
Depends on 53-02 because README/docs must not cite raw-hybrid package artifacts until the generated package exists and validates.
</dependencies>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-CONTEXT.md
@.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-RESEARCH.md
@.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-PATTERNS.md
@.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-02-PLAN.md
@artifacts/paper/v1.9/raw-hybrid/manifest.json
@artifacts/paper/v1.9/raw-hybrid/source-locks.json
@artifacts/paper/v1.9/raw-hybrid/regime-summary.json
@artifacts/paper/v1.9/raw-hybrid/raw-hybrid-report.md
@artifacts/paper/v1.9/raw-hybrid/scientific-law-table.json
@artifacts/paper/v1.9/raw-hybrid/claim-boundaries.md
@artifacts/paper/v1.9/raw-hybrid/centered-negative-diagnostics.md
@README.md
@docs/IMPLEMENTATION.md

<interfaces>
Expected generated package root:
```text
artifacts/paper/v1.9/raw-hybrid/
```

Docs must cite these reproduction commands:
```bash
PYTHONPATH=src python -m eml_symbolic_regression.cli raw-hybrid-paper \
  --output-dir artifacts/paper/v1.9/raw-hybrid \
  --require-existing

PYTHONPATH=src python -m pytest tests/test_raw_hybrid_paper.py tests/test_raw_hybrid_paper_regression.py -q
```

Regression test should read generated artifacts directly:
```python
PACKAGE_ROOT = Path("artifacts/paper/v1.9/raw-hybrid")
manifest = json.loads((PACKAGE_ROOT / "manifest.json").read_text(encoding="utf-8"))
rows = json.loads((PACKAGE_ROOT / "scientific-law-table.json").read_text(encoding="utf-8"))["rows"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add final raw-hybrid package regression locks</name>
  <files>tests/test_raw_hybrid_paper_regression.py</files>
  <behavior>
    - Test 1: all generated package files from 53-02 exist under `artifacts/paper/v1.9/raw-hybrid/`.
    - Test 2: manifest schema is `eml.raw_hybrid_paper.v1`, preset id is `v1.9-raw-hybrid-paper`, and reproduction command contains `raw-hybrid-paper`.
    - Test 3: source locks contain every required source id and every required source has a 64-character SHA-256 hash.
    - Test 4: regime summary contains separate `pure_blind`, `scaffolded`, `compile_only`, `warm_start`, `same_ast_return`, `repaired`, `refit`, and `perturbed_basin` keys.
    - Test 5: scientific-law table rows include Beer-Lambert, Shockley, Arrhenius, Michaelis-Menten, Planck diagnostic, and logistic diagnostic with required RHY-03 fields.
    - Test 6: report Markdown contains same-family witness and negative diagnostic wording, and forbidden overclaim phrases are absent.
  </behavior>
  <action>Create `tests/test_raw_hybrid_paper_regression.py` with file-backed regression tests over `artifacts/paper/v1.9/raw-hybrid/`. Keep this test file focused on committed package artifacts and report wording; do not import private implementation helpers unless needed for a stable public contract. Include a forbidden-phrase list that rejects at least: `universal blind recovery`, `warm-start blind discovery`, `same-AST blind discovery`, `centered families are impossible`, `intrinsically impossible`, and `Planck solved`. Include positive assertions for `not blind discovery`, `same-family witness`, and `negative diagnostic`. If 53-02 package artifacts are missing, the test should fail with a clear message telling the executor to run the package command, not silently skip.</action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_raw_hybrid_paper_regression.py -q</automated>
  </verify>
  <done>Committed regression tests lock the generated package files, source locks, required regimes, required scientific-law rows, centered caveat, and forbidden overclaim wording.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and implementation docs from validated artifacts</name>
  <files>README.md, docs/IMPLEMENTATION.md</files>
  <action>After Task 1 passes against the generated 53-02 package, update `README.md` with a concise `raw-hybrid-paper` command, the artifact root `artifacts/paper/v1.9/raw-hybrid/`, and a regime-safe summary of what the package supports. Name the positive rows as paper-facing raw-hybrid diagnostics: shallow pure-blind boundary, scaffolded shallow proof, perturbed-basin evidence, Beer-Lambert, Shockley, Arrhenius, Michaelis-Menten, and repair-only no-improvement evidence. State explicitly that Arrhenius and Michaelis-Menten are exact compiler warm-start / same-AST evidence, not blind discovery. Update `docs/IMPLEMENTATION.md` with the package writer contract, source-lock behavior, artifact inventory, scientific-law table columns, and claim-boundary policy. Include the centered-family same-family witness caveat and preserve Planck/logistic as unsupported/stretch diagnostics. Do not edit docs before package artifacts exist; do not cite any artifact path that is absent from the generated package.</action>
  <verify>
    <automated>rg "raw-hybrid-paper|artifacts/paper/v1\\.9/raw-hybrid|scientific-law-table|source-locks|not blind discovery|same-family witness" README.md docs/IMPLEMENTATION.md</automated>
    <automated>python - <<'PY'
from pathlib import Path

for path in (Path("README.md"), Path("docs/IMPLEMENTATION.md")):
    text = path.read_text(encoding="utf-8").lower()
    assert "artifacts/paper/v1.9/raw-hybrid" in text, path
    assert "not blind discovery" in text, path
    assert "same-family witness" in text, path
    for forbidden in (
        "universal blind recovery",
        "warm-start blind discovery",
        "same-ast blind discovery",
        "centered families are impossible",
        "intrinsically impossible",
        "planck solved",
    ):
        assert forbidden not in text, (path, forbidden)
PY</automated>
  </verify>
  <done>README and implementation docs cite the validated package artifacts, list reproduction commands and package outputs, and preserve all claim boundaries without blind-discovery overclaims.</done>
</task>

</tasks>

<tests>
Required automated checks:
```bash
PYTHONPATH=src python -m eml_symbolic_regression.cli raw-hybrid-paper --output-dir artifacts/paper/v1.9/raw-hybrid --require-existing --overwrite
PYTHONPATH=src python -m pytest tests/test_raw_hybrid_paper.py tests/test_raw_hybrid_paper_regression.py -q
PYTHONPATH=src python -m pytest tests/test_paper_decision.py tests/test_campaign.py::test_reproduction_command_quotes_shell_sensitive_values -q
rg "raw-hybrid-paper|artifacts/paper/v1\\.9/raw-hybrid|scientific-law-table|source-locks|not blind discovery|same-family witness" README.md docs/IMPLEMENTATION.md
```
</tests>

<risks>
- Docs may already contain related v1.9 evidence language. Preserve correct existing Arrhenius, Michaelis, and repair boundaries while adding the raw-hybrid package section.
- Tests that read generated artifacts are intentionally file-backed. If artifacts are absent, fail clearly and rerun 53-02 package generation.
- Documentation can drift into a single headline recovery claim. Keep evidence lists and tables regime-labeled.
- `--overwrite` in final verification can rewrite package files. Use it only for the declared package root and only after confirming generated artifacts are owned by this phase.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated package -> regression tests | Tests trust committed artifacts as the final paper package and lock their schema/content. |
| Generated package -> docs | README/docs cite package artifacts and claim language derived from them. |
| Docs -> user expectations | User-facing docs can affect scientific claims and demos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03-01 | Tampering | generated artifact regression locks | mitigate | Tests assert source-lock hashes, required files, and package schema. |
| T-53-03-02 | Repudiation | documentation claims | mitigate | Docs cite concrete artifact root, command, table files, and source locks. |
| T-53-03-03 | Information Integrity | README/docs claim language | mitigate | Grep and Python checks reject forbidden blind-discovery and centered-impossibility phrases. |
| T-53-03-04 | Information Disclosure | docs content | accept | Docs cite local artifact paths only and introduce no secrets or external service data. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Docs update after successful artifacts | 53-03 | COVERED | Depends on 53-02 and cites generated package. |
| REQ | RHY-01 | Runnable raw-hybrid package exists | 53-03 | COVERED | Docs and tests reference 53-02 package command and root. |
| REQ | RHY-02 | Regime-separated reports | 53-03 | COVERED | Regression tests lock regime summary and docs wording. |
| REQ | RHY-03 | Scientific-law table required fields | 53-03 | COVERED | Regression tests lock rows/columns and docs list table fields. |
| REQ | RHY-04 | Centered-family negative diagnostics with witness caveat | 53-03 | COVERED | Tests and docs assert caveat. |
| REQ | RHY-05 | README/docs after successful artifacts and no overclaims | 53-03 | COVERED | Main purpose of plan. |
| RESEARCH | - | Documentation gate after package succeeds | 53-03 | COVERED | 53-02 dependency and artifact-backed docs. |
| RESEARCH | - | Avoid blind-discovery overclaims | 53-03 | COVERED | Forbidden-phrase tests and docs assertions. |
| CONTEXT | - | Treat centered results as negative diagnostics, not impossibility | 53-03 | COVERED | Docs and regression tests. |
| CONTEXT | - | Do not claim Planck or logistic solved status | 53-03 | COVERED | Tests reject `Planck solved`; docs preserve unsupported/stretch wording. |
</source_audit>

<success_criteria>
Plan 53-03 is complete when README and `docs/IMPLEMENTATION.md` cite the validated raw-hybrid package and command, `tests/test_raw_hybrid_paper_regression.py` passes against generated artifacts, all final verification commands pass, and no docs/report text violates the Phase 53 claim boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/53-raw-hybrid-paper-campaign-and-claim-package/53-03-SUMMARY.md`.
</output>
