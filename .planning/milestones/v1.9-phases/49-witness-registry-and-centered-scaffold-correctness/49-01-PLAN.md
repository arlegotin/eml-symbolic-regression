---
phase: 49-witness-registry-and-centered-scaffold-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/witnesses.py
  - src/eml_symbolic_regression/__init__.py
  - src/eml_symbolic_regression/benchmark.py
  - tests/test_benchmark_contract.py
autonomous: true
requirements:
  - WIT-01
  - WIT-02
  - WIT-04
must_haves:
  truths:
    - "Developer can inspect registered scaffold witnesses and see exp, log, and scaled_exp available only for raw_eml."
    - "Centered benchmark variants remove exp, log, and scaled_exp scaffold initializers before runs are materialized."
    - "Centered benchmark budgets serialize exp/log/scaled_exp exclusions with centered_family_same_family_witness_missing reason codes."
    - "Raw benchmark variants preserve the existing raw scaffold defaults."
  artifacts:
    - path: "src/eml_symbolic_regression/witnesses.py"
      provides: "Inspectable scaffold witness registry and resolver"
      exports: ["CENTERED_FAMILY_SAME_FAMILY_WITNESS_MISSING", "ScaffoldWitness", "ScaffoldPlan", "known_scaffold_kinds", "list_scaffold_witnesses", "scaffold_witness_for", "resolve_scaffold_plan"]
    - path: "src/eml_symbolic_regression/benchmark.py"
      provides: "Registry-backed budget validation and family-variant scaffold filtering"
      contains: "resolve_scaffold_plan"
    - path: "tests/test_benchmark_contract.py"
      provides: "Registry inspection and centered benchmark exclusion tests"
      contains: "centered_family_same_family_witness_missing"
  key_links:
    - from: "src/eml_symbolic_regression/benchmark.py"
      to: "src/eml_symbolic_regression/witnesses.py"
      via: "OptimizerBudget.validate uses known_scaffold_kinds"
      pattern: "known_scaffold_kinds"
    - from: "src/eml_symbolic_regression/benchmark.py"
      to: "src/eml_symbolic_regression/witnesses.py"
      via: "_operator_variant_budget resolves requested scaffolds against the variant initial operator"
      pattern: "resolve_scaffold_plan"
    - from: "tests/test_benchmark_contract.py"
      to: "src/eml_symbolic_regression/witnesses.py"
      via: "direct registry assertions"
      pattern: "list_scaffold_witnesses"
---

<objective>
Create the scaffold witness registry and make benchmark suite expansion consume it.

Purpose: Phase 49 begins by replacing implicit scaffold availability with a single auditable contract. This plan keeps raw EML defaults intact while making centered-family benchmark budgets drop raw witnesses before execution.

Output: New witness registry module, public inspection exports, benchmark validation/routing through the registry, and focused benchmark contract tests.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-CONTEXT.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-RESEARCH.md
@.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-PATTERNS.md
@.planning/milestones/v1.5-phases/30-bounded-shallow-blind-training-recovery/30-02-SUMMARY.md
@.planning/milestones/v1.7-phases/41-operator-family-campaign-matrix/41-01-SUMMARY.md
@.planning/milestones/v1.8-phases/45-centered-integration-fixes/45-01-SUMMARY.md
@src/eml_symbolic_regression/semantics.py
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/__init__.py
@tests/test_benchmark_contract.py

<interfaces>
Existing operator contract from `src/eml_symbolic_regression/semantics.py`:
```python
@dataclass(frozen=True)
class EmlOperator:
    family: str = "raw_eml"
    s: float = 1.0
    t: complex = 1.0 + 0.0j
    terminal: str = "one"

    @property
    def is_raw(self) -> bool:
        return self.family == "raw_eml"

    @property
    def label(self) -> str: ...
    def as_dict(self) -> dict[str, Any]: ...
```

Existing benchmark budget fields from `src/eml_symbolic_regression/benchmark.py`:
```python
@dataclass(frozen=True)
class OptimizerBudget:
    scaffold_initializers: tuple[str, ...] = ("exp", "log", "scaled_exp")
    scaffold_exclusions: tuple[str, ...] = ()
    operator_family: EmlOperator = field(default_factory=raw_eml_operator)
    operator_schedule: tuple[EmlOperator, ...] = ()

    def validate(self, path: str) -> None: ...
    def as_dict(self) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add raw-only scaffold witness registry</name>
  <read_first>
    <file>src/eml_symbolic_regression/semantics.py</file>
    <file>src/eml_symbolic_regression/__init__.py</file>
    <file>tests/test_benchmark_contract.py</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-CONTEXT.md</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-PATTERNS.md</file>
  </read_first>
  <files>src/eml_symbolic_regression/witnesses.py, src/eml_symbolic_regression/__init__.py, tests/test_benchmark_contract.py</files>
  <behavior>
    - Test 1: Registry inspection returns exactly raw_eml witness entries for exp, log, and scaled_exp.
    - Test 2: Raw EML resolves exp/log/scaled_exp as enabled with no exclusions.
    - Test 3: CEML_2 and ZEML_8 resolve exp/log/scaled_exp as excluded with centered_family_same_family_witness_missing.
  </behavior>
  <action>Create `src/eml_symbolic_regression/witnesses.py` with stdlib-only dataclasses plus `EmlOperator`. Define `CENTERED_FAMILY_SAME_FAMILY_WITNESS_MISSING = "centered_family_same_family_witness_missing"`. Add frozen `ScaffoldWitness(kind: str, operator_family: str, attempt_kind: str, min_depth: int, strategy: str)` with `as_dict()`. Add frozen `ScaffoldPlan(enabled: tuple[str, ...], exclusions: tuple[str, ...])` with `as_dict()` returning JSON-safe lists. Register only three entries: `exp/raw_eml/scaffold_exp/min_depth=1/strategy=generic_paper_primitive`, `log/raw_eml/scaffold_log/min_depth=3/strategy=generic_paper_primitive`, and `scaled_exp/raw_eml/scaffold_scaled_exp/min_depth=9/strategy=paper_scaled_exponential_family`. Add `known_scaffold_kinds()`, `list_scaffold_witnesses()`, `scaffold_exclusion_code(kind, reason)`, `scaffold_witness_for(kind, operator)`, and `resolve_scaffold_plan(requested, operator)`. `resolve_scaffold_plan` must preserve requested order, de-duplicate with `tuple(dict.fromkeys(...))`, enable only entries with a same-family witness, and emit `{kind}:centered_family_same_family_witness_missing` for known scaffold kinds requested against non-raw operators without a same-family witness. Export registry inspection helpers and the reason constant from `src/eml_symbolic_regression/__init__.py` so WIT-01 is top-level inspectable. Add `test_scaffold_witness_registry_declares_raw_only_current_witnesses` to `tests/test_benchmark_contract.py` using `raw_eml_operator()`, `ceml_s_operator(2.0)`, and `zeml_s_operator(8.0)`. Do not register centered witnesses; CENT-01/CENT-02 remain deferred.</action>
  <verify>
    <automated>python -m pytest tests/test_benchmark_contract.py::test_scaffold_witness_registry_declares_raw_only_current_witnesses -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "CENTERED_FAMILY_SAME_FAMILY_WITNESS_MISSING" src/eml_symbolic_regression/witnesses.py` finds the canonical constant.</criterion>
    <criterion>`rg "ScaffoldWitness\\(\"exp\", \"raw_eml\", \"scaffold_exp\", 1" src/eml_symbolic_regression/witnesses.py` finds the raw exp entry, with matching raw log and scaled_exp entries present.</criterion>
    <criterion>`python -m pytest tests/test_benchmark_contract.py::test_scaffold_witness_registry_declares_raw_only_current_witnesses -q` passes.</criterion>
  </acceptance_criteria>
  <done>WIT-01 is satisfied: developer inspection shows exp/log/scaled_exp are raw_eml-only witnesses and centered operators receive explicit missing-witness exclusions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route benchmark budgets through witness availability</name>
  <read_first>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>src/eml_symbolic_regression/witnesses.py</file>
    <file>tests/test_benchmark_contract.py</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-RESEARCH.md</file>
    <file>.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-PATTERNS.md</file>
  </read_first>
  <files>src/eml_symbolic_regression/benchmark.py, tests/test_benchmark_contract.py</files>
  <behavior>
    - Test 1: `v1.7-family-shallow` centered Beer-Lambert scaffolded runs have empty scaffold_initializers and exp/log/scaled_exp missing-witness exclusions.
    - Test 2: Raw variants still keep `("exp", "log", "scaled_exp")` unless the source suite explicitly narrows them.
    - Test 3: `v1.8-family-calibration` continuation rows use the first scheduled operator for scaffold availability and record the same centered exclusions.
    - Test 4: Unknown scaffold names still raise `BenchmarkValidationError(reason="invalid_budget", path="optimizer.scaffold_initializers")`.
  </behavior>
  <action>In `src/eml_symbolic_regression/benchmark.py`, import `known_scaffold_kinds` and `resolve_scaffold_plan` from `witnesses.py`. Replace the hardcoded `allowed_scaffolds = {"exp", "log", "scaled_exp"}` in `OptimizerBudget.validate()` with `set(known_scaffold_kinds())`. Replace `_operator_variant_budget()`'s current `scaled_exp`-only centered special case and the old `scaled_exp:centered_family_incompatible_raw_witness` reason. Compute `initial_operator = variant.operator_schedule[0] if variant.operator_schedule else variant.operator_family`; call `plan = resolve_scaffold_plan(base.scaffold_initializers, initial_operator)`; return `replace(base, scaffold_initializers=plan.enabled, scaffold_exclusions=tuple(dict.fromkeys((*base.scaffold_exclusions, *plan.exclusions))), operator_family=variant.operator_family, operator_schedule=variant.operator_schedule)`. Update `tests/test_benchmark_contract.py` so `test_family_matrix_suites_clone_regimes_with_operator_variants` expects centered scaffolded `CEML_2` runs to have no `exp`, `log`, or `scaled_exp` initializers and exactly these exclusions in order: `exp:centered_family_same_family_witness_missing`, `log:centered_family_same_family_witness_missing`, `scaled_exp:centered_family_same_family_witness_missing`. Add a continuation assertion for a `ZEML_8 -> ZEML_4 -> ZEML_2 -> ZEML_1` calibration row proving the same exclusions are based on `ZEML_8`. Keep all raw EML suite rows and raw defaults unchanged.</action>
  <verify>
    <automated>python -m pytest tests/test_benchmark_contract.py::test_family_matrix_suites_clone_regimes_with_operator_variants tests/test_benchmark_contract.py::test_v18_family_matrix_expands_scales_and_schedules tests/test_benchmark_contract.py::test_optimizer_budget_parses_and_serializes_constants -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg "centered_family_incompatible_raw_witness" src/eml_symbolic_regression/benchmark.py tests/test_benchmark_contract.py` returns no matches.</criterion>
    <criterion>`python -m pytest tests/test_benchmark_contract.py::test_family_matrix_suites_clone_regimes_with_operator_variants -q` passes and asserts exp/log/scaled_exp exclusions for centered variants.</criterion>
    <criterion>`python -m pytest tests/test_benchmark_contract.py::test_optimizer_budget_parses_and_serializes_constants -q` passes, preserving fail-closed invalid scaffold validation.</criterion>
  </acceptance_criteria>
  <done>WIT-02 and the benchmark side of WIT-04 are satisfied: centered benchmark variants cannot carry raw scaffold attempts and budget artifacts expose the canonical reason code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite JSON -> OptimizerBudget | User-authored or generated benchmark configs provide scaffold names and operator-family selections. |
| benchmark family expansion -> run artifacts | Suite expansion mutates raw cases into centered variants and records evidence denominators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | `OptimizerBudget.validate` | mitigate | Validate scaffold names against `known_scaffold_kinds()` so unknown initializers fail closed before suite execution. |
| T-49-02 | Repudiation | `_operator_variant_budget` | mitigate | Serialize ordered `*:centered_family_same_family_witness_missing` exclusions in budget payloads for centered runs. |
| T-49-03 | Information Integrity | raw benchmark variants | mitigate | Contract tests assert raw EML scaffold defaults remain available and centered exclusions do not change verifier-owned recovery semantics. |
</threat_model>

<verification>
Run focused contract checks:

```bash
python -m pytest tests/test_benchmark_contract.py::test_scaffold_witness_registry_declares_raw_only_current_witnesses tests/test_benchmark_contract.py::test_family_matrix_suites_clone_regimes_with_operator_variants tests/test_benchmark_contract.py::test_v18_family_matrix_expands_scales_and_schedules tests/test_benchmark_contract.py::test_optimizer_budget_parses_and_serializes_constants -q
```
</verification>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Make scaffold/witness availability explicit by operator family | Task 1 creates `witnesses.py`; Task 2 consumes it in benchmark expansion. |
| GOAL | Prevent raw witnesses from contaminating centered-family runs | Task 2 removes exp/log/scaled_exp from centered benchmark budgets. |
| REQ | WIT-01 inspectable registry | Task 1. |
| REQ | WIT-02 centered families no longer receive raw scaffolds through benchmark budgets | Task 2. Direct optimizer coverage is in Plan 49-02. |
| REQ | WIT-04 benchmark artifacts record centered scaffold exclusions | Task 2 budget serialization. Runner artifact survival is in Plan 49-02. |
| RESEARCH | Use `raw_eml` entries for exp/log/scaled_exp only | Task 1 exact registry entries. |
| RESEARCH | Use canonical reason `centered_family_same_family_witness_missing` | Tasks 1 and 2. |
| CONTEXT | Preserve raw EML defaults | Task 2 raw-regression assertions. |
| CONTEXT | Do not construct centered witnesses | Task 1 explicitly registers no centered entries. |
</source_audit>

<success_criteria>
Plan 49-01 is complete when the registry is inspectable, benchmark validation uses registry-known scaffold kinds, centered family matrix budgets drop all raw-only scaffold initializers, and tests prove the old `centered_family_incompatible_raw_witness` reason is gone from benchmark contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/49-witness-registry-and-centered-scaffold-correctness/49-01-SUMMARY.md`.
</output>
