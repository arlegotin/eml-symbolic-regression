---
phase: 52-verifier-gated-exact-cleanup-expansion
plan: 03
type: execute
wave: 3
depends_on:
  - 52-02
files_modified:
  - artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/suite-result.json
  - artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/aggregate.json
  - artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/aggregate.md
  - artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.json
  - artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.md
  - docs/IMPLEMENTATION.md
  - README.md
autonomous: true
requirements:
  - REP-04
must_haves:
  truths:
    - "Developer can run one focused command to regenerate `v1.9-repair-evidence` artifacts."
    - "Evidence artifacts compare default selected-only cleanup with expanded candidate-pool cleanup for declared near-miss pairs."
    - "The evidence summary states whether expanded cleanup improved measured near-miss outcomes, and no-improvement remains valid evidence if fallback behavior is preserved."
    - "Docs describe expanded cleanup as verifier-gated repair evidence, not blind discovery, same-AST warm-start, compile-only, or perturbed true-tree recovery."
    - "Docs cite generated artifacts only after JSON validation passes."
  artifacts:
    - path: "artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/suite-result.json"
      provides: "Machine-readable focused repair evidence suite output"
      contains: "v1.9-repair-evidence"
    - path: "artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/aggregate.json"
      provides: "Aggregate repair evidence counts and metrics"
      contains: "repaired_candidate"
    - path: "artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/aggregate.md"
      provides: "Human-readable aggregate report for the focused repair suite"
      contains: "Repair"
    - path: "artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.json"
      provides: "Before/after default-vs-expanded repair comparison summary"
      contains: "expanded_candidate_pool"
    - path: "artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.md"
      provides: "Readable before/after repair evidence notes"
      contains: "expanded candidate-pool cleanup"
    - path: "docs/IMPLEMENTATION.md"
      provides: "Implementation contract for expanded verifier-gated cleanup"
      contains: "v1.9-repair-evidence"
    - path: "README.md"
      provides: "User-facing command and regime-safe evidence summary"
      contains: "v1.9-repair-evidence"
  key_links:
    - from: "artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.json"
      to: "artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/suite-result.json"
      via: "summary generated from suite-result paired runs"
      pattern: "default_cleanup"
    - from: "docs/IMPLEMENTATION.md"
      to: "artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.json"
      via: "docs cite validated evidence artifact path"
      pattern: "repair-evidence-summary.json"
    - from: "README.md"
      to: "src/eml_symbolic_regression/benchmark.py"
      via: "documented CLI command uses built-in suite"
      pattern: "v1.9-repair-evidence"
---

<objective>
Generate targeted Phase 52 evidence artifacts and document the expanded cleanup contract.

Purpose: REP-04 requires inspectable before/after evidence showing whether expanded cleanup improves declared near-miss subsets without weakening fallback behavior. This plan produces the evidence and updates docs only after validation.

Output: Focused benchmark artifacts, before/after repair evidence summary JSON/Markdown, and regime-safe README/implementation documentation.
</objective>

<scope>
In scope:
- Running `v1.9-repair-evidence` and committing the generated artifacts under `artifacts/campaigns/v1.9-repair-evidence/`.
- Generating a small before/after summary artifact from suite results.
- Updating `docs/IMPLEMENTATION.md` and `README.md` with commands, artifact paths, measured outcomes, and taxonomy boundaries.

Out of scope:
- Source-code changes in `src/` or `tests/`.
- Changing the focused suite contract created by 52-02.
- Reclassifying repaired candidates as blind discovery, same-AST warm-start recovery, compile-only verification, or perturbed true-tree recovery.
- Running broad standard, showcase, proof, v1.8, or paper-facing campaigns.
</scope>

<dependencies>
Depends on 52-02 because the focused suite, repair preset, artifact metrics, and report columns must exist before evidence can be generated and documented.
</dependencies>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-verifier-gated-exact-cleanup-expansion/52-CONTEXT.md
@.planning/phases/52-verifier-gated-exact-cleanup-expansion/52-RESEARCH.md
@.planning/phases/52-verifier-gated-exact-cleanup-expansion/52-PATTERNS.md
@.planning/phases/52-verifier-gated-exact-cleanup-expansion/52-02-PLAN.md
@.planning/phases/51-reciprocal-and-saturation-compiler-motifs/51-03-SUMMARY.md
@docs/IMPLEMENTATION.md
@README.md

<interfaces>
Expected focused benchmark command:
```bash
PYTHONPATH=src python -m eml_symbolic_regression.cli benchmark \
  v1.9-repair-evidence \
  --output-dir artifacts/campaigns/v1.9-repair-evidence
```

Expected repair metadata fields from 52-01 and 52-02:
```json
{
  "repair": {
    "status": "repaired_candidate | not_repaired",
    "candidate_root_count": 1,
    "deduped_variant_count": 24,
    "accepted_candidate_id": "attempt-...",
    "accepted_candidate_source": "hardening_checkpoint",
    "accepted_candidate_root_source": "retained"
  },
  "metrics": {
    "repair_candidate_root_count": 1,
    "repair_deduped_variant_count": 24,
    "repair_accepted_candidate_root_source": "retained"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and validate focused repair evidence artifacts</name>
  <files>artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/suite-result.json, artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/aggregate.json, artifacts/campaigns/v1.9-repair-evidence/v1.9-repair-evidence/aggregate.md, artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.json, artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.md</files>
  <action>Run the focused benchmark command exactly as documented in the context. Confirm the command writes `suite-result.json`, `aggregate.json`, `aggregate.md`, and one per-run JSON artifact for each expanded run in the generated suite directory. Then create `artifacts/campaigns/v1.9-repair-evidence/repair-evidence-summary.json` and `repair-evidence-summary.md` by reading `suite-result.json` and pairing `default_cleanup` runs with matching `expanded_cleanup` runs for the same formula/start mode/seed/noise. The summary must include for each pair: case ids, run ids, statuses, claim statuses, evidence classes, repair statuses, repair verifier statuses, selected candidate ids, fallback candidate ids, repair variant counts, candidate root counts, deduped variant counts, accepted candidate id/source/root source, and a boolean `fallback_manifest_preserved` that is true only when each artifact still contains selected and fallback candidate manifests from the optimizer payload. Include aggregate totals: number of pairs, default repaired count, expanded repaired count, expanded improvements over paired default, regressions in final status, and any cases with missing repair metadata. If expanded cleanup produces no measured improvement, state that explicitly in both summary files; do not edit suite definitions to force an improvement. Keep generated artifacts deterministic except for existing timing/environment fields emitted by the benchmark runner.</action>
  <verify>
    <automated>PYTHONPATH=src python -m eml_symbolic_regression.cli benchmark v1.9-repair-evidence --output-dir artifacts/campaigns/v1.9-repair-evidence</automated>
    <automated>PYTHONPATH=src python - <<'PY'
import json
from pathlib import Path

root = Path("artifacts/campaigns/v1.9-repair-evidence")
suite = root / "v1.9-repair-evidence" / "suite-result.json"
aggregate = root / "v1.9-repair-evidence" / "aggregate.json"
summary = root / "repair-evidence-summary.json"
for path in (suite, aggregate, summary, root / "repair-evidence-summary.md"):
    assert path.exists(), path

suite_payload = json.loads(suite.read_text())
aggregate_payload = json.loads(aggregate.read_text())
summary_payload = json.loads(summary.read_text())
assert suite_payload["suite"]["id"] == "v1.9-repair-evidence"
assert summary_payload["suite_id"] == "v1.9-repair-evidence"
assert summary_payload["pair_count"] >= 2
assert "expanded_improvements" in summary_payload
assert "regressions" in summary_payload
assert aggregate_payload["counts"]["total"] == len(suite_payload["results"])
for pair in summary_payload["pairs"]:
    assert pair["default"]["case_id"].endswith("default")
    assert pair["expanded"]["case_id"].endswith("expanded")
    assert pair["expanded"]["repair_preset"] == "expanded_candidate_pool"
    assert pair["default"]["fallback_manifest_preserved"] is True
    assert pair["expanded"]["fallback_manifest_preserved"] is True
PY</automated>
  </verify>
  <done>Focused evidence artifacts and before/after summary files exist, validate structurally, and state measured improvement/regression counts without changing fallback manifests.</done>
</task>

<task type="auto">
  <name>Task 2: Document expanded cleanup evidence without overclaiming recovery</name>
  <files>docs/IMPLEMENTATION.md, README.md</files>
  <action>Update `docs/IMPLEMENTATION.md` to describe Phase 52 expanded cleanup: selected-only cleanup remains the default direct repair behavior, the benchmark `expanded_candidate_pool` repair preset can inspect selected/fallback/retained candidate roots, exact AST roots/variants are deduplicated, accepted repairs remain verifier-gated, and original selected/fallback manifests remain intact. Add `v1.9-repair-evidence` to the focused evidence suite documentation with the command, artifact root, summary files, and the measured default-vs-expanded results from `repair-evidence-summary.json`. Update `README.md` with a concise command and evidence note for developers. The wording must say repaired candidates are `repaired_candidate` evidence, not blind discovery, not compile-only evidence, not same-AST warm-start evidence, and not perturbed true-tree recovery. Mention Arrhenius and Michaelis same-AST evidence only to preserve regime separation; do not change their claims or artifact paths except if shared wording needs to name Phase 52 as repair-only evidence. Cite the generated artifact paths only after Task 1 validation passes.</action>
  <verify>
    <automated>rg "v1\\.9-repair-evidence|repair-evidence-summary|expanded candidate-pool cleanup|repaired_candidate|expanded_candidate_pool" README.md docs/IMPLEMENTATION.md</automated>
    <automated>PYTHONPATH=src python -m pytest tests/test_benchmark_contract.py::test_repair_evidence_suite_contains_default_and_expanded_near_miss_pairs tests/test_benchmark_runner.py::test_candidate_pool_cleanup_promotes_artifact_without_mutating_selected_or_fallback -q</automated>
  </verify>
  <done>Docs provide reproducible commands, validated artifact paths, measured before/after repair results, and regime-safe wording for expanded cleanup.</done>
</task>

</tasks>

<tests>
Required automated checks:
```bash
PYTHONPATH=src python -m eml_symbolic_regression.cli benchmark v1.9-repair-evidence --output-dir artifacts/campaigns/v1.9-repair-evidence
PYTHONPATH=src python -m pytest \
  tests/test_benchmark_contract.py::test_repair_evidence_suite_contains_default_and_expanded_near_miss_pairs \
  tests/test_benchmark_runner.py::test_candidate_pool_cleanup_promotes_artifact_without_mutating_selected_or_fallback \
  -q
rg "v1\\.9-repair-evidence|repair-evidence-summary|expanded candidate-pool cleanup|repaired_candidate|expanded_candidate_pool" README.md docs/IMPLEMENTATION.md
```
</tests>

<risks>
- The targeted near-miss suite may show no improvement. That is acceptable for REP-04 if the summary says so and fallback behavior is preserved.
- Generated per-run filenames depend on run ids created in 52-02. Use `suite-result.json` to discover them rather than hardcoding hashes in validation scripts.
- Documentation can accidentally overclaim repaired candidates as blind discovery. Mitigate with explicit taxonomy wording and references to `repaired_candidate`.
- Broad campaign reruns can consume time and alter unrelated evidence. Run only `v1.9-repair-evidence` in this plan.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark runner -> artifact files | Local benchmark execution writes evidence consumed by docs and later paper packaging. |
| Artifact files -> documentation | Docs cite measured JSON/Markdown evidence. |
| Focused evidence -> future paper suite | Phase 53 may consume these artifacts but must keep regime labels separate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03-01 | Tampering | generated evidence summary | mitigate | Validate summary against suite-result and aggregate JSON before docs edits. |
| T-52-03-02 | Repudiation | measured improvement claims | mitigate | Store pair-level default/expanded statuses, repair metrics, run ids, and artifact paths in JSON and Markdown. |
| T-52-03-03 | Information Integrity | docs claim language | mitigate | Docs explicitly classify repaired candidates as `repaired_candidate` only. |
| T-52-03-04 | Denial of Service | broad reruns | mitigate | Run only the focused `v1.9-repair-evidence` suite. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Broaden cleanup and preserve verifier-owned fallback behavior | 52-03 | COVERED | Artifacts measure expanded cleanup and fallback preservation. |
| REQ | REP-04 | Inspect targeted before/after evidence without weakening fallback behavior | 52-03 | COVERED | Focused suite artifacts and repair evidence summary. |
| RESEARCH | - | Treat no-improvement on real near-miss subsets as valid evidence if fallback behavior is unchanged | 52-03 | COVERED | Summary must report measured result without forcing pass. |
| RESEARCH | - | Document focused repair suite as near-miss repair evidence, not proof denominator | 52-03 | COVERED | README and implementation docs. |
| CONTEXT | - | Favor focused near-miss fixtures and targeted evidence suite over broad reruns | 52-03 | COVERED | Only `v1.9-repair-evidence` is run. |
| CONTEXT | - | Do not present repaired candidates as blind discovery | 52-03 | COVERED | Explicit doc wording and artifact taxonomy. |
| CONTEXT | - | Do not change Arrhenius or Michaelis same-AST regimes except shared compatibility wording | 52-03 | COVERED | Docs keep them separate. |
</source_audit>

<success_criteria>
Plan 52-03 is complete when `v1.9-repair-evidence` artifacts exist, `repair-evidence-summary.json` and `.md` compare default versus expanded cleanup for declared near-miss pairs, validation confirms fallback manifests remain present, and README/implementation docs cite the evidence with repaired-candidate-only claim language.
</success_criteria>

<output>
After completion, create `.planning/phases/52-verifier-gated-exact-cleanup-expansion/52-03-SUMMARY.md`.
</output>
