---
phase: 24-baseline-failure-triage-and-diagnostic-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eml_symbolic_regression/benchmark.py
  - src/eml_symbolic_regression/campaign.py
  - src/eml_symbolic_regression/cli.py
  - src/eml_symbolic_regression/diagnostics.py
  - tests/test_diagnostics.py
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
autonomous: true
user_setup: []
must_haves:
  truths:
    - Baseline triage compares v1.3 standard and showcase failures by formula, start mode, perturbation noise, and recovery class.
    - Triage rows link to representative raw run artifacts and include optimizer/verifier metrics.
    - Focused diagnostic reruns can target blind failures, Beer-Lambert perturbation failures, and compiler/depth gates without a full campaign.
    - v1.3 aggregate and suite-result fingerprints are persisted without modifying v1.3 campaign folders.
  artifacts:
    - src/eml_symbolic_regression/diagnostics.py
    - tests/test_diagnostics.py
  key_links:
    - src/eml_symbolic_regression/cli.py exposes diagnostics triage and rerun commands backed by src/eml_symbolic_regression/diagnostics.py.
    - src/eml_symbolic_regression/benchmark.py RunFilter supports perturbation-noise filtering used by diagnostics reruns.
---

<objective>
Add a baseline diagnostics layer over committed v1.3 campaign evidence.

Purpose: Later optimizer/compiler work needs a stable scoreboard and small reproducible subsets instead of repeatedly reading full campaign reports.
Output: Diagnostics API, CLI commands, perturbation-aware filters, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-baseline-failure-triage-and-diagnostic-harness/24-CONTEXT.md
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/campaign.py
@src/eml_symbolic_regression/cli.py
@tests/test_campaign.py
@tests/test_benchmark_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add perturbation-aware run filtering</name>
  <files>src/eml_symbolic_regression/benchmark.py, src/eml_symbolic_regression/campaign.py, src/eml_symbolic_regression/cli.py</files>
  <read_first>src/eml_symbolic_regression/benchmark.py, src/eml_symbolic_regression/campaign.py, src/eml_symbolic_regression/cli.py, tests/test_benchmark_runner.py</read_first>
  <action>Add a `perturbation_noises: tuple[float, ...] = ()` field to `RunFilter`, include it in `includes`, serialize it in campaign manifest filter payloads, and add repeatable CLI flags `--perturbation-noise` to benchmark and campaign commands. Reproduction commands must emit the same flag for each selected noise.</action>
  <verify>python -m pytest tests/test_benchmark_runner.py tests/test_campaign.py -q</verify>
  <acceptance_criteria>
    - src/eml_symbolic_regression/benchmark.py contains `perturbation_noises: tuple[float, ...] = ()`.
    - src/eml_symbolic_regression/cli.py contains `--perturbation-noise`.
    - src/eml_symbolic_regression/campaign.py contains `"perturbation_noises"`.
  </acceptance_criteria>
  <done>Focused filters can select exact Beer-Lambert perturbation noise levels.</done>
</task>

<task type="auto">
  <name>Task 2: Add baseline triage and focused rerun diagnostics</name>
  <files>src/eml_symbolic_regression/diagnostics.py, src/eml_symbolic_regression/cli.py</files>
  <read_first>artifacts/campaigns/v1.3-standard/aggregate.json, artifacts/campaigns/v1.3-showcase/aggregate.json, src/eml_symbolic_regression/campaign.py</read_first>
  <action>Create `diagnostics.py` with functions to load campaign directories, build triage JSON/Markdown, fingerprint baseline files with SHA-256, select focused subsets named `blind-failures`, `beer-perturbation-failures`, and `compiler-depth-gates`, and run a focused campaign through existing `run_campaign`. Add CLI subcommands `diagnostics triage` and `diagnostics rerun`.</action>
  <verify>python -m pytest tests/test_diagnostics.py -q</verify>
  <acceptance_criteria>
    - src/eml_symbolic_regression/diagnostics.py contains `write_baseline_triage`.
    - src/eml_symbolic_regression/diagnostics.py contains `select_diagnostic_runs`.
    - src/eml_symbolic_regression/cli.py contains `diagnostics`.
  </acceptance_criteria>
  <done>Triage and focused reruns are available from Python and CLI.</done>
</task>

<task type="auto">
  <name>Task 3: Lock tests for baseline diagnostics</name>
  <files>tests/test_diagnostics.py</files>
  <read_first>tests/test_campaign.py, tests/test_benchmark_reports.py</read_first>
  <action>Add tests that write triage reports from synthetic campaign folders, verify failure grouping and raw artifact links, verify baseline fingerprints are present, and verify rerun filters include exact perturbation noise instead of rerunning every noise level.</action>
  <verify>python -m pytest tests/test_diagnostics.py tests/test_benchmark_runner.py tests/test_campaign.py -q</verify>
  <acceptance_criteria>
    - tests/test_diagnostics.py contains `test_baseline_triage_writes_report_and_lock`.
    - tests/test_diagnostics.py contains `test_diagnostic_rerun_uses_exact_perturbation_noise_filter`.
  </acceptance_criteria>
  <done>Automated tests cover all DIAG requirements.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_diagnostics.py tests/test_benchmark_runner.py tests/test_campaign.py -q
- [ ] PYTHONPATH=src python -m eml_symbolic_regression.cli diagnostics triage --baseline artifacts/campaigns/v1.3-standard --baseline artifacts/campaigns/v1.3-showcase --output-dir artifacts/diagnostics/v1.4-baseline
</verification>

<success_criteria>
- Diagnostics files and CLI commands exist.
- Tests pass.
- v1.3 campaign folders are not modified.
- Phase 24 requirements DIAG-01 through DIAG-04 are demonstrably covered.
</success_criteria>
