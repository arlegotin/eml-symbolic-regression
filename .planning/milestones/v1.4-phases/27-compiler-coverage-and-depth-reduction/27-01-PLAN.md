---
phase: 27-compiler-coverage-and-depth-reduction
plan: 01
type: execute
wave: 1
depends_on: ["24-01"]
files_modified:
  - src/eml_symbolic_regression/compiler.py
  - src/eml_symbolic_regression/benchmark.py
  - tests/test_compiler_warm_start.py
  - tests/test_benchmark_runner.py
requirements: [COV-01, COV-02, COV-03, COV-04]
autonomous: true
user_setup: []
must_haves:
  truths:
    - Compiler diagnostics expose strict unsupported reason plus relaxed depth, node count, rule trace, and validation when available.
    - Shockley moves from v1.3 depth-gated unsupported to a verified compiled EML AST under the v1.4 default gate.
    - Damped oscillator remains explicitly unsupported for `cos` with tests documenting the deferred path.
    - Unsupported formulas continue to emit structured reasons and diagnostics instead of invalid ASTs.
  artifacts:
    - src/eml_symbolic_regression/compiler.py
    - src/eml_symbolic_regression/benchmark.py
    - tests/test_compiler_warm_start.py
    - tests/test_benchmark_runner.py
  key_links:
    - Benchmark compile failures include compiler diagnostics from compiler.py.
---

<objective>
Improve compiler coverage diagnostics and verify a lower-depth Shockley compile path.

Purpose: v1.3 campaign evidence showed compiler/depth gates as a major unsupported category. This plan makes those gates inspectable and moves one formula into verified compiled coverage.
Output: Compiler diagnostics, Shockley lower-depth template, benchmark diagnostic payloads, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-compiler-coverage-and-depth-reduction/27-CONTEXT.md
@src/eml_symbolic_regression/compiler.py
@src/eml_symbolic_regression/benchmark.py
@tests/test_compiler_warm_start.py
@tests/test_benchmark_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compiler diagnostics and benchmark payloads</name>
  <files>src/eml_symbolic_regression/compiler.py, src/eml_symbolic_regression/benchmark.py, tests/test_compiler_warm_start.py, tests/test_benchmark_runner.py</files>
  <read_first>src/eml_symbolic_regression/compiler.py, src/eml_symbolic_regression/benchmark.py, tests/test_compiler_warm_start.py, tests/test_benchmark_runner.py</read_first>
  <action>Add `diagnose_compile_expression` to compiler.py. On strict failure, return strict `UnsupportedExpression.as_dict()` plus relaxed compile metadata (`depth`, `node_count`, `trace`) and validation if a relaxed compile succeeds. Add this diagnostic payload to `_compile_demo` unsupported results. Add tests for Planck depth diagnostics and damped oscillator unsupported operator diagnostics.</action>
  <verify>python -m pytest tests/test_compiler_warm_start.py tests/test_benchmark_runner.py -q</verify>
  <acceptance_criteria>
    - src/eml_symbolic_regression/compiler.py contains `diagnose_compile_expression`.
    - src/eml_symbolic_regression/benchmark.py contains `diagnostic`.
    - tests cover depth and unsupported-operator diagnostic payloads.
  </acceptance_criteria>
  <done>Unsupported compiler paths are inspectable without accepting invalid ASTs.</done>
</task>

<task type="auto">
  <name>Task 2: Add Shockley lower-depth template</name>
  <files>src/eml_symbolic_regression/compiler.py, src/eml_symbolic_regression/benchmark.py, tests/test_compiler_warm_start.py</files>
  <read_first>src/eml_symbolic_regression/compiler.py, src/eml_symbolic_regression/datasets.py, tests/test_compiler_warm_start.py</read_first>
  <action>Add a fail-closed template for Add expressions of the form `c*exp(a) - c`: compile to `multiply_expr(Const(c), Eml(compiled_a, Const(e)))`, record rule `scaled_exp_minus_one_template`, and validate normally. Raise default `CompilerConfig.max_depth` and `OptimizerBudget.max_compile_depth` from 12 to 13 so this lower-depth candidate is accepted while deeper formulas remain gated. Add tests that Shockley compiles and validates under default v1.4 gates and that the trace includes the new rule.</action>
  <verify>python -m pytest tests/test_compiler_warm_start.py tests/test_benchmark_runner.py -q</verify>
  <acceptance_criteria>
    - src/eml_symbolic_regression/compiler.py contains `scaled_exp_minus_one_template`.
    - tests assert Shockley compile validation passes and depth is <= 13.
    - Damped oscillator unsupported-operator test still passes.
  </acceptance_criteria>
  <done>At least one v1.3 depth-gated formula moves to verified compiled EML coverage.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_compiler_warm_start.py tests/test_benchmark_runner.py tests/test_campaign.py -q
</verification>

<success_criteria>
- Shockley compiles and validates under v1.4 defaults.
- Other unsupported formulas remain fail-closed with diagnostics.
- Tests pass.
</success_criteria>
