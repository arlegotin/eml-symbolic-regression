---
phase: 32-paper-depth-curve-training-evidence
plan: 01
status: pending
type: execute
wave: 1
depends_on:
  - 29
  - 30
  - 31
files_modified:
  - src/eml_symbolic_regression/depth_curve.py
  - src/eml_symbolic_regression/datasets.py
  - src/eml_symbolic_regression/benchmark.py
  - src/eml_symbolic_regression/proof.py
  - src/eml_symbolic_regression/campaign.py
  - README.md
  - tests/test_depth_curve_targets.py
  - tests/test_benchmark_contract.py
  - tests/test_benchmark_runner.py
  - tests/test_benchmark_reports.py
  - tests/test_campaign.py
  - tests/test_proof_contract.py
requirements:
  - CURV-01
  - CURV-02
  - CURV-03
  - CURV-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "User can run a deterministic `proof-depth-curve` suite covering depths 2 through 6 for both blind and perturbed training."
    - "Depth-curve reports include per-depth recovery rates, seed counts, runtime, losses, and snap metrics split by training mode."
    - "The paper blind-depth claim remains a measured boundary: deeper blind failures stay visible and perturbed rows stay comparative."
    - "Depth-curve raw artifacts are reproducibly generated through benchmark and campaign entry points."
  artifacts:
    - path: "src/eml_symbolic_regression/depth_curve.py"
      provides: "Deterministic exact EML target inventory for depths 2 through 6"
      exports: ["DepthCurveTargetSpec", "depth_curve_target_specs", "depth_curve_target_spec"]
    - path: "src/eml_symbolic_regression/benchmark.py"
      provides: "Built-in proof-depth-curve suite, validation, and aggregate depth-curve summaries"
      exports: ["builtin_suite", "aggregate_evidence", "render_aggregate_markdown"]
    - path: "src/eml_symbolic_regression/campaign.py"
      provides: "Campaign preset, depth-curve CSV/report output, and recovery plot"
      exports: ["campaign_preset", "run_campaign", "write_campaign_report", "write_campaign_plots"]
    - path: "tests/test_depth_curve_targets.py"
      provides: "Inventory determinism and verifier-owned target checks"
    - path: "tests/test_benchmark_runner.py"
      provides: "Blind failure versus perturbed recovery runner smoke"
  key_links:
    - from: "src/eml_symbolic_regression/depth_curve.py"
      to: "src/eml_symbolic_regression/datasets.py"
      via: "Depth-curve target specs become registered proof/demo formulas"
      pattern: "depth_curve_target_specs|demo_specs"
    - from: "src/eml_symbolic_regression/benchmark.py"
      to: "src/eml_symbolic_regression/proof.py"
      via: "Depth-curve rows bind to `paper-blind-depth-degradation` with `measured_depth_curve`"
      pattern: "paper-blind-depth-degradation|measured_depth_curve"
    - from: "src/eml_symbolic_regression/campaign.py"
      to: "benchmark aggregate depth_curve rows"
      via: "Campaign tables and plots consume per-depth blind/perturbed summaries"
      pattern: "depth_curve|depth_curve_recovery"
---

<objective>
Add deterministic depth-curve training evidence that reproduces the paper's qualitative blind-versus-perturbed behavior.

Purpose: CURV-01 through CURV-04 require a reproducible exact-target inventory, a measured proof suite, aggregate depth metrics, and artifact preservation semantics that later proof reporting can lock down.
Output: target inventory, proof suite, aggregate/report plumbing, campaign preset, docs, and regression tests.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-paper-depth-curve-training-evidence/32-CONTEXT.md
@.planning/phases/29-paper-claim-contract-and-proof-dataset-harness/29-VERIFICATION.md
@.planning/phases/30-bounded-shallow-blind-training-recovery/30-VERIFICATION.md
@.planning/phases/31-perturbed-basin-training-and-local-repair/31-VERIFICATION.md
@AGENTS.md
@src/eml_symbolic_regression/datasets.py
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/proof.py
@src/eml_symbolic_regression/campaign.py
@tests/test_benchmark_contract.py
@tests/test_benchmark_runner.py
@tests/test_benchmark_reports.py
@tests/test_campaign.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic exact depth-curve targets and dataset registration</name>
  <action>Create `src/eml_symbolic_regression/depth_curve.py` with an exact EML inventory at depths 2 through 6, each carrying explicit domains and provenance. Register those targets in `datasets.py` so proof dataset manifests and benchmark formulas resolve them as first-class demo specs. Add tests that assert deterministic IDs, depths, node counts, domains, manifest determinism, and verifier-owned recovery on the declared splits.</action>
  <verify>
    <automated>python -m pytest tests/test_depth_curve_targets.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add proof-depth-curve suite, validation, and aggregate reporting</name>
  <action>Extend `benchmark.py` with built-in suite `proof-depth-curve`, measured claim validation for `paper-blind-depth-degradation`, and aggregate helpers that summarize recovery rate, seed counts, runtime, and fit metrics by depth and training mode. Update `proof.py` so the claim enumerates both blind and perturbed rows and explicitly documents that perturbed rows are comparative evidence, not a bounded 100% claim. Add runner and aggregate tests that prove depth-4 blind failure stays visible while the paired perturbed row recovers.</action>
  <verify>
    <automated>python -m pytest tests/test_benchmark_contract.py tests/test_benchmark_runner.py tests/test_benchmark_reports.py tests/test_proof_contract.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add campaign/report hooks and user-facing reproduction commands</name>
  <action>Extend `campaign.py` and `README.md` with a `proof-depth-curve` preset, depth-curve tables/plot output, and reproduction commands that keep raw artifacts reproducible. Add campaign tests that confirm the preset appears in discovery, writes depth-curve CSV/report sections, and preserves the measured blind-boundary narrative.</action>
  <verify>
    <automated>python -m pytest tests/test_campaign.py -q</automated>
    <automated>PYTHONPATH=src python -m eml_symbolic_regression.cli benchmark proof-depth-curve --case depth-2-blind --case depth-2-perturbed --seed 0 --output-dir /tmp/eml-phase32-smoke</automated>
    <automated>PYTHONPATH=src python -m eml_symbolic_regression.cli campaign proof-depth-curve --case depth-2-blind --case depth-2-perturbed --seed 0 --output-root /tmp/eml-phase32-campaign --label phase32-depth-curve --overwrite</automated>
  </verify>
</task>

</tasks>
