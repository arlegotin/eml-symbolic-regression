---
phase: 92
plan: 92-01
name: v1.16 ablation and paper-figure assets
wave: 1
depends_on:
  - phase-90
  - phase-91
files_modified:
  - src/eml_symbolic_regression/paper_v116.py
  - src/eml_symbolic_regression/cli.py
  - tests/test_paper_v116.py
  - artifacts/paper/v1.16-geml/ablations/
requirements_addressed:
  - ABL-01
  - ABL-02
  - ABL-03
  - ABL-04
  - PAPER-01
autonomous: true
---

<objective>
Generate deterministic v1.16 ablation tables, failure examples, and paper-figure assets from the locked pilot campaign and budget ladder.
</objective>

<tasks>
<task id="1">
<read_first>
- src/eml_symbolic_regression/paper_v116.py
- artifacts/campaigns/v1.16-geml-pilot/tables/geml-paired-comparison.csv
- artifacts/campaigns/v1.16-geml-budget-ladder/failure-taxonomy.json
</read_first>
<action>
Add `write_v116_ablation_assets` and path helpers that write ablation table JSON/CSV/Markdown, failure-example JSON/CSV/Markdown, figure metadata, deterministic SVGs, source locks, and a manifest.
</action>
<acceptance_criteria>
- Ablation assets include initialization, branch guards, constants, depth, budget, and candidate pooling rows.
- Failure examples preserve exact-recovery, repaired, same-AST/warm-start, verified-equivalent, loss-only, and failure outcome separation.
- Manifest decision remains non-positive when the Phase 91 package is inconclusive.
</acceptance_criteria>
</task>
<task id="2">
<read_first>
- src/eml_symbolic_regression/cli.py
</read_first>
<action>
Expose a `geml-v116-ablations` command with output-dir, campaign-dir, budget-ladder-dir, package-dir, and overwrite arguments.
</action>
<acceptance_criteria>
- CLI parser resolves `geml-v116-ablations` to the new command function.
</acceptance_criteria>
</task>
<task id="3">
<read_first>
- tests/test_paper_v116.py
</read_first>
<action>
Add focused tests for ablation asset generation, source locks, figure metadata, failure examples, and CLI registration.
</action>
<acceptance_criteria>
- `python -m pytest tests/test_paper_v116.py -q` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python -m pytest tests/test_paper_v116.py -q` and generate `artifacts/paper/v1.16-geml/ablations/` via CLI.
</verification>

<must_haves>
- Deterministic tables and SVG figures
- Source-locked failure taxonomy
- Honest loss-only and no-recovery labeling
- Actionable next-step diagnostics for the inconclusive result
</must_haves>
