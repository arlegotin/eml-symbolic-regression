---
phase: 37-compiler-macro-shortening-and-warm-start-coverage
plan: 01
status: pending
type: execute
wave: 1
depends_on:
  - 36
files_modified:
  - src/eml_symbolic_regression/compiler.py
  - src/eml_symbolic_regression/benchmark.py
  - src/eml_symbolic_regression/cli.py
  - docs/IMPLEMENTATION.md
  - tests/test_compiler_warm_start.py
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Compiler tries explicit validated shortcuts before generic expansion and records which shortcuts hit or missed."
    - "Diagnostics report macro depth/node wins against the old no-macro expansion."
    - "Warm-start coverage expands only for formulas whose macro-shortened exact trees are validated and still verify after warm-start."
    - "Unsupported cases remain fail-closed when no validated macro applies or the shortened tree still exceeds the warm-start gate."
  artifacts:
    - path: "src/eml_symbolic_regression/compiler.py"
      provides: "Macro-aware compiler shortcuts plus macro diagnostics"
      exports: ["CompilerConfig", "CompileMetadata", "compile_and_validate", "diagnose_compile_expression"]
    - path: "src/eml_symbolic_regression/benchmark.py"
      provides: "Updated warm-start depth gates for macro-shortened formulas"
      exports: ["OptimizerBudget", "builtin_suite", "execute_benchmark_run"]
    - path: "src/eml_symbolic_regression/cli.py"
      provides: "User-visible warm-start defaults aligned with validated macro coverage"
      exports: ["run_demo", "build_parser"]
  key_links:
    - from: "src/eml_symbolic_regression/compiler.py"
      to: "tests/test_compiler_warm_start.py"
      via: "Macro hits, misses, and depth/node deltas must be locked by regression tests"
      pattern: "macro|template|diagnostic|depth"
    - from: "src/eml_symbolic_regression/compiler.py"
      to: "src/eml_symbolic_regression/benchmark.py"
      via: "Shorter exact trees should unlock warm-start coverage only through validated depth gates"
      pattern: "max_warm_depth|compile_and_validate|warm_start"
---

<objective>
Ship macro-aware compiler shortening and expand warm-start coverage only where the shortened exact tree is already validated and verifier-safe.

Purpose: COMP-01 through COMP-03 require a real shortcut layer, macro diagnostics, and user-visible warm-start coverage gains without weakening fail-closed behavior.
Output: macro-aware compiler metadata, a direct-division arithmetic shortcut, updated warm-start depth gates/defaults, and regression coverage for new supported and still-unsupported cases.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-compiler-macro-shortening-and-warm-start-coverage/37-CONTEXT.md
@AGENTS.md
@src/eml_symbolic_regression/compiler.py
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/cli.py
@src/eml_symbolic_regression/warm_start.py
@tests/test_compiler_warm_start.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Formalize macro-aware compiler diagnostics</name>
  <read_first>
    <file>src/eml_symbolic_regression/compiler.py</file>
    <file>tests/test_compiler_warm_start.py</file>
  </read_first>
  <action>Turn the existing shortcut path into an explicit macro layer, report macro hits/misses and baseline depth/node deltas, and preserve fail-closed semantics when macros do not apply.</action>
  <acceptance_criteria>
    <criterion>Compiler metadata exposes macro hits, misses, and baseline depth/node comparisons.</criterion>
    <criterion>Shockley retains the existing shortcut and reports a positive depth/node delta against the no-macro baseline.</criterion>
    <criterion>Canonical low-depth primitives such as `log` stay validated and locked by regression tests.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_compiler_warm_start.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add direct-division arithmetic shortcut and coverage gates</name>
  <read_first>
    <file>src/eml_symbolic_regression/compiler.py</file>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>src/eml_symbolic_regression/cli.py</file>
  </read_first>
  <action>Add a direct-division shortcut for `Mul(..., Pow(denominator, -1))`, then raise warm-start depth defaults only enough to admit validated macro-shortened formulas.</action>
  <acceptance_criteria>
    <criterion>Michaelis-style direct division compiles to a shallower exact tree than the old generic lowering.</criterion>
    <criterion>At least one currently depth-gated warm-start path becomes verified with the new defaults.</criterion>
    <criterion>Logistic, Planck, and damped oscillator still fail closed when their formulas remain unsupported or too deep.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_compiler_warm_start.py tests/test_benchmark_runner.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document macro coverage and diagnostics</name>
  <read_first>
    <file>docs/IMPLEMENTATION.md</file>
    <file>src/eml_symbolic_regression/compiler.py</file>
  </read_first>
  <action>Update implementation notes so the macro layer, coverage gains, and still-unsupported cases are documented honestly.</action>
  <acceptance_criteria>
    <criterion>Implementation notes explain macro hits/misses, depth/node deltas, and newly unlocked warm-start coverage without overstating unsupported formulas.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_compiler_warm_start.py -q</automated>
  </verify>
</task>

</tasks>
