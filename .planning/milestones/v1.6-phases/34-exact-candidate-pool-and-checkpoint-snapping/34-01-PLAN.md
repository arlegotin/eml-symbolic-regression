---
phase: 34-exact-candidate-pool-and-checkpoint-snapping
plan: 01
status: pending
type: execute
wave: 1
depends_on:
  - 33
files_modified:
  - src/eml_symbolic_regression/optimize.py
  - src/eml_symbolic_regression/warm_start.py
  - src/eml_symbolic_regression/basin.py
  - src/eml_symbolic_regression/benchmark.py
  - src/eml_symbolic_regression/cli.py
  - docs/IMPLEMENTATION.md
  - tests/test_optimizer_cleanup.py
  - tests/test_benchmark_runner.py
  - tests/test_verifier_demos_cli.py
requirements:
  - HARD-01
  - HARD-02
  - HARD-03
  - HARD-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Training runs an explicit late hardening window and emits exact snaps from selected checkpoints."
    - "Every restart contributes candidate provenance and exact-loss metrics to a retained candidate pool."
    - "When verification splits are available, final exact-candidate selection is verifier-gated and keeps the legacy soft-loss selector as fallback."
    - "CLI and benchmark artifacts expose the winning candidate provenance and the legacy fallback candidate."
  artifacts:
    - path: "src/eml_symbolic_regression/optimize.py"
      provides: "Hardening-stage candidate emission, exact-candidate pool manifests, and verifier-gated selector"
      exports: ["TrainingConfig", "FitResult", "fit_eml_tree"]
    - path: "src/eml_symbolic_regression/benchmark.py"
      provides: "Run artifact wiring and metrics extraction for winner/fallback candidate provenance"
      exports: ["execute_benchmark_run", "_extract_run_metrics"]
    - path: "tests/test_optimizer_cleanup.py"
      provides: "Optimizer regression coverage for checkpoint snapping and candidate-pool provenance"
    - path: "tests/test_benchmark_runner.py"
      provides: "Benchmark artifact coverage for winner/fallback selection exposure"
  key_links:
    - from: "src/eml_symbolic_regression/optimize.py"
      to: "src/eml_symbolic_regression/verify.py"
      via: "Candidate pool selection uses verifier-owned ranking when evaluation splits are available"
      pattern: "verify_candidate|VerificationReport"
    - from: "src/eml_symbolic_regression/benchmark.py"
      to: "src/eml_symbolic_regression/optimize.py"
      via: "Blind and wrapped training runs must serialize selected and fallback exact candidates"
      pattern: "fit_eml_tree|trained_eml_candidate|metrics"
    - from: "src/eml_symbolic_regression/warm_start.py"
      to: "src/eml_symbolic_regression/optimize.py"
      via: "Warm-start and basin flows should inherit candidate-pool selection by routing verification splits into the optimizer"
      pattern: "fit_eml_tree|verification_splits"
---

<objective>
Ship verifier-gated exact-candidate pooling for training outputs.

Purpose: HARD-01 through HARD-04 require explicit late hardening, retained exact snaps across restarts/checkpoints, verifier-owned final selection, and artifact-visible provenance for both the winning candidate and the legacy soft-loss fallback.
Output: optimizer candidate-pool support, winner/fallback manifest structure, blind/warm-start/basin artifact plumbing, and regression coverage for the new selection contract.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-exact-candidate-pool-and-checkpoint-snapping/34-CONTEXT.md
@AGENTS.md
@docs/IMPLEMENTATION.md
@src/eml_symbolic_regression/optimize.py
@src/eml_symbolic_regression/master_tree.py
@src/eml_symbolic_regression/verify.py
@src/eml_symbolic_regression/warm_start.py
@src/eml_symbolic_regression/basin.py
@src/eml_symbolic_regression/benchmark.py
@src/eml_symbolic_regression/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hardening checkpoints and exact-candidate pool manifests</name>
  <read_first>
    <file>src/eml_symbolic_regression/optimize.py</file>
    <file>src/eml_symbolic_regression/master_tree.py</file>
    <file>src/eml_symbolic_regression/verify.py</file>
    <file>tests/test_optimizer_cleanup.py</file>
  </read_first>
  <action>Extend the optimizer configuration and fit result so training can run an explicit late hardening stage, emit exact snapped candidates from selected checkpoints, retain legacy final-snap behavior as an explicit fallback candidate, and serialize full candidate provenance plus selection details in the optimizer manifest.</action>
  <acceptance_criteria>
    <criterion>The optimizer manifest includes a retained candidate pool with per-candidate provenance, margins, active-node counts, and exact post-snap loss.</criterion>
    <criterion>The manifest exposes both the selected candidate and the legacy fallback candidate.</criterion>
    <criterion>Hardening checkpoints can emit multiple exact snaps from the same restart rather than only a single final snap.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_optimizer_cleanup.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make selection verifier-gated across blind, warm-start, basin, and CLI paths</name>
  <read_first>
    <file>src/eml_symbolic_regression/benchmark.py</file>
    <file>src/eml_symbolic_regression/warm_start.py</file>
    <file>src/eml_symbolic_regression/basin.py</file>
    <file>src/eml_symbolic_regression/cli.py</file>
    <file>tests/test_benchmark_runner.py</file>
    <file>tests/test_verifier_demos_cli.py</file>
  </read_first>
  <action>Thread verification splits into optimizer-driven training flows so the selected exact candidate is ranked by verifier-owned criteria when available, and update run/report metrics to expose the winning restart/checkpoint provenance alongside the legacy fallback candidate.</action>
  <acceptance_criteria>
    <criterion>Blind benchmark and CLI training flows expose winner and fallback candidate provenance.</criterion>
    <criterion>Warm-start and perturbed-basin wrappers reuse optimizer-selected candidates instead of re-verifying an unrelated single final snap.</criterion>
    <criterion>Benchmark metrics extraction surfaces enough data to compare winner versus legacy fallback in later campaign/report phases.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_benchmark_runner.py tests/test_verifier_demos_cli.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Lock the phase contract in docs and run a focused regression slice</name>
  <read_first>
    <file>docs/IMPLEMENTATION.md</file>
    <file>src/eml_symbolic_regression/benchmark.py</file>
  </read_first>
  <action>Update implementation notes to describe candidate-pool and fallback semantics, then run a focused regression slice that covers optimizer manifests, benchmark artifacts, and the affected training wrappers.</action>
  <acceptance_criteria>
    <criterion>The implementation notes describe the exact-candidate pool and verifier-gated selection contract.</criterion>
    <criterion>The focused regression slice passes after the new selector lands.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_optimizer_cleanup.py tests/test_benchmark_runner.py tests/test_verifier_demos_cli.py tests/test_basin_targets.py tests/test_compiler_warm_start.py -q</automated>
  </verify>
</task>

</tasks>
