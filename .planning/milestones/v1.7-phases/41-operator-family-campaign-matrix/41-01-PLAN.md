# Phase 41 Plan: Operator-Family Campaign Matrix

**Status:** Ready
**Requirements:** EVD-01, EVD-02

## Goal

Users can run raw-vs-centered proof and showcase campaign matrices without overwriting archived v1.6 evidence.

## Tasks

1. Add reusable operator-family variant helpers for raw EML, fixed `CEML_s`, fixed `ZEML_s`, and scheduled `ZEML_s` continuation rows.
2. Add built-in v1.7 family suites for smoke, shallow pure blind, shallow scaffolded, perturbed basin, depth curve, standard, and showcase-style comparisons.
3. Add campaign presets for those suites with v1.7 names and isolated campaign output roots.
4. Ensure suite manifests and expanded run IDs include operator-family and schedule metadata.
5. Add contract and smoke tests for built-in suite expansion, centered budget metadata, unsupported centered basin behavior, and campaign preset registration.

## Success Criteria

1. `proof-shallow-pure-blind`, `proof-shallow`, `proof-basin`, and `proof-depth-curve` style matrices can be loaded as v1.7 family suites.
2. Standard and showcase-style family suites exist under v1.7 names and use isolated output locations.
3. Expanded runs preserve regime tags and add variant tags without proof-claim threshold overreach.
4. Raw, fixed centered, and continuation schedule variants produce distinct stable run IDs.
5. Tests cover suite registry, campaign preset registration, and a cheap family smoke execution path.

## Verification

Run:

```bash
python -m pytest tests/test_benchmark_contract.py tests/test_campaign.py tests/test_benchmark_runner.py::test_runner_executes_operator_family_smoke_matrix
```
