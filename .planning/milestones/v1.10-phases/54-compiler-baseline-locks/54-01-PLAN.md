---
phase: 54-compiler-baseline-locks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_compiler_warm_start.py
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
  - BASE-04
  - BASE-05
must_haves:
  truths:
    - "Logistic relaxed compile diagnostic is locked at depth 27 with no macro hits under the strict gate failure path."
    - "Planck relaxed compile diagnostic is locked at depth 20 with scaled_exp_minus_one_template and direct_division_template hits."
    - "Michaelis-Menten strict compile remains depth 12 with saturation_ratio_template."
    - "Arrhenius strict compile remains direct_division_template."
    - "Shockley strict compile remains scaled_exp_minus_one_template."
  artifacts:
    - path: "tests/test_compiler_warm_start.py"
      provides: "Compiler baseline regression tests for v1.10 motif work"
      contains: "test_compile_logistic_archived_relaxed_baseline"
---

<objective>
Add regression tests that pin the current compiler state before v1.10 motif changes.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock logistic and Planck relaxed diagnostics</name>
  <files>tests/test_compiler_warm_start.py</files>
  <behavior>
    - Logistic strict diagnostic remains unsupported with reason `depth_exceeded`.
    - Logistic relaxed metadata is exactly depth `27`, node count `77`, and macro hits `[]`.
    - Planck strict diagnostic remains unsupported with reason `depth_exceeded`.
    - Planck relaxed metadata is exactly depth `20`, node count `67`, and macro hits `scaled_exp_minus_one_template` plus `direct_division_template`.
  </behavior>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_compiler_warm_start.py::test_compile_logistic_archived_relaxed_baseline tests/test_compiler_warm_start.py::test_compile_planck_archived_relaxed_baseline -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen existing supported macro locks</name>
  <files>tests/test_compiler_warm_start.py</files>
  <behavior>
    - Michaelis-Menten strict compile remains depth `12`, node count `41`, and macro hit `saturation_ratio_template`.
    - Arrhenius strict compile remains depth `7` with macro hit `direct_division_template`.
    - Shockley strict compile remains strict-supported with macro hit `scaled_exp_minus_one_template`.
    - Default compiler gate remains `max_depth=13`, `max_nodes=256`.
  </behavior>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_compiler_warm_start.py::test_existing_macro_supported_demos_do_not_regress -q</automated>
  </verify>
</task>

</tasks>

<verification>
Run the targeted compiler warm-start tests:

```bash
PYTHONPATH=src python -m pytest tests/test_compiler_warm_start.py -q
```
</verification>

<success_criteria>
Phase 54 is complete when targeted tests lock the archived diagnostic baselines and the full compiler warm-start suite passes.
</success_criteria>
