---
phase: 96
plan: 01
name: verifier-first candidate ranking
wave: 1
depends_on: [95]
requirements: [NBR-02, RANK-01, RANK-02, RANK-03]
files_modified:
  - src/eml_symbolic_regression/paper_v117.py
  - src/eml_symbolic_regression/cli.py
  - tests/test_paper_v117.py
autonomous: true
---

<objective>
Rank Phase 95 candidate pools using verifier status before loss and produce explanation/accounting artifacts.
</objective>

<tasks>
<task type="auto">
<name>Add verifier-first ranking writer</name>
<read_first>
- src/eml_symbolic_regression/optimize.py
- src/eml_symbolic_regression/paper_v117.py
- tests/test_paper_v117.py
</read_first>
<action>
Add `write_v117_candidate_ranking()` to read `neighborhood-candidates.json`, classify every row, rank exact verifier statuses first, explain the selected candidate, and write ranked JSON/CSV/Markdown, source locks, and manifest.
</action>
<acceptance_criteria>
- `src/eml_symbolic_regression/paper_v117.py` contains `eml.v117_candidate_ranking.v1`.
- `tests/test_paper_v117.py` asserts recovered candidates outrank lower-loss failed candidates.
- `tests/test_paper_v117.py` asserts loss-only, fallback, original-snap, and exact recovery classes remain separate.
</acceptance_criteria>
</task>

<task type="auto">
<name>Add CLI registration</name>
<read_first>
- src/eml_symbolic_regression/cli.py
- tests/test_paper_v117.py
</read_first>
<action>
Register `geml-v117-rank-candidates` with output directory, neighborhoods directory, and overwrite options.
</action>
<acceptance_criteria>
- `src/eml_symbolic_regression/cli.py` contains `geml-v117-rank-candidates`.
- `tests/test_paper_v117.py` asserts parser registration.
</acceptance_criteria>
</task>
</tasks>

<verification>
python -m pytest tests/test_paper_v117.py -q
</verification>

<must_haves>
- Every neighborhood candidate is checked through the same verifier-status field before promotion.
- Ranking explains why the winner was selected and why lower-loss candidates failed.
- Tables separate exact recovery, verified equivalence, repair-only, loss-only, compile-only, same-AST, fallback, and original-snap outcomes.
- Existing recovery-accounting tests remain compatible with the new ranking fields.
</must_haves>
