---
milestone: v1.15
milestone_name: GEML family and i*pi EML exploration
audited: 2026-04-22
status: passed
scores:
  requirements: 28/28
  phases: 6/6
  integration: 6/6
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
nyquist:
  overall: skipped_by_config
  compliant_phases: []
  partial_phases: []
  missing_phases: []
---

# v1.15 Milestone Audit

## Status

**Passed.** All v1.15 requirements are satisfied, all six phase verifications passed, and the final GEML claim audit passed.

## Scope

Milestone goal: generalize EML into `GEML_a`, specialize `a = i*pi`, make restricted theory and branch behavior explicit, integrate the fixed family into training and snapping, add matched oscillatory and negative-control protocols, and end with a claim-safe paper-decision package.

Phases audited:

| Phase | Name | Verification |
|-------|------|--------------|
| 82 | GEML Family Semantics and Structural Identity | passed |
| 83 | i*pi EML Restricted Theory and Branch Contract | passed |
| 84 | Family-Aware Training and Snapping Integration | passed |
| 85 | Oscillatory Benchmark Pack and Negative Controls | passed |
| 86 | Matched EML versus i*pi EML Campaign Runner | passed |
| 87 | GEML Evidence Package and Claim Boundary | passed |

## Requirements Coverage

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| GEML-01 | 82 | satisfied | `GEML_a(x, y) = exp(a*x) - log(y)/a` is represented by `EmlOperator("geml_a")` and exact `Geml` AST nodes. |
| GEML-02 | 82 | satisfied | Raw EML canonicalizes to `a = 1`; i*pi EML is exposed through named helpers. |
| GEML-03 | 82 | satisfied | NumPy, PyTorch, and mpmath evaluator agreement is covered by focused tests. |
| GEML-04 | 82 | satisfied | Exact AST JSON, semantics docs, and SymPy export preserve family parameter metadata. |
| GEML-05 | 82 | satisfied | Structural identity tests cover representative `a` values including `a = i*pi`. |
| THRY-01 | 83 | satisfied | Positive-real reciprocal identity is executable-checked in theory artifacts. |
| THRY-02 | 83 | satisfied | Nested reciprocal recovery is executable-checked with branch assumptions. |
| THRY-03 | 83 | satisfied | Real-axis derivative and magnitude are checked. |
| THRY-04 | 83 | satisfied | One-step composition identity and magnitude bound are checked. |
| THRY-05 | 83 | satisfied | Theory artifacts include explicit non-claims against unsupported closure language. |
| BRAN-01 | 83 | satisfied | Principal complex-log branch convention is documented. |
| BRAN-02 | 83 | satisfied | Evaluator stats and branch helpers report proximity and crossing diagnostics. |
| BRAN-03 | 83 | satisfied | Branch guard metadata is manifest-visible and faithful verification remains unchanged. |
| BRAN-04 | 83 | satisfied | Verifier reports expose invalid-domain skips and branch-related failures. |
| TRN-01 | 84 | satisfied | `TrainingConfig.operator_family` flows through soft-tree training, hardening, snapping, and manifests. |
| TRN-02 | 84 | satisfied | i*pi optimizer coverage verifies snapped GEML artifacts and same-family candidate selection. |
| TRN-03 | 84 | satisfied | Candidate and run artifacts expose gradient, anomaly, branch, pre/post-snap, and wall-clock metrics. |
| TRN-04 | 84 | satisfied | Raw EML, verifier, repair, and v1.14 claim-accounting regressions pass. |
| BENCH-01 | 85 | satisfied | Periodic and harmonic targets include `sin_pi`, `cos_pi`, and `harmonic_sum`. |
| BENCH-02 | 85 | satisfied | Standing-wave and damped-oscillator coverage is registered. |
| BENCH-03 | 85 | satisfied | `log_periodic_oscillation` is registered on a positive safe domain. |
| BENCH-04 | 85 | satisfied | Negative controls include `exp`, `log`, polynomial, and rational targets. |
| BENCH-05 | 85 | satisfied | Matched raw/i*pi suites lock datasets, depths, budgets, constants, verifier gates, and split policy. |
| EVID-01 | 86 | satisfied | Campaign tables emit paired raw EML and i*pi EML rows by matched protocol key. |
| EVID-02 | 86 | satisfied | Paired outputs include recovery, loss, gradient, anomaly, branch, runtime, and environment fields. |
| EVID-03 | 87 | satisfied | Target-family classification includes declared targets, paired rows, wins, neutral/loss-only outcomes, and negative controls. |
| EVID-04 | 87 | satisfied | Claim audit rejects unsupported comparative, blind-recovery, and universality language. |
| EVID-05 | 87 | satisfied | Final package includes theory links, manifests, aggregate tables, source locks, reproduction commands, and a bounded decision. |

## Integration Check

The cross-phase GEML flow is intact:

| Flow | Status | Evidence |
|------|--------|----------|
| GEML semantics -> training integration | passed | Phase 82 exact operators are used by Phase 84 `operator_family` training paths. |
| Restricted theory -> branch diagnostics | passed | Phase 83 theory and branch contracts feed verifier and training artifact fields used later. |
| Training metrics -> campaign aggregates | passed | Phase 84 metrics are surfaced in Phase 86 paired campaign rows. |
| Benchmark manifests -> campaign runner | passed | Phase 85 matched suites are the presets consumed by Phase 86 campaign outputs. |
| Campaign outputs -> final package | passed | Phase 87 source-locks `geml-paired-comparison.csv` and `geml-paired-summary.json` from the v1.15 smoke campaign. |
| Claim boundary -> package decision | passed | Phase 87 claim audit passed and the package decision is `inconclusive_smoke_only`. |

## E2E Flows

| Flow | Status |
|------|--------|
| Represent and evaluate `GEML_a` across exact and numeric evaluators | passed |
| Build restricted i*pi theory and branch diagnostics | passed |
| Train/snap/verify with raw EML and fixed i*pi EML operators | passed |
| Generate matched benchmark suites and paired campaign outputs | passed |
| Assemble claim-safe final package with source locks and reproduction commands | passed |

## Deferred Work

- The full 20-row `geml-oscillatory` campaign remains a reproduction command rather than a checked-in campaign result. This is not a blocker because Phase 87's decision package is explicitly `inconclusive_smoke_only`.
- Continuous or learned `a` search remains FUT-01 and was intentionally out of scope.
- Formal theorem-prover certificates remain FUT-03 and were intentionally out of scope.

## Conclusion

v1.15 achieved its definition of done. The repo now has fixed-parameter GEML semantics, restricted i*pi theory with branch contracts, family-aware training/snap integration, matched raw-versus-i*pi benchmark protocols, paired campaign outputs, and a claim-safe final package. The evidence does not justify a stronger i*pi EML paper claim yet, and the package records that boundary directly.
